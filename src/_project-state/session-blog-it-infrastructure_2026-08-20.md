# Session — New blog post: "What 'IT infrastructure' actually means for a 10-person business" (2026-08-20)

One request: write and publish a new blog post to Vertex, authored by Lazar, in the
**Marketing** division. Copy was hand-written for this session (not generated by the
Phase 13B pipeline), but published through the same document shape and the same
quality gates so it is indistinguishable from a generated post in the Studio.

Everything below is what IS, not what should be.

---

## The post

| Field | Value |
|---|---|
| `_id` | `post-it-infrastructure-ten-person-business` |
| `slug` | `what-it-infrastructure-actually-means-for-a-10-person-business` (shared EN/MK) |
| Title EN | What "IT infrastructure" actually means for a 10-person business |
| Title MK | Што навистина значи „IT инфраструктура" за бизнис со десет луѓе |
| `author` | → `author-lazar` (Lazar — Web Development & AI, Vertex Marketing) |
| `division` | `marketing` |
| `status` | `published` |
| `publishedAt` | 2026-08-20 |
| `readTime` | 7 |
| Tags EN | IT infrastructure · hosting · email · small business |
| Tags MK | IT инфраструктура · хостинг · е-мејл · мал бизнис |
| Featured image | Pexels → Sanity asset `image-ce37e40d428dafd83881be1bf6c78acb8e376168-1880x1251-jpg` (server rack / patch cables, photo by Brett Sayles, https://www.pexels.com/photo/cables-connected-on-server-2881229/) |

Live at:
- https://www.vertexconsulting.mk/en/blog/what-it-infrastructure-actually-means-for-a-10-person-business
- https://www.vertexconsulting.mk/mk/blog/what-it-infrastructure-actually-means-for-a-10-person-business

### Topic provenance

The topic came off the existing backlog rather than being invented:
`topic-what-it-infrastructure-means-for-small-business` (seeded by `scripts/seed-topics.ts`,
division `marketing`, status was `pending`). The script closes it out the way
`createPost.ts` does — `status: 'used'`, `usedAt`, `resultingPost` → the new post — so the
13B/13C generator can never write this topic a second time.

The other pending marketing topic, `topic-website-costing-customers` ("Five signs your
website is costing you customers"), was deliberately **not** used: it duplicates the
already-published `post-website-costing-customers`. It is still `pending` in the backlog
and should probably be retired by hand.

### Structure (both locales)

7 × `h2` sections, EN 1083 words / MK 1039 words:

1. Infrastructure is six things, not a server room — domain, DNS, hosting, email, files, backups
2. The failures are quiet, not dramatic — the four realistic failure modes at this size
3. Email is usually the weakest part — SPF / DKIM / DMARC explained in one line each
4. Overpaying and underpaying are the same mistake
5. What "good" looks like at ten people — the six-item target state
6. The audit you can run yourself this afternoon — six questions
7. Where we come in — CTA

Internal links: `/marketing/it-infrastructure` (primary CTA) and `/consulting/it-systems`
(the cross-division "this is organisational, not technical" hand-off). Both resolve —
they are real routes, and the locale-aware `<Link>` branch in `BlogPostClient`'s
Portable Text `link` mark keeps the `/en` / `/mk` prefix.

Every factual claim about what Vertex does is taken from
`messages/en.json → marketing.itInfrastructure` (the service page copy) — the Cloudflare
mention, the "we document everything and hand it to you / no vendor lock-in" position,
and the hosting/email/DNS/backup/security scope. Nothing about the offering was invented.

---

## `scripts/publish-post-it-infrastructure.ts`

New one-off script, same family as `seed-blog.ts` / `seed-topics.ts`.

- **Env loading:** tries `.env.local`, then falls back to `env.local`. On this machine
  the values live in **`env.local`** (no leading dot) — which is also why the Sanity-backed
  blog is empty on a local `next dev`/`next start`: Next only auto-loads `.env.local`,
  so the local server has no `NEXT_PUBLIC_SANITY_PROJECT_ID` and `isSanityConfigured`
  is false. The existing seed scripts, which load `.env.local` only, cannot run as
  documented on this machine either.
- **Idempotent:** fixed `_id` + `createOrReplace`; re-runs reuse the already-uploaded
  featured image instead of pushing a duplicate into the media library, and preserve the
  original `publishedAt` so an edit does not shuffle the post to the top of the listing.
- **Guards:** aborts if the slug is owned by a different document, or if `author-lazar`
  is missing from the dataset.
- **Quality gates:** imports the real `validateDraft()` from
  `src/lib/contentGenerator/validateDraft.ts` and runs the hand-written copy through the
  exact gates Claude's output has to pass — word counts, ≥2 `h2` per locale, excerpt
  60–300 chars, 3–5 tags, read time 3–15, banned marketing phrases. All pass.
- **Portable Text:** inline `mdToPortableText()` with the same contract as `seed-blog.ts`,
  restricted to what `localizedPortableText` actually allows (`normal` + `h2`, `bullet`
  lists, `strong`, `link`).
- **Post-publish:** POSTs `/api/revalidate` with `x-revalidate-secret`, `{tag:'blog', slug}`.
  Targets `siteConfig.url` by default (override with `REVALIDATE_TARGET_URL`) because
  `NEXT_PUBLIC_SITE_URL` in `env.local` is `http://localhost:3000` — the generator's own
  revalidate step therefore only ever hits localhost from this machine.

Flags: `--check` (validate, no writes) · `--draft` (write as draft) · `--no-revalidate`.

---

## Verification (production, not local)

Local verification was not possible — see the `env.local` note above; the local server
renders the blog as empty. Verified against production instead:

- `/en/blog/<slug>` and `/mk/blog/<slug>` both render: full body, MARKETING division
  badge, `L` avatar, "Lazar — Web Development & AI, Vertex Marketing", 7 min read.
- `/en/blog` lists it first, above the Apr 5 AI-tools post.
- Featured image serves through `/_next/image` off `cdn.sanity.io` → **200**.
- `POST /api/revalidate` → `200 {"ok":true,"revalidated":"blog","indexNow":{"skipped":"no-key"}}`.
- `sitemap.xml` and `llms.txt` both contain the new post (they lag the flush by a minute
  or so; `llms.txt` picked it up first).

**Pre-existing, not introduced here:** `/mk` renders the post date as "August 20, 2026"
in English — the known Node ICU gap already logged as #29 in `TRANSLATION_NOTES.md`.
IndexNow is skipped entirely because `INDEXNOW_KEY` is unset in this environment.

---

## Not done

- **Nothing was committed.** The working tree already carried a large pile of unrelated
  staged changes from another session, so the new script + these doc updates were left
  uncommitted rather than swept into someone else's commit.
- **No social post.** The 13C Facebook/Instagram pipeline was not triggered — this went
  straight to Sanity.
- **MK copy is LLM-drafted**, like every other MK string on the site, and is queued for
  the same native-speaker review. New phrasing decisions are logged in
  `TRANSLATION_NOTES.md` (§ Blog post — IT infrastructure, 2026-08-20).
