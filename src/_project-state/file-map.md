# File Map — Vertex Consulting

Last updated: 2026-06-28 (Session — Lazar Portfolio Redesign /lazar)

## Root
| File | Description |
|------|-------------|
| `package.json` | Deps + `dev`/`build`/`start`/`lint` scripts |
| `package-lock.json` | npm lockfile |
| `tsconfig.json` | Strict TS, ES2017, `@/*` → `./src/*`, Next plugin |
| `tsconfig.tsbuildinfo` | TS incremental-build cache (gitignored) |
| `next.config.ts` | `NextConfig` scaffold wired through `withNextIntl`. As of Session H (2026-04-19), carries `experimental: { optimizePackageImports: ['motion', 'lucide-react', 'gsap'] }` — retained as zero-risk future-proofing (measured bundle delta was +0.3 KB noise per route because those three packages' canonical import patterns in this codebase already tree-shake under Next 16's default config). **Phase 13A (2026-04-24):** added `images.remotePatterns: [{ protocol: 'https', hostname: 'cdn.sanity.io' }]` so `next/image` can render Sanity CDN-served blog images. |
| `sanity.config.ts` | **Phase 13A (2026-04-24).** Sanity project config at the repo root (expected location for `next-sanity`). `defineConfig({ name: 'vertex-blog', projectId, dataset, basePath: '/studio', plugins: [structureTool(), visionTool()], schema: { types: schemaTypes } })`. Reads `NEXT_PUBLIC_SANITY_PROJECT_ID` + `NEXT_PUBLIC_SANITY_DATASET`. Imported by `src/app/studio/[[...tool]]/Studio.tsx` via `../../../../sanity.config`. |
| `sanity.cli.ts` | **Phase 13A (2026-04-24).** Sanity CLI config — required for ad-hoc `npx sanity ...` commands (dataset export, schema deploy, etc.). Reads the same project ID + dataset env vars. Not imported by the app. |
| `next-env.d.ts` | Next-generated types (gitignored) |
| `eslint.config.mjs` | Flat config extending eslint-config-next |
| `postcss.config.mjs` | Registers `@tailwindcss/postcss` |
| `components.json` | shadcn config — `base-nova` style, `neutral` base, lucide icons |
| `.gitignore` | Standard ignores + env + tsbuildinfo |
| `README.md` | Default Next.js bootstrap README |
| `D-15_Website_Design_Document.md` | Full product/design spec for the Vertex site |
| `.env.example` | Placeholders for the 4 Resend env vars (Session B — 2026-04-17) + the Phase 12 chat var (`ANTHROPIC_AUTH_TOKEN`) + 5 Phase 13A vars (`NEXT_PUBLIC_SANITY_*` × 2, `SANITY_API_READ_TOKEN`, `SANITY_API_WRITE_TOKEN`, `REVALIDATE_SECRET`) + **4 Phase 13B vars (2026-04-26): `VERTEX_PEXELS_API_KEY`, `VERTEX_ADMIN_PASSWORD`, `ANTHROPIC_API_KEY` (real key for the generator), `NEXT_PUBLIC_SITE_URL`**. The two `VERTEX_*` names are intentionally namespaced after a 13B debugging session found that the shell session shadowed unprefixed names like `ADMIN_PASSWORD`. No real values — safe to commit. |
| `.env.local` | **Gitignored.** Local dev values for everything in `.env.example`. Phase 13A added the 4 Sanity tokens + `REVALIDATE_SECRET`. **Phase 13B (2026-04-26) added `VERTEX_PEXELS_API_KEY`, `VERTEX_ADMIN_PASSWORD`, `ANTHROPIC_API_KEY` (real `sk-ant-api03-...`), `NEXT_PUBLIC_SITE_URL`.** Kept `ANTHROPIC_AUTH_TOKEN` as-is for the Phase 12 chat widget (the comment above it now warns not to reuse it for the generator — OAuth tokens 401 on direct Messages API calls). All values must also live in Vercel env vars (Production / Preview / Development). Never commit. |
| `.env.local.example` | **Phase 12 (2026-04-20).** Documents the 5 chat-widget env vars. `.gitignore` exempts this filename (`!.env.local.example`) so it's committable alongside `.env.example`. |
| `TRANSLATION_NOTES.md` | Phase 15B — working decisions on MK phrasing, plus font / date-format follow-ups. **Phase 12 (2026-04-20) added §12-A**: the `chat.*` namespace was LLM-drafted and wants a native-speaker pass (5–10 min review — five context-specific greetings, "Vertex асистент" mixed-script title, "интеграција со ИИ" Cyrillic AI). |

## messages/ (Phases 15A–15E — 2026-04-17)
| File | Description |
|------|-------------|
| `messages/en.json` | English translation dictionary. Namespaces: `common.*` (now including `skipToContent` — Session G); full `nav.*` (+ `nav.dropdown.*` + `nav.submenuToggleAria` — Session G + **`nav.themeToggle.{toLight, toDark}` — Phase L2 2026-04-23**); full `footer.*`; full `home.*`; `sections.*`; full `consulting.*` (meta, landing, serviceCommon, 4 service namespaces); full `marketing.*` (meta, landing — including `team.members.*` for Lazar/Petar/Andrej — serviceCommon, 4 service namespaces); `about.*` (meta, hero, values, team × 4 members, timeline, ctaBanner); `contact.*` (meta, hero, form × 20+ keys, info panel, map title); `privacy.*` (meta + pendingTranslationNotice only); `thankYou.*` (meta, headline, subtitle, cta); `blog.*` (meta, listing.hero + filters + empty, post.backLink + readTimeLong + relatedHeading); `home.projects.*` (Our Work section — 10 keys: overline/heading/subheading/divisionLabel/serviceLabel/viewProject/viewProjectAria/comingSoon/placeholder/imageAlt — Session — 2026-06-02). |
| `messages/mk.json` | Macedonian dictionary. Same structure as `en.json`; Phases 15B–15E translated every value. Session G added `common.skipToContent: "Прескокни до содржина"` and `nav.submenuToggleAria: "Отвори/затвори подмени за {label}"`. **Phase L2 (2026-04-23) added `nav.themeToggle.toLight: "Премини на светла тема"` and `nav.themeToggle.toDark: "Премини на темна тема"`.** **Session — 2026-06-02:** added `home.projects.*` (Our Work — same 10 keys as en.json; MK strings flagged in `TRANSLATION_NOTES.md`). ~7,300 total words of MK prose. Flagged phrasing decisions in `TRANSLATION_NOTES.md`. |

## .claude/
| File | Description |
|------|-------------|
| `launch.json` | Preview server configs — **(1)** `vertex-dev` on port 3000, `npm run dev`, autoPort (Turbopack dev server with HMR). **(2)** `vertex-prod` on port 3001, `npx next start -p 3001`, autoPort (production-build server; used for perf measurement without colliding with the dev server — added Session H 2026-04-19). Don't run both simultaneously: they conflict on `.next` directory writes during concurrent builds. |

## public/
| File | Description |
|------|-------------|
| `file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg` | Default Next.js starter icons — not used anywhere yet |
| `projects/.gitkeep` | **Session — 2026-06-02.** Empty tracked file keeping `public/projects/` in git. Goran drops client-project screenshots here (e.g. `public/projects/northgate-dental.jpg`) and references them from `src/config/projects.ts` as `image: '/projects/northgate-dental.jpg'`. |
| `projects/northgate.png` | **Session — 2026-07-12 (re-captured).** Screenshot of the Northgate Dental site (`northgate.optimind000.com`), 2560×1440 16:9. Wired into the "Our Work" card via `src/config/projects.ts` and the `/lazar` Work section via `src/config/lazar.ts`. Captured headless via `scripts/capture-projects.mjs`. Supersedes the 2026-06-20 capture (site redesigned). |
| `projects/sunset.png` | **Session — 2026-07-12 (re-captured).** Screenshot of the Sunset Services site (`sunsetservices.vercel.app`), 2560×1440 16:9. Wired into the "Our Work" card via `src/config/projects.ts` and the `/lazar` Work section via `src/config/lazar.ts`. Captured headless via `scripts/capture-projects.mjs`. Supersedes the 2026-06-20 capture (site redesigned). Near-losslessly re-encoded with sharp (palette, q92) to trim it from ~3.6 MB to ~1.3 MB — the only capture over the 1.5 MB budget. |
| `projects/daliborac.png` | **Session — 2026-07-12 (re-captured).** Screenshot of the Dalibor Plečić author site (`daliborac.vertexconsulting.mk`), 2560×1440 16:9. Wired into the "Our Work" card via `src/config/projects.ts` and the `/lazar` Work section via `src/config/lazar.ts`. Captured headless via `scripts/capture-projects.mjs`. Supersedes the 2026-06-20 capture — the site's real content has now shipped (the former `[PLACEHOLDER]` build is gone), so this shows the live author homepage. |
| `lazar.png` | **Session — Lazar Portfolio (2026-06-24).** Lazar's color portrait (background-removed PNG), processed with sharp (trim transparent margins → small top headroom + side padding → 426×486) and shown in the `/lazar` hero tile via `next/image` (`fill` + `object-cover object-top` + `priority`). **Redesign (2026-06-28):** now framed over a CSS dot-grid + `--division-glow` inside the brutalist 2px-bordered hero (was a gradient tile); still colour, still `next/image` + `priority`. Original source backed up at `/tmp/lazar_src.png`. |
| `lazar-bw.png` | **Session — Lazar Portfolio (2026-06-24).** Grayscale, face-centered head-and-shoulders crop (316×316) derived from the same portrait via sharp (alpha-based head locate → horizontally-centered square → `.grayscale()`). Fills Lazar's avatar circle in `TeamGrid` (About) + `TeamShowcase` (Marketing), **and (Redesign 2026-06-28) the 2px-framed portrait in `LazarAbout` on `/lazar`** (matching the approved Stitch design — the hero keeps the colour photo, the About section uses this B&W crop). |

## src/app/
| File | Description |
|------|-------------|
| `layout.tsx` | **Root layout, minimal pass-through (Session G — 2026-04-19).** Returns `children` directly — no `<html>`, no `<body>`, no fonts, no providers. Exports only `metadata` + `viewport` (Next.js merges metadata across the whole segment tree regardless of where in the layout tree the export lives). The HTML shell + font loading + all client providers + (as of Phase L1) the pre-hydration theme script live in `src/app/[locale]/layout.tsx` so `<html lang={locale}>` can be set per request — WCAG 3.1.1. `/api/*` routes never invoke layouts, so they bypass everything below this file. |
| `globals.css` | Tailwind v4 tokens, shadcn HSL tokens, three division theme blocks (`:root` shared default + `[data-division="consulting"]` + `[data-division="marketing"]`), keyframes, utility classes (`glass`, `noise`, `glow-hover`, prose-* rules), `@layer utilities` block holding `.focus-ring` and `.form-input-focus` (Session C — accessibility), reduced-motion fallback. Font tokens live in a dedicated `@theme inline` block at the top (Session F — 2026-04-17) so Tailwind generates `.font-heading { font-family: var(--font-heading), system-ui, sans-serif; }` directly from next/font's `<html>` class injection without a circular `:root` emission. `.gradient-text` + `.gradient-text-brand` removed in Session E. 6 `.prose-*` h2+h3 rules updated in Session F from `var(--font-sora)` → `var(--font-heading)`. Dead shadcn sidebar tokens removed in Session J (8 `--color-sidebar-*` aliases from `@theme inline` + 8 `--sidebar*: oklch(...)` definitions from `:root`). **As of Phase L1 (2026-04-23): light-mode foundation added.** Three new blocks. (1) `html { color-scheme: dark; }` + `html[data-theme="light"] { color-scheme: light; }` right after `@custom-variant dark` — tells the UA to render native scrollbars, form controls, and selection highlights in the matching theme. (2) `html[data-theme="light"] { --color-ink / surface / elevated / border / muted / bright }` override block right after the closing `}` of the main `@theme { }` block — flips the six grayscale core tokens to the Option 1 Crisp White palette (`#FFFFFF / #F8F9FA / #F1F3F5 / #E5E7EB / #9AA0AD / #0A0B12`). (3) Combined selector list `html[data-theme="light"], html[data-theme="light"] [data-division="consulting"], html[data-theme="light"] [data-division="marketing"]` inside `@layer base` right after the marketing block closes — overrides all ten `--division-*` tokens + nineteen shadcn HSL tokens to the same Crisp White palette. The naked `html[data-theme="light"]` selector entry in the list is there because `<body>` lives outside the `DivisionProvider` wrapper and reads `--division-*` from the `:root` cascade; it also covers the `:root` HSL fallbacks. Dark values left byte-identical to pre-phase. The pre-hydration inline script in `[locale]/layout.tsx` writes `data-theme` on `<html>` before React hydrates. **As of Phase L4 (2026-04-23):** three token blocks added after the main `@theme` close and before the L1 `--color-*` light override — (a) BorderGlow tokens (`--borderglow-glow` RGB triplet 241,243,247 → 10,11,18; `--borderglow-bg` chains via `var(--division-surface)` so needs no light override), (b) Glass surface tokens (`--glass-bg` / `--glass-border` / `--glass-bg-strong` / `--glass-border-strong` for subtle panels; `--glass-nav-bg` / `--glass-nav-border` for the navbar backdrop at higher alpha), (c) Nav hover overlay token (`--nav-hover-bg`, replaces `hover:bg-white/5`). `.glass` utility rewritten to consume the variables; the redundant `[data-division="consulting"]` and `[data-division="marketing"]` `.glass` variants were deleted (post-Finishing-Touches v2 they held identical values to the base — dead duplication). `.glass-nav` rewritten to consume its own token pair. Dead `[data-division="marketing"] .glow-hover:hover` rule deleted (grep confirmed zero callers; inline comment in the rule said "superseded by BorderGlow in Phase 4"). `@keyframes chat-trigger-pulse` rewritten to use `rgba(var(--borderglow-glow) / α)` so the bright-on-dark pulse ring inverts to dark-on-light in light mode. L1 token tune: light-mode `--division-text-muted` raised `#9AA0AD` → `#5F6670` for WCAG AA compliance on footer muted links (contrast 2.49:1 → 5.50:1). **As of Phase L3 (2026-04-24):** ten new hero-background CSS variables added under `:root` (dark = live pre-phase values) and `html[data-theme="light"]` (recalibrated palette) — `--hero-grid-bg`, `--hero-panel-stop-1/2/3`, `--hero-panel-stripe-bright/shadow`, `--hero-panel-border`, `--metallic-panel-shadow`, `--hero-scrim-center/mid`. `.metallic-panel` class (consulting hero GridMotion cells) rewritten to consume the variables: `background-color`, radial-gradient stops, repeating-linear-gradient stripe alphas, `border`, `box-shadow` all theme-aware. Dark values match live pre-phase byte-for-byte (regression-clean); light values invert to dark-titanium-on-light-surface metallic palette (center darker than edge). **As of Phase L6 (2026-04-24):** two new token pairs added right after the L3 hero-token block — (a) `--silk-opacity` (`1` dark / `0.55` light — retuned from L6's initial `0.3` in Session — Silk Visibility + Scroll-Resume Fix; see `BackgroundSilk.tsx`) consumed by `BackgroundSilk.tsx`'s outer wrapper to dampen the shader output in light mode where the pattern's dark zones would otherwise compete with the headline; (b) `--division-border-hover` (`rgba(245,245,245,0.19)` dark / `rgba(10,11,18,0.19)` light) consumed by `DivisionSplit.tsx`'s hover border so the hover-more-visible-than-resting-border affordance works in both themes without hardcoded rgba literals. **Session — Cursor & Micro-Interactions (2026-06-24):** added micro-interaction tuning tokens (`--vt-theme-duration: 520ms`, `--magnetic-pull: 0.30`, `--magnetic-max: 12px`, `--magnetic-squish: 0.94`); top-level `::view-transition-old/new(root) { animation: none }` (+ reduced-motion repeat) so JS owns the theme wipe; replaced `.typing-dot` + `@keyframes typing-dot-pulse` with the 7px `typing-dot-wave` (rest 0.35 / crest 1.0 −2.5px lift, `:nth-child` delays 0/0.095/0.19s, colored `--division-text-muted`); reworked `@keyframes chat-trigger-pulse` + the `.chat-trigger` animation into a scaling `.chat-trigger::before` halo ring (1.5px `--division-text-primary`, scale 1→1.6 / opacity 0.5→0, 2.4s linear, z-index −1); added `.footer-link` (`::after` draw-from-left 1.5px underline + text2→text), `.nav-link` (background-size gradient underline + text2→text), and `.cta-sheen` (`isolation:isolate` + a z-index −1 `::after` neutral-gray sweep band, `cta-sheen-sweep` keyframe). All grayscale, all reduced-motion-guarded. |
| `favicon.ico` | Default favicon |
| `opengraph-image.tsx` | **Session K (2026-04-19).** Next.js file-convention OG image generator. Edge-compatible `ImageResponse` producing a 1200×630 branded PNG (`#141414` background, white "V" tile + wordmark, "We help businesses grow smarter." headline with subtle white→`#A3A3A3` gradient on "smarter.", division-dot row, `vertexconsulting.mk` URL). Exports `alt` / `size` / `contentType` route-segment config. Uses system-ui font fallback — explicit Google Fonts loading was tried and dropped because satori (the renderer powering `ImageResponse`) only supports ttf/otf and Google Fonts v2 API serves woff2 (`Unsupported OpenType signature wOF2` at render). Lives at `src/app/` (above `[locale]/`) so it cascades site-wide. Static-prerendered at build time (`○ /opengraph-image` in the route table). |
| `twitter-image.tsx` | **Session K (2026-04-19).** One-line re-export of `opengraph-image` — `export { default, alt, size, contentType } from './opengraph-image'`. Keeps Twitter/X card in sync with OG via a single source of truth. |
| `proxy.ts` | **next-intl edge handler (Phase 15A).** `createMiddleware(routing)` with three matcher entries (`/`, `/(en\|mk)/:path*`, catch-all exclusion). Next 16 renamed `middleware` → `proxy`; a file called `middleware.ts` works but emits a deprecation warning. **Session K (2026-04-19): matcher exclusion list extended** from `(?!api\|_next\|_vercel\|.*\..*)` to `(?!api\|_next\|_vercel\|opengraph-image\|twitter-image\|.*\..*)` so the locale-neutral OG file-convention routes (URLs without dots) serve directly without being 307-redirected to `/en/opengraph-image` (which 404s because the file lives at `src/app/`, not `[locale]/`). Sitemap/robots routes already bypassed the matcher via the `.xml`/`.txt` extensions. **Phase 13A (2026-04-24):** matcher exclusion extended to `(?!api\|_next\|_vercel\|studio\|...)` for `/studio/**`. **Phase 13B (2026-04-26):** extended again to `(?!api\|_next\|_vercel\|studio\|admin\|opengraph-image\|twitter-image\|.*\..*)` so `/admin/**` (the content-generator dashboard) bypasses i18n locale-prefixing too. |
| `sitemap.ts` | **Phase 15F; async as of Phase 13A (2026-04-24).** Emits `/sitemap.xml`. Iterates every static path × every locale with `<xhtml:link hreflang>` entries for `en`, `mk`, `x-default`. Excludes noIndex pages (`/privacy`, `/thank-you`). Includes blog slugs from `await getAllSlugs()` (Sanity-backed since Phase 13A; the default export is now `async function sitemap(): Promise<MetadataRoute.Sitemap>`). **Lazar Portfolio (2026-06-24):** `/lazar` added to `STATIC_PATHS` (after `/about`) → `/en/lazar` + `/mk/lazar` emitted with hreflang alternates. Not in `robots.ts` disallow (crawlable). |
| `robots.ts` | **Phase 15F.** Emits `/robots.txt`. `Allow: /` + disallow the 4 noIndex paths + `/api/`. Points at sitemap URL. |

## src/i18n/ (Phase 15A — 2026-04-17)
| File | Description |
|------|-------------|
| `routing.ts` | `defineRouting({ locales: ['en','mk'], defaultLocale: 'en', localePrefix: 'always' })` + `Locale` type export. |
| `request.ts` | `getRequestConfig` — validates `requestLocale` with `hasLocale`, dynamically imports `messages/${locale}.json`. Wired into `next.config.ts` via `createNextIntlPlugin('./src/i18n/request.ts')`. |
| `navigation.ts` | `createNavigation(routing)` exports — `Link`, `redirect`, `usePathname`, `useRouter`, `getPathname`. Use these instead of `next/link` / `next/navigation` for anything locale-aware. |

## src/app/[locale]/ (Phase 15A — 2026-04-17)
| File | Description |
|------|-------------|
| `layout.tsx` | **Locale-scoped layout, owns the HTML shell + fonts (Session G — 2026-04-19).** `generateStaticParams` returns all locales. Awaits `params`, validates with `hasLocale`, calls `setRequestLocale`, loads messages via `getMessages`. Loads Archivo (headings, 400/500/600/700/800, latin + latin-ext) + Source Serif 4 (body, 400/500/600/700, latin + latin-ext) via `next/font/google`, exposing CSS variables `--font-heading` / `--font-body`. Renders `<html lang={locale} className={archivo.variable + sourceSerif.variable} suppressHydrationWarning>` → `<head>` (Phase L1 — 2026-04-23) containing the synchronous `<script dangerouslySetInnerHTML={{ __html: themeInitScript }} />` that reads `localStorage['vertex-theme']` → falls back to `matchMedia('(prefers-color-scheme: dark)')` → writes `data-theme` on `<html>` before React hydrates (no flash-of-wrong-theme) → `<body className="min-h-screen font-body antialiased overflow-x-hidden">` → `ThemeProvider` (Phase L1 — outermost provider) → `NextIntlClientProvider` → `MotionWrapper` → `DivisionProvider` → `ScrollProgress` → children → `BackToTop` → `ChatWidget`. The old `<div lang={locale} data-locale={locale}>` wrapper div was deleted in Session G — `lang` now lives on `<html>` where WCAG 3.1.1 evaluates it. No static `data-theme` attribute on the JSX `<html>` — the inline script owns that attribute (`suppressHydrationWarning` keeps React quiet about the resulting server-client diff). The `themeInitScript` constant is defined at module scope at the top of the file, just below imports. |
| `not-found.tsx` | **Phase 15F.** Localized 404 page. Translated title / description / CTA from the `notFound.*` namespace. Falls back to EN copy if locale can't be resolved at the root level. |

## src/app/[locale]/(site)/
| File | Description |
|------|-------------|
| `layout.tsx` | Site layout — now `async` server component (Session G — 2026-04-19). Uses `getTranslations('common')` to pull the skip-link label. Renders the skip-to-content `<a href="#main-content">` (fixed position, -translate-y off-screen at rest, `focus-visible:translate-y-0` to slide in on keyboard focus, colored via `var(--division-accent)` / `var(--division-bg)`) before `<Navbar />`. `<main>` gained `id="main-content"` so the anchor target resolves. `<Footer />` unchanged. |
| `page.tsx` | Homepage — 6 sections: `HeroSection` (Silk bg), `DivisionSplit`, `ServicesOverview`, `ProjectsShowcase` ("Our Work" — Session — 2026-06-02), `SocialProof`, `CTABanner`. **Server component (Session H Part 3 — 2026-04-19)** — async with `await getTranslations('home')`. Locale flows in via the parent `[locale]/layout.tsx`'s `setRequestLocale(locale)` call, so no params needed. `ServicesOverview` + `ProjectsShowcase` (both server) render directly; other sections stay client. `ProjectsShowcase` is self-contained (renders its own `<Section>` + heading), inserted as a single bare `<ProjectsShowcase />` line between Services and SocialProof (section comments renumbered 4→6). |

## src/app/[locale]/(site)/consulting/
| File | Description |
|------|-------------|
| `page.tsx` | Consulting landing — **server component** exporting metadata and rendering `<ConsultingLandingClient />` |
| `ConsultingLandingClient.tsx` | Full landing content: `HeroSection` (with `BackgroundGrid`, plain-white headline), `ConsultingServicesGrid`, `LeaderIntro` for Goran, `CTABanner`. **Server component (Session H Part 3 — 2026-04-19)** — `useTranslations` swapped for `await getTranslations` on both `consulting.landing` and `nav.dropdown` namespaces, filename kept to minimise churn but the "Client" suffix is now a naming carryover. BackgroundGrid renders inside HeroSection (client) via props; server parent → client child is fine under RSC. |
| `business-consulting/page.tsx` | Full service page — metadata + `<ConsultingServicePage>` with 5 `<h2>` prose, 4 process steps, 5 FAQ items |
| `workflow-restructuring/page.tsx` | Full service page — metadata + `<ConsultingServicePage>` with long-form content, 4 process steps, 4 FAQ items |
| `it-systems/page.tsx` | Full service page — metadata + `<ConsultingServicePage>` with long-form content, 4 process steps, 4 FAQ items |
| `ai-consulting/page.tsx` | Full service page — metadata + `<ConsultingServicePage>`; related-services link crosses into `/marketing/ai-development` |

## src/app/[locale]/(site)/marketing/
| File | Description |
|------|-------------|
| `page.tsx` | Marketing landing — **server component** exporting metadata and rendering `<MarketingLandingClient />` |
| `MarketingLandingClient.tsx` | Full landing: `HeroSection` (with `BackgroundPlasma`, inherits HeroSection's inline-style `--division-text-primary` color), `MarketingServicesGrid`, `TeamShowcase` on surface-tinted section, `CTABanner`. Session E removed the `headlineClassName="gradient-text"` prop and the two `text-[#A3A3A3]` overlines are now `text-[var(--division-text-muted)]`. **Server component (Session H Part 3 — 2026-04-19)** — async with `await getTranslations` on `marketing.landing` + `nav.dropdown`; BackgroundPlasma renders inside HeroSection (client) via props. **Lazar Portfolio (2026-06-24):** the `members` map now sets `href: key === 'lazar' ? '/lazar' : undefined` so Lazar's `TeamShowcase` card links to his portfolio. |
| `web-design/page.tsx` | Full service page — metadata + `<MarketingServicePage>` with long-form content, 4 process steps, 5 FAQ items |
| `social-media/page.tsx` | Full service page — metadata + `<MarketingServicePage>` with long-form content, 4 process steps, 4 FAQ items |
| `it-infrastructure/page.tsx` | Full service page — cross-links related-services to `/consulting/it-systems` |
| `ai-development/page.tsx` | Full service page — cross-links related-services to `/consulting/ai-consulting` |

## src/app/[locale]/(site)/ (shared pages)
| File | Description |
|------|-------------|
| `about/page.tsx` | Server component — metadata + renders `<AboutPageClient />` |
| `about/AboutPageClient.tsx` | 4 `<Section>`s (story/values/team/timeline) + custom `CTABanner`. **Server component (Session H Part 3 — 2026-04-19)** — async with `await getTranslations('about')`; `t.raw('hero.paragraphs')` still works under server context. Renders server sections (ValuesGrid, TeamGrid, CompanyTimeline) directly; CTABanner is client but that's fine. Filename kept (not renamed). **Lazar Portfolio (2026-06-24):** `TEAM_KEYS` entries gained an `href` field (Lazar → `/lazar`, others `undefined`), passed through to each `TeamGridMember` so Lazar's card links to his portfolio. |
| `lazar/page.tsx` | **Session — Lazar Portfolio (2026-06-24).** Lazar Dinov's personal portfolio at `/lazar` (`/en/lazar` + `/mk/lazar`). Server component — `generateMetadata` via `generatePageMetadata({ path: '/lazar' })` (indexed, no `noIndex`) + `setRequestLocale` + renders 5 sections in order: `LazarHero` → `LazarAbout` → `LazarSkills` → `LazarWork` → `LazarContact`. Lives in `(site)` so it inherits the real navbar + footer + shared (neutral) division theme. Reached only via Lazar's card on `/about` + `/marketing` (not in the navbar). The `Lazar*` sections are imported directly (not via the `sections/index.ts` barrel). **(Redesign 2026-06-28: the five sections were re-skinned to the approved brutalist Stitch design; `page.tsx` itself is unchanged — same imports, same order.)** |
| `contact/page.tsx` | Server component — metadata + renders `<ContactPageClient />` |
| `contact/ContactPageClient.tsx` | Client component — header, 5-col form+info grid, embedded Google Maps iframe with dark-theme filter |
| `blog/page.tsx` | Server component — metadata + renders `<BlogListingClient />` |
| `blog/BlogListingClient.tsx` | Client component — header, 4 filter pills, responsive `BlogCard` grid with keyed stagger on filter change, empty state, default `CTABanner` |
| `blog/[slug]/page.tsx` | Server component — `generateStaticParams` awaits Sanity-backed `getAllSlugs()` + returns `{ locale, slug }` pairs for every combination (6 total). Async `generateMetadata` + `BlogPostPage` both await `params` (Next 16), then `await getPostBySlug(slug, locale)` and `await getRelatedPosts(slug, locale, 2)` server-side. Passes `post` + `related` as props to `BlogPostClient`. **Phase 13A (2026-04-24):** refactored for async data layer — no more synchronous mock lookups. |
| `blog/[slug]/BlogPostClient.tsx` | Client component — back link, header with author block, prose content rendered via `@portabletext/react`'s `<PortableText value={post.body} components={portableTextComponents} />`, optional related posts (2 same-division, passed in as a prop — no more `getRelatedPosts` call in the client), default `CTABanner`, inline `BlogPosting` JSON-LD. `Link` comes from `@/i18n/navigation`. **Phase 13A (2026-04-24):** swapped from regex + `dangerouslySetInnerHTML` to Portable Text. Author pill now sources `post.author.name` + `post.author.initials` + `post.author.role` from the new nested author shape. `portableTextComponents` defines custom handling for inline links — internal (`/…`) paths go through the locale-aware `Link`, everything else opens in a new tab with safe rel attributes. |
| `privacy/page.tsx` | Full 13-section privacy policy (EN) on the shared theme — `max-w-3xl` reading width, `prose-blog` body, `<hr>` dividers, bordered third-party-services `<table>` (Vercel/Cloudflare/Resend/Google Maps), italicized disclaimer, `noIndex: true`. Session A — 2026-04-17. |
| `thank-you/page.tsx` | Post-contact confirmation — centered message, `noIndex: true` |

## src/app/api/
| File | Description |
|------|-------------|
| `chat/route.ts` | **Phase 12 (2026-04-20).** Node runtime (`export const runtime = 'nodejs'`), `dynamic = 'force-dynamic'`. POST accepts `{messages, pageUrl, locale}`, validates (≤40 messages, ≤2000 chars per message, valid role, valid locale), builds the system prompt via `buildSystemPrompt()` and pipes `streamAIResponse()` through a `ReadableStream` of raw UTF-8 text chunks. Headers: `text/plain; charset=utf-8`, `no-cache`, `X-Accel-Buffering: no` (prevents Vercel proxy from buffering the stream). Kill switch via `NEXT_PUBLIC_CHAT_ENABLED=false` → 503. |
| `chat/lead/route.ts` | **Stub** POST → `{ok:true, route:"chat/lead"}`. Reserved for Phase 12B lead capture |
| `contact/route.ts` | **Removed 2026-07-13.** The contact form now posts directly to Formspree (`NEXT_PUBLIC_VERTEX_FORMSPREE_ENDPOINT`), so this Resend-backed route no longer exists. See `ContactForm.tsx`. |
| `newsletter/route.ts` | POST — honeypot-checks, validates email, adds to Resend Audience `RESEND_AUDIENCE_ID` via `resend.contacts.create`, sends a welcome email to the subscriber. Duplicate adds return success without leaking list membership. Session B — 2026-04-17. |
| `revalidate/route.ts` | **Phase 13A (2026-04-24).** POST-only tag-based revalidation. Reads `x-revalidate-secret` header, validates against `REVALIDATE_SECRET` env var (401 on mismatch or missing secret), then calls `revalidateTag(tag, 'max')` (Next 16 two-arg form; `'max'` invalidates immediately). Default tag is `'blog'`; request body `{ tag?: string }` overrides. The Phase 13B orchestrator POSTs here when `publish: true` is passed; 13C will continue to use the same endpoint after social posting. |
| `generate-post/route.ts` | **Phase 13B (2026-04-26), extended Phase 13C (2026-05-01).** Cookie-gated POST endpoint that drives the content generator. `runtime: 'nodejs'`, `dynamic: 'force-dynamic'`, `maxDuration: 60` (Hobby cap; localhost-only in practice — full pipeline takes ~2-3 min with FB+IG+Telegram). Validates the `vertex-admin` cookie against `VERTEX_ADMIN_PASSWORD` (401 on mismatch). Reads 4 flags from the body: `publish?: boolean`, `postToFacebook?: boolean`, `postToInstagram?: boolean`, `sendTelegramNotification?: boolean` (default true). Returns a `text/event-stream` Response, streaming `LogFn` lines as `data: { level, msg, ts }` SSE chunks. Wraps the orchestrator in a try/catch so any uncaught error becomes a final `data: { level: 'error', ... }` event before the controller closes. |

## src/app/studio/[[...tool]]/ (Phase 13A — 2026-04-24)
| File | Description |
|------|-------------|
| `page.tsx` | Server component. Exports `{ metadata, viewport }` re-exported from `next-sanity/studio` + `export const dynamic = 'force-static'` + default export renders `<Studio />`. Must stay a server component — exporting `metadata` is disallowed in `'use client'` files, which is why the NextStudio renderer was split into a sibling client component. |
| `Studio.tsx` | `'use client'` component. Imports `NextStudio` from `next-sanity/studio` and `config` from `../../../../sanity.config`, returns `<NextStudio config={config} />`. Client-only because NextStudio owns a huge React tree with its own providers, editors, and ref-based DOM manipulation. |

## src/sanity/ (Phase 13A — 2026-04-24)
| File | Description |
|------|-------------|
| `schemas/index.ts` | Schema registry — exports `schemaTypes = [blogPost, author, topicBacklog, localizedString, localizedText, localizedPortableText]`. Consumed by `sanity.config.ts`. **Phase 13B (2026-04-26):** `topicBacklog` added. |
| `schemas/topicBacklog.ts` | **Phase 13B (2026-04-26).** Document type for the AI generator's input queue. Fields: `title` (localizedString — the seed angle, AI may refine into a sharper final title), optional `description` (localizedText), `division` (3-way radio), optional `targetService` (radio of the 8 service paths so the AI knows what to internally link), optional `assignedAuthor` (reference — overrides the division-default), `priority` (number 1-10), `status` (radio: pending/used/skipped/failed, default pending), read-only `usedAt` + `resultingPost` (reference) + `failureReason` (set automatically by the orchestrator). Custom Studio preview prefixes status with an emoji (⏳/✅/❌/⏭️) for at-a-glance backlog scanning. |
| `schemas/blogPost.ts` | `blogPost` document type. Fields: `title` (localizedString), `slug` (shared EN/MK, sourced from `title.en`), `excerpt` (localizedText), `body` (localizedPortableText), `author` (reference to `author`), `division` (radio: consulting / marketing / shared), `publishedAt` (datetime), `readTime` (number 1–60), `tags` (object with en/mk string arrays), `featuredImage` (optional image with hotspot + localized alt), `status` (radio: draft / published, default draft). Default ordering: `publishedAt desc`. Preview uses `title.en`. |
| `schemas/author.ts` | `author` document type. Fields: `name`, `initials` (≤3 chars), `role` (localizedString), `division` (same 3-way radio as blogPost), optional `image`. Preview shows name + division + image. |
| `schemas/localized.ts` | Three reusable object types. `localizedString` + `localizedText` each have `{ en, mk }` string/textarea pair. `localizedPortableText` wraps two arrays of Portable Text blocks, allowing `normal` + `h2` styles, `strong` + `em` decorators, `link` annotations (with `href` string), and `bullet` lists. Every translated field on `blogPost` / `author` uses one of these wrappers. |

## scripts/ (Phase 13A onwards)
| File | Description |
|------|-------------|
| `seed-blog.ts` | **Phase 13A (2026-04-24).** One-shot migration of the 3 original mock posts + 2 authors into Sanity. Run via `npx tsx scripts/seed-blog.ts`. Idempotent: every document uses a fixed `_id` (`author-goran`, `author-lazar`, `post-workflow-overhaul`, `post-website-costing-customers`, `post-ai-tools-2026`) and `createOrReplace` so re-running updates in place. Includes an inline `mdToPortableText()` that handles the structures present in the original mock: `## h2`, paragraphs, `- bullet lines`, `**bold**`, `[label](url)`. Dev-only; Phase 13B's AI generator writes Portable Text directly via Claude structured output. |
| `seed-topics.ts` | **Phase 13B (2026-04-26).** Seeds the topic backlog with 20 starter topics derived from Vertex's 8 service pages (2-3 per service + 1 shared). Same idempotent pattern as `seed-blog.ts` (fixed `_id` like `topic-three-questions-before-hiring-consultant`, `createOrReplace`). Each topic has bilingual title + bilingual notes, division, target service path, and priority (1-10). Run via `npx tsx scripts/seed-topics.ts`. |
| `capture-projects.mjs` | **Session — 2026-07-12.** Playwright script that re-captures all three client-project screenshots into `public/projects/` at 2560×1440 (viewport 1280×720 @ 2× DPR, above-the-fold, `reducedMotion: 'reduce'`). Targets the exact locale pages the "Our Work" / `/lazar` cards link to (`northgate.optimind000.com/en`, `sunsetservices.vercel.app/`, `daliborac.vertexconsulting.mk/mk`). Run `node scripts/capture-projects.mjs` whenever a client site is redesigned. Filenames are fixed (`northgate` / `sunset` / `daliborac`) so no config change is ever needed. Robust page-load (prefers `networkidle`, falls back to `load` for chatty sites); best-effort cookie-banner dismissal; exits non-zero if any capture fails. Needs Playwright + Chromium, installed on-demand (`npm install --no-save playwright && npx playwright install chromium`) — deliberately NOT a committed dependency, to keep the refresh diff to just the images + this script. |

## src/app/admin/ (Phase 13B — 2026-04-26)
| File | Description |
|------|-------------|
| `layout.tsx` | Provides `<html lang="en">` + `<body>` shell for `/admin/*` routes. Required because the root `src/app/layout.tsx` is a pass-through (the `[locale]/layout.tsx` owns the public shell, but admin lives outside `[locale]`). Imports `globals.css` so Tailwind utilities work. `metadata.robots = { index: false, follow: false }` so search engines never crawl the panel. |
| `login/page.tsx` | Server component with a `'use server'` form action. POST → password compare against `process.env.VERTEX_ADMIN_PASSWORD` → set httpOnly `vertex-admin` cookie (path `/`, 7-day max-age, `secure: true` only in production so localhost works) → redirect to `/admin/generate`. Wrong password redirects back with `?error=1` and an inline alert. Self-contained: no imports from any other admin file. |
| `generate/page.tsx` | Server component. Cookie-checks before rendering (redirect to login on mismatch). Fetches up to 30 topics via GROQ ordered by `status` then `priority`, calls `getTokenHealth()` (Phase 13C — Meta token age check), and passes both to `<GenerateClient>`. `dynamic = 'force-dynamic'` so the cookie + topic-list state always reflects the latest Sanity write. |
| `generate/GenerateClient.tsx` | `'use client'` UI for the dashboard. **Phase 13C (2026-05-01)** added the Meta token health banner (rendered above the stats grid when `tokenHealth.status !== 'ok'`, amber for `warning`, red for `expired`) and 2 new checkboxes — "Post to Facebook" + "Post to Instagram", both auto-disabled and visually muted when "Publish immediately" is unchecked (gating social on publish). Stats cards (Pending / Used / Failed counts), "Next topic" preview, "Publish immediately" checkbox, "Generate next post" button. On click: POST to `/api/generate-post` with `{ publish, postToFacebook: publish && fb, postToInstagram: publish && ig }`, read SSE stream chunk-by-chunk via `body.getReader()` + `TextDecoder`, parse `data: {...}` lines into log entries, render colored log lines (info gray / warn amber / error red / done green). After the stream closes, sets a 1.2s timeout to `window.location.reload()` so the topic list refreshes with the new statuses. Three sections below the controls: Pending (next-up first), Failed (with reasons), Recently used (top 5). |
| File | Description |
|------|-------------|
| `index.ts` | Exports `BackgroundSilk`, `BackgroundPlasma`, `BackgroundGrid` |
| `BackgroundSilk.tsx` | Wrapper — dynamic-imports `Silk`, reduced-motion fallback. **As of Phase L3 (2026-04-24):** imports `useTheme` from `@/components/global`. `color` prop is now fully optional; when omitted, the wrapper resolves `theme === 'light' ? SILK_COLOR_LIGHT : SILK_COLOR_DARK` (dark value = pre-phase live) and passes it to `<Silk color={...} />`. Silk's existing `useMemo` already had `color` in deps, so prop changes propagate to the `uColor` uniform without canvas remount — theme swap is seamless (canvas count stays 1 across toggle click, animation continues). Reduced-motion fallback (`var(--division-bg)`) unchanged. **L6 (2026-04-24):** outer wrapper `<div>` gained `style={{ opacity: 'var(--silk-opacity, 1)' }}` — theme-aware opacity drops in light mode so the shader's high-contrast pattern output (native math: `uColor × pattern 0.2–1.0 + noise`) reads as a subtle flowing texture rather than a dominant feature competing with the hero headline. Dark-mode render byte-identical (opacity `1`). **Session — Silk Visibility + Scroll-Resume Fix (2026-04-24):** theme-aware colour retuned — `SILK_COLOR_DARK = #2A2D33`, `SILK_COLOR_LIGHT = #C5CBD3` (retuned alongside `--silk-opacity` 0.3 → 0.55 — L6's 0.3 + near-white `#D8DCE2` was past subtle into barely-there; 0.55 + `#C5CBD3` is visibly atmospheric without competing with the headline). |
| `Silk.tsx` | R3F Canvas running GLSL silk/wave shader on a plane mesh. **As of Session H (2026-04-19): Canvas is wrapped in a ref'd `<div>` + a `useState<'always'\|'never'>('always')` drives `<Canvas frameloop>`.** A viewport-gating `useEffect` installs `IntersectionObserver({ threshold: 0 })` on the wrapper plus a `document.visibilitychange` listener; both AND-ed through an `apply()` that flips `frameloop` to `'never'` when the wrapper is offscreen OR the tab is hidden — R3F's `useFrame` tick fully halts (zero CPU/GPU). Initial state `'always'` so the above-fold hero renders on first frame. The pre-existing three-step resize-dispatch timers (R3F use-measure workaround) are untouched. **Session — Silk Visibility + Scroll-Resume Fix (2026-04-24):** `useFrame` delta clamped at `1 / 30` (~33ms) before being scaled by `0.1` into the `uTime` uniform, to prevent `uTime` jumping forward by the full pause duration when the IO/visibility gate flips `frameloop` `'never'` → `'always'` after the hero scrolls back on-screen or the tab re-foregrounds. Normal 60/30 fps frames are always at/below the clamp, so only the resume tick is affected — no steady-state behavior change. |
| `BackgroundPlasma.tsx` | Wrapper — dynamic-imports `Plasma`, reduced-motion fallback. **As of Phase L3 (2026-04-24):** imports `useTheme` from `@/components/global`. Same wrapper-level tokenization pattern as `BackgroundSilk`: `color` prop optional; when omitted, resolves `theme === 'light' ? '#2A2D33' : '#F5F5F5'` (dark value = pre-phase marketing-hero call-site value). Unlike Silk, Plasma's single `useEffect` has `color` in its deps and tears down the whole OGL renderer when color changes — theme flip on marketing hero causes a brief canvas re-init (~150ms). Acceptable since theme flips are rare explicit user actions; a flash-free swap would require splitting Plasma's renderer-setup effect from a uniform-update effect via a ref. |
| `Plasma.tsx` | Raw OGL renderer + fragment shader, optional mouse interaction. **As of Session H (2026-04-19): rAF loop is gated and uses a local `simSeconds` time accumulator.** The loop body tracks `let lastFrame = 0; let simSeconds = 0` inside the effect; each frame computes `dt = lastFrame === 0 ? 0 : (t - lastFrame) * 0.001` then advances `simSeconds += dt` — sim-time only advances while the loop is active, so a pause followed by resume doesn't skip-jump the shader forward by however long the tab was hidden (`lastFrame = 0` on start zeroes the first resume frame). `start()`/`stop()` closures guard the rAF with a `raf === 0` sentinel. An `IntersectionObserver({ threshold: 0 })` on `containerEl` + a `document.visibilitychange` listener drive `start`/`stop` via a shared `apply()`; initial `inView = true` so the marketing hero renders immediately on first frame. Pre-existing mouse-interactive uniforms, ResizeObserver on `containerEl`, pingpong direction logic, and canvas teardown are all preserved. |
| `BackgroundGrid.tsx` | Wrapper — default 28-item consulting vocabulary list. **As of Phase L3 (2026-04-24):** the two hardcoded `backgroundColor: '#121212'` declarations (on the outer wrapper `<div>` and on the reduced-motion fallback `<div>`) are now both `'var(--division-bg)'`. One-line-each pre-existing theme-unawareness fix — the reduced-motion fallback IS the hero for users with `prefers-reduced-motion: reduce`, so getting it theme-correct matters. |
| `GridMotion.tsx` | GSAP-driven parallaxing DOM grid, mousemove-driven inertia. **As of Phase L3 (2026-04-24):** the outer `<section>` `backgroundColor` in `variant="panels"` mode (the consulting hero) changed from hardcoded `'#0E0E0E'` to `'var(--hero-grid-bg)'` so it flips with the theme. Cell JSX (`<div className="metallic-panel" aria-hidden="true" />`) was NOT modified — all panel theme-awareness lives in the rewritten `.metallic-panel` CSS class in `globals.css`, which consumes seven new CSS variables (`--hero-panel-stop-1/2/3`, `--hero-panel-stripe-bright/shadow`, `--hero-panel-border`, `--metallic-panel-shadow`). GSAP ticker logic, transforms, grid layout, `willChange`, and mouse-parallax math all preserved verbatim. **As of Session H (2026-04-19): the `gsap.ticker.add(updateMotion)` callback is attached/detached via IntersectionObserver + visibilitychange.** A `tickerAttached` boolean guards double-attach; `attach()`/`detach()` call `gsap.ticker.add`/`.remove` respectively — when paused, gsap does zero per-frame work for this component (not just skips the body, fully detaches). On resume, in-flight `gsap.to(row, {...})` tweens continue or overwrite naturally because gsap manages its own tween timelines independent of the ticker callback. IO observes `gridRef.current`; visibilitychange listens on `document`. Initial `inView = true` so the consulting hero grid animates from first frame. |

## src/components/global/
| File | Description |
|------|-------------|
| `index.ts` | Barrel — exports `AnimateIn`, `StaggerContainer`, `StaggerItem`, `MotionWrapper`, `DivisionProvider`, `ScrollProgress`, `BackToTop`, `Section`, `Navbar`, `Footer`, `ThemeProvider` (default) + `useTheme` (named — Phase L1 2026-04-23), and (Phase L2 — 2026-04-23) `ThemeToggle` (default). |
| `MotionWrapper.tsx` | `<MotionConfig reducedMotion="user">` wrapper (client) |
| `DivisionProvider.tsx` | Reads pathname, sets `data-division` attr on a wrapper div |
| `ThemeToggle.tsx` | **Phase L2 — 2026-04-23.** Client component (85 lines). Sun/Moon icon button consumed by `Navbar` in two places (desktop right cluster immediately left of the language toggle, and mobile overlay top-right cluster). Uses `useTheme()` from `ThemeProvider`. Renders `<Button variant="ghost">` with a hand-tuned className mirroring the language toggle's pill shape (`min-h-[44px] min-w-[44px] h-auto w-auto px-3 py-1.5 rounded-full hover:bg-white/5`) — none of the CVA `icon-*` sizes match, so the sibling match is className-based. Outer className STABLE across the mount boundary (only attributes + inner content swap) to avoid the Button primitive's `transition-all` registering a stuck CSS opacity transition at `currentTime: 0` when React patches className post-mount. SSR renders an icon-free placeholder (empty `<span>` inside an `aria-hidden="true"` button) → no wrong-icon flash. `AnimatePresence mode="wait"` + `motion.span` crossfade on swap (opacity + 90° rotate + scale 0.7↔1 over 0.2s, ease: 'easeOut'). Translated aria-label via `nav.themeToggle.{toLight, toDark}`. `.focus-ring` utility for a11y; 44×44 touch target floor met. **Phase L4 (2026-04-23):** `hover:bg-white/5` in `BUTTON_SHAPE` converted to `hover:bg-[var(--nav-hover-bg)]` for theme-aware hover overlay. **Session — Cursor & Micro-Interactions (2026-06-24):** the click handler now passes the cursor coords — `onClick={mounted ? (e) => toggleTheme({ x: e.clientX, y: e.clientY }) : undefined}` — so `ThemeProvider`'s circular View-Transition wipe expands from the button. Mounted guard preserved. |
| `ThemeProvider.tsx` | **Phase L1 — 2026-04-23.** Client component (99 lines). Provides `ThemeContext` with `{ theme: 'light' \| 'dark', setTheme, toggleTheme }`. Initial state `'dark'` matches the server render (no `window` / `localStorage` access during SSR); mount `useEffect` syncs state to whatever the pre-hydration inline script wrote on `<html data-theme="...">`. `setTheme(next)` writes `localStorage['vertex-theme']` (nested try/catch in case localStorage is unavailable) + calls `applyTheme` (which writes the attribute + updates state). `toggleTheme()` flips and calls `setTheme`. Second `useEffect` subscribes to `window.matchMedia('(prefers-color-scheme: dark)')`; OS changes only apply when localStorage is empty so explicit user choice wins forever after. Exports named `useTheme()` hook that throws `'useTheme must be used within a ThemeProvider'` if called outside the provider. Mounted as the outermost provider in `[locale]/layout.tsx` so `useTheme()` is available inside Navbar / Footer / BackToTop / ChatWidget / every section. **Session — Cursor & Micro-Interactions (2026-06-24):** `toggleTheme` is now `(origin?: { x: number; y: number }) => void` (no-arg callers still get an instant swap — backward-compatible). New `setThemeAnimated(next, origin?)` drives `document.startViewTransition(() => setTheme(next))` and, on `transition.ready`, runs a WAAPI `clipPath` circle from `0px` to the furthest-corner radius over `VT_DURATION` (520ms, `cubic-bezier(0.4,0,0.2,1)`) on `::view-transition-new(root)`. Reduced-motion / no `startViewTransition` / aborted-`ready` (`.catch`) all fall back to the instant `setTheme` — the theme flip always commits because the update callback runs regardless. |
| `AnimateIn.tsx` | `whileInView` single-element animator, default `fadeInUp`. Client component. Now used by server-component sections (`LeaderIntro`) to compose a scroll entrance without importing `motion` directly (Session H Part 3 — 2026-04-19). |
| `StaggerContainer.tsx` | `whileInView` parent for `staggerItem` children. Client component. |
| `StaggerItem.tsx` | **New Session H Part 3 (2026-04-19).** Client component — `motion.<as>` wrapper with the shared `staggerItem` variant by default. Supports `as` of `'div' \| 'li' \| 'article' \| 'section' \| 'span'`. Exists so server-rendered sections can compose stagger animations without importing `motion` directly — pairs with the existing `StaggerContainer` wrapper. |
| `ScrollProgress.tsx` | 2px spring-smoothed top progress bar, color = `--division-accent` |
| `BackToTop.tsx` | Circular back-to-top button, appears after 500px scroll |
| `Section.tsx` | Server component section shell — `py-20 md:py-28` + `max-w-7xl` |
| `Navbar.tsx` | **Fully implemented** — scroll-hide, glass on scroll, **desktop dropdowns now W3C disclosure pattern (Session G — 2026-04-19)**: each parent with children renders `<Link>` + a dedicated `<button aria-haspopup="menu" aria-expanded aria-controls aria-label={t('submenuToggleAria', { label })}>` chevron trigger; Escape closes + returns focus to trigger via `disclosureRefs`; `onBlur`/`onMouseLeave` keep the menu open if keyboard focus is still inside; child Links are plain links (no `role="menuitem"`) so Tab traverses. **Mobile full-screen overlay now focus-trapped via `inert`**: `useEffect` on `mobileOpen` toggles `main.inert` + `footer.inert` imperatively via `document.querySelector`, Escape closes menu + returns focus to the hamburger (`document.getElementById(hamburgerId).focus()`). All decorative icons (ChevronDown / Menu / X / Globe / new disclosure chevron) carry `aria-hidden="true"`. Active-link underline (`layoutId="nav-underline"`) moved from inside the Link to the wrapper so it spans both Link and disclosure button. Language toggle button, CTA button, `useId()`-derived stable ids for hamburger + menus. **Phase L2 (2026-04-23):** imported `ThemeToggle` via relative import (`./ThemeToggle`, not the barrel — Navbar is re-exported from the barrel, so using the barrel here would be a minor circular-import risk). Inserted `<ThemeToggle className="hidden md:inline-flex" />` as the first child of the desktop right cluster (immediately before the language toggle). Created a new `<motion.div className="absolute top-[72px] right-4 flex items-center gap-2">` as the first child of the mobile overlay's root `<motion.div>`, holding `<ThemeToggle />` + the relocated language toggle. The language toggle was moved from its Session C location at the bottom of the overlay's flex-col (inside a `<motion.div className="mt-2">` after the "Get in Touch" CTA) into this new cluster. The `top-[72px]` y-offset puts the cluster 16px below the 56px fixed header's bottom edge, clear of the X close button (which stays in the header at z-50 above the overlay at z-40). Language toggle keeps its `setMobileOpen(false); toggleLocale()` onClick; theme toggle does NOT close the overlay (users can see the theme flip reflected on the page before dismissing the menu). **Phase L4 (2026-04-23):** all 4 `hover:bg-white/5` patterns (dropdown-child link at line 297, desktop language toggle, hamburger, mobile-overlay language toggle) converted to `hover:bg-[var(--nav-hover-bg)]` for theme-aware hover overlay. **Session — Cursor & Micro-Interactions (2026-06-24):** the 5 top-level desktop nav labels (Consulting / Marketing / About / Blog / Contact) gained the `.nav-link` class on their non-active branch (active item omits it + keeps its inline accent color so the gradient underline never doubles up with the `layoutId` underline); their inline `color` + dead `hover:text-*` + `transition-colors` were dropped so `.nav-link` owns the text2→text hover + underline. The desktop "Get in Touch" pill is now wrapped in `<MagneticButton className="hidden md:inline-flex">` (responsive visibility on the wrapper so its forced inline-flex doesn't leave a phantom mobile flex gap) with `cta-sheen` added to the inner Link. **Session — Client Portal button (2026-07-12):** added a secondary/outline "Client Portal" link (`nav.clientPortal`) that points OUT to `https://portal.vertexconsulting.mk/login` — a plain `<a>` (external origin, not the i18n `<Link>`), styled with `border` + `--division-border` / `--division-text-secondary` tokens, sitting immediately left of the "Get in Touch" CTA on desktop and stacked above it (in a `flex-col gap-3` wrapper) at the bottom of the mobile overlay. To fit the extra button, the header inner switched from the centered `grid grid-cols-[1fr_auto_1fr]` to `flex … gap-6 lg:gap-8` with the logo + nav **left-grouped** and the action cluster pushed right via `ml-auto` (logo/cluster gained `shrink-0`, logo lost `justify-self-start`, cluster lost `col-start-3 justify-self-end`). The whole desktop bar (nav + ThemeToggle + language toggle + Client Portal + CTA) moved from the `md:` breakpoint to **`lg:`**, and the hamburger + mobile overlay from `md:hidden` to `lg:hidden` — below `lg` the 5-link nav + two-button cluster can't share the bar without overflowing iPad-portrait (768px), so 768–1023 now uses the hamburger menu (which carries every page + both buttons). Verified centered/no-overflow at 1024/1280/1440 and hamburger fallback at 768; light + dark both read correctly. |
| `Footer.tsx` | **Fully implemented** (Phase 7) — gradient accent line, newsletter CTA strip (stateful — POSTs to `/api/newsletter` with honeypot, shows submitting/success/error states; Session B — 2026-04-17). **Newsletter input now carries `aria-invalid={newsletterStatus === 'error'}` + `aria-describedby` pointing to a `<p id role="alert">` error element; submit button carries `aria-busy={newsletterStatus === 'submitting'}`; error clears on edit (Session G — 2026-04-19).** 4-column grid (logo+contact / consulting / marketing / company), social icons row, dynamic-year copyright, back-to-top. Division-aware via CSS vars. Three inline brand-icon SVGs (Linkedin/Instagram/Facebook) because `lucide-react@1.8.0` ships no brand marks. `socialLinks` array reads hrefs from `siteConfig.social` (Session A — 2026-04-17). **Phase L4 (2026-04-23):** top-edge hairline separator `style={{ backgroundColor: '#262626' }}` → `'var(--division-border)'` so it flips cleanly to `#E5E7EB` in light mode. Social-icon row `hover:bg-white/5` converted to `hover:bg-[var(--nav-hover-bg)]`. Brand SVGs already used `stroke="currentColor"` (verified). **Session — Cursor & Micro-Interactions (2026-06-24):** the consulting/marketing/company column links gained `.footer-link` (and shed their redundant Tailwind color/transition utilities — `.footer-link` owns the text2→text + draw-from-left underline). The newsletter `<form>` is now `position:relative` and renders `{newsletterStatus === 'success' && <Confetti />}` (anchored to the form rather than the button because the success UI swaps the button out — the grayscale burst still fans up over the subscribe region). The Subscribe `<Button>` is wrapped in `<MagneticButton>` with `cta-sheen` added. Imports `MagneticButton` + `Confetti` from `@/components/ui`. |

## src/components/sections/ (Phase 8 + Phase 9 + Phase 10 + Phase 11)
| File | Description |
|------|-------------|
| `index.ts` | Barrel — 19 total: Phase 8–10 thirteen + Phase 11 five (`ContactForm`, `TeamGrid`, `CompanyTimeline`, `ValuesGrid`, `BlogCard`) + `ProjectsShowcase` (Session — 2026-06-02). **Lazar Portfolio (2026-06-24): the five `Lazar*` sections are intentionally NOT in this barrel** — they are page-specific to `/lazar` and imported directly by `lazar/page.tsx`. |
| `LazarHero.tsx` | **Brutalist redesign (2026-06-28; orig. 2026-06-24).** The `/lazar` page's only client component (`'use client'` — animate-on-mount). Brutalist bordered hero frame (`border-2 border-[var(--division-text-primary)]`, sharp corners) split by a `lg:border-r-2` vertical rail: left = overline mono chip + oversized uppercase `LAZAR DINOV` (`font-heading` `font-extrabold`) + mono role + left-ruled tagline + two square CTAs (magnetic `cta-sheen` `#work` primary + outlined `<Link href="/contact">` secondary); right = the colour `/public/lazar.png` via `next/image` (`fill object-cover object-top priority`) framed over a CSS dot-grid + `--division-glow` with an aria-hidden registration-mark SVG (mobile gets a `border-t-2` divider, removed on `lg`). `motion` from `motion/react`; reuses `heroHeadline`/`heroSubtitle`/`heroCTA` variants + `MagneticButton`. All colour via `--division-*` tokens; `font-mono` technical labels. |
| `LazarAbout.tsx` | **Brutalist redesign (2026-06-28; orig. 2026-06-24).** Server component. Bordered document panel (`border-x-2 border-b-2`) with a 12-col grid: sticky left = numbered `01 //` mono chip + oversized uppercase heading + the existing grayscale `/public/lazar-bw.png` as a 2px-framed portrait (`next/image`, alt = the hero name); right = lead paragraph (`text-3xl`) + supporting prose (`t.raw('paragraphs')`) + the 4 `t.raw('chips')` as bordered mono tags above a hairline. Reuses `AnimateIn`. |
| `LazarSkills.tsx` | **Brutalist redesign (2026-06-28; orig. 2026-06-24).** Server component (`<section id="expertise">`). Numbered `02 //` chip + oversized heading, then the brutalist **no-gap bordered services table** — a `StaggerContainer` grid (`border-l-2 border-t-2`, cols 1/2/3) of 6 `StaggerItem` cells (`border-b-2 border-r-2`, hover-fill) each with an inline grayscale line icon (index-aligned `ICONS[]`, scaled `[&_svg]:h-8`, now keyed) + a `// 0n` mono code + uppercase title + desc from `t.raw('items')`. **`BorderGlow` dropped here** (a glow contradicts the flat brutalist table; it is reused on the Work cards instead). |
| `LazarWork.tsx` | **Brutalist redesign (2026-06-28; orig. 2026-06-24).** Server component (`<section id="work">` — the hero `#work` anchor). Numbered `03 //` chip + heading + subheading, then an offset 2-col grid (`md:mt-20` zig-zag on the right column) of `lazarProjects` (from `src/config/lazar.ts`): each a `BorderGlow` (`borderRadius={0}`) wrapping the card `<a target="_blank" rel="noopener noreferrer">` (+ aria-label) with a 2px-framed `aspect-video` screenshot (grayscale→colour + scale on hover), a header row (name + bordered `label` tag), desc, and a full-width `bg-[var(--division-accent)]` "View project →" bar (hand-rolled inline arrow SVG — lucide `ExternalLink` removed). Hatch-field "& more" card stays. |
| `LazarContact.tsx` | **Brutalist redesign (2026-06-28; orig. 2026-06-24).** Server component. Full-bleed **INVERTED** closing band — `bg-[var(--division-text-primary)]` with every descendant on `--division-bg` (dark = white block / black text per the Stitch design; light = the clean inverse). Numbered `04 //` chip + oversized uppercase heading + subtext, a magnetic `cta-sheen` square "Get in touch" `<Link href="/contact">`, a `mailto:` to `siteConfig.contact.emailMarketing` (`marketing@vertexconsulting.mk`), and a 3-square personal-social row (inline GitHub/LinkedIn/Instagram SVGs) reading the real URLs from `lazarSocials` in `src/config/lazar.ts`. (No longer the `--color-ink` "deepest surface" — the redesign inverts it.) |
| `HeroSection.tsx` | Reusable hero — takes `headline`, `subtitle`, `buttons[]`, `headlineClassName`, and `children` (for a background component). Uses `heroHeadline`/`heroSubtitle`/`heroCTA` animation variants. **Session — Cursor & Micro-Interactions (2026-06-24):** each hero button `<a>` is now wrapped in `<MagneticButton>`; the filled `primary` button also gets `cta-sheen` (the outline `secondary` does not). |
| `DivisionSplit.tsx` | Homepage-only. Two interactive hover cards for Consulting and Marketing. `useState` tracks hovered division; inline styles swap card bg to division theme color on hover. Uses `Briefcase` + `Megaphone` + `ArrowRight` from lucide. **L6 (2026-04-24): hover text color fix + rgba-literal tokenization.** Dropped `group-hover:text-white` on h3 title (base `--division-text-primary` already readable in both hover states across both themes); CTA arrow row changed `group-hover:text-white` → `group-hover:text-[var(--division-text-primary)]`. Hover `borderColor` `rgba(245,245,245,0.19)` → new `var(--division-border-hover)` token. Icon container `backgroundColor` `rgba(245,245,245,0.08)` → `var(--division-glow)`. Service chip `backgroundColor` `rgba(245,245,245,0.06)` → `var(--division-glow)`; chip `color` `rgba(245,245,245,0.8)` → `var(--division-text-secondary)`. Small dark-mode aesthetic drift on chip text (pre-L6 perceived `~#CB`, post-L6 solid `#A3A3A3`) accepted as documented tradeoff for theme-agnostic chips. |
| `ServicesOverview.tsx` | All 8 services in a single stagger grid. Each card has a division indicator dot, icon, title, description. Consulting cards glow white on hover, Marketing cards glow lavender. Icons: `Briefcase`, `Settings`, `Monitor`, `Brain`, `Globe`, `Share2`, `Server`, `Cpu`. **Server component (Session H Part 3 — 2026-04-19)** — async, uses `await getTranslations('home.servicesOverview')`, composes `<StaggerContainer>` + `<StaggerItem>` + `<BorderGlow>` instead of inline `motion` elements. **L6 (2026-04-24): hover text color fix.** Icon `text-gray-400 group-hover:text-white` → `text-[var(--division-text-secondary)] group-hover:text-[var(--division-text-primary)]`. h3 `group-hover:text-white transition-colors` dropped — BorderGlow provides hover affordance. |
| `ProjectsShowcase.tsx` | **Server component (Session — 2026-06-02).** "Our Work" homepage section — heading block + `StaggerContainer` grid of `BorderGlow` cards from `src/config/projects`. Renders `next/image` when a project has an `image`, else a grayscale placeholder (faint initial + "Screenshot coming soon"); renders an external "View project" `<a target="_blank" rel="noopener noreferrer">` + lucide `ExternalLink` when `href` is set, else a muted "Coming soon". Pulls copy from `home.projects.*` (EN + MK). Self-contained: own `<Section id="work">` + `AnimateIn` heading + `StaggerContainer amount={0.1}` / `StaggerItem` grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`). `BorderGlow` is the **default** import (`@/components/ui/BorderGlow`) with `borderRadius={12} glowRadius={40} glowIntensity={0.8} coneSpread={25} animated={false}` and NO `colors` prop (grayscale defaults — no rainbow); inner card uses `overflow-hidden rounded-[12px]` to clip the screenshot to the card radius (no extra border → no double border), exactly like `BlogCard`. All colors via `--division-*` tokens + `.overline`/`text-h2`/`text-h3`/`text-small` utilities (theme-aware — renders correctly in light + dark). Neutral division dot uses `--division-text-muted` per the "division signaled by label" convention. |
| `SocialProof.tsx` | 4 stat counters with `AnimatedCounter` child component + founder quote. Uses a raw `IntersectionObserver` (threshold 0.3) to trigger a 2-second `setInterval` count-up — Motion's `useInView` was unreliable with React 19 / Next 16 / motion 12. **Grid ladder is `grid-cols-2 md:grid-cols-4 gap-8 lg:gap-12` (Session I — 2026-04-19)** — 2-col on narrow phones (counters comfortably fit in 156px cells at 375px), flips to 4-col at `md:` (768px) so the full 768–1023px tablet range gets the desktop-style stats row instead of the old awkward 2×2 layout. |
| `CTABanner.tsx` | Full-width section with gradient background (reads `--division-accent-muted` + `--division-bg` + `--division-accent`). Optional props `headline`, `subtext`, `buttonText`, `buttonHref` all default to the "Ready to grow your business?" / `/contact` combo. **Session — Cursor & Micro-Interactions (2026-06-24):** the filled CTA `<Link>` is now wrapped in `<MagneticButton>` and carries `cta-sheen`. |
| `ProcessSteps.tsx` | Numbered (01-NN) timeline component. Props: `steps: { title, description }[]`. 2-col grid on desktop. **Server component (Session H Part 3 — 2026-04-19)** — uses `<StaggerContainer as="ol" variants={staggerContainerSlow}>` + `<StaggerItem as="li">` to preserve `<ol>`/`<li>` semantics. |
| `FAQAccordion.tsx` | Expandable FAQ with `ChevronDown` indicator. Only one item open at a time (first open by default). **Uses `grid-template-rows: 0fr ↔ 1fr` CSS transition** (not Motion `height: 'auto'`) for the expand/collapse panel — pure compositor-driven, zero layout thrash (Session D — 2026-04-17). Content stays always-mounted inside the outer `grid transition-[grid-template-rows,opacity]` wrapper + inner `overflow-hidden` wrapper; `aria-hidden` flips with state for screen-reader correctness. **Each trigger gets `id={triggerId}` + `aria-controls={panelId}`; each panel gets matching `id={panelId}` + `role="region"` + `aria-labelledby={triggerId}` (Session G — 2026-04-19).** ChevronDown + its wrapping `motion.span` both `aria-hidden="true"`. Chevron still rotates via `motion.span`. Emits inline `<script type="application/ld+json">` with `FAQPage` schema — no separate schema component needed. IDs built from `useId()` so multiple accordions on the same page don't collide. |
| `ConsultingServicesGrid.tsx` | Consulting-only 2×2 grid of the 4 consulting services. Monochrome palette. Icons: `Briefcase`, `Settings`, `Monitor`, `Brain`, plus `ArrowRight` on each card. **Server component (Session H Part 3 — 2026-04-19)** — prop-driven, no translations needed; uses `<StaggerContainer variants={staggerContainerSlow}>` + `<StaggerItem>` + `<BorderGlow>`. |
| `LeaderIntro.tsx` | Founder/leader introduction with initials avatar (derived from name). Props: `name`, `role`, `bio`. **Server component (Session H Part 3 — 2026-04-19)** — async with `await getTranslations('sections.leader')`, wraps content in `<AnimateIn amount={0.2}>` instead of inline `motion.div`. |
| `ConsultingServicePage.tsx` | Template for consulting service subpages. Props: `title`, `subtitle`, `content` (ReactNode), `processSteps`, `faqItems`, `relatedServices`. Composes: hero section → prose body (`.prose-consulting`) → process steps → FAQ → related services → CTABanner. Client component |
| `MarketingServicesGrid.tsx` | Marketing-only 2×2 grid of the 4 marketing services. Lavender/purple palette with purple glow hover (box-shadow rgba(180,144,240,0.12)) and radial gradient overlay. Icons: `Globe`, `Share2`, `Server`, `Cpu`. **Server component (Session H Part 3 — 2026-04-19)** — prop-driven; uses `<StaggerContainer variants={staggerContainerFast}>` + `<StaggerItem>` + `<BorderGlow>`. **L6 (2026-04-24): hover text color fix.** Icon `group-hover:text-white` → `group-hover:text-[var(--division-text-primary)]`; h3 `group-hover:text-white transition-colors` dropped — BorderGlow provides hover affordance. |
| `TeamShowcase.tsx` | Marketing-only 3-card team showcase for Lazar/Petar/Andrej. Initials avatars (20×20 rounded border-2 `#B490F030`) — photos TBD. No props. **Server component (Session H Part 3 — 2026-04-19)** — composes `<StaggerContainer>` + `<StaggerItem>` + `<BorderGlow>`. **Grid ladder is `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` (Session I — 2026-04-19)** — 1-col through the phone range, 2-col across the iPad range (2+1 layout with Andrej on row 2), 3-col from 1024px. The old `sm:grid-cols-3` jumped straight to 3-col at 640px and crammed the bios into ~195px columns on iPad mini. **Lazar Portfolio (2026-06-24): `TeamMember` gained an optional `href?` field** — when set, the card is wrapped in a locale-aware `<Link className="group block h-full rounded-[12px] focus-ring">` (Lazar → `/lazar`); members without `href` render unchanged. **(2026-06-24) also gained an optional `image?` field** — when set, a `next/image` grayscale portrait fills the avatar circle instead of initials (Lazar → `/lazar-bw.png`). |
| `MarketingServicePage.tsx` | Template for marketing service subpages. Same shape as `ConsultingServicePage` but "Vertex Marketing" overline in `#B490F0`, purple hover states on related pills, `.prose-marketing` body class (lavender bold + links). Client component |
| `ContactForm.tsx` | Client component. 5 visible fields (name, email, phone optional, division select, message) plus a hidden off-screen `website` honeypot with client-side validation (non-empty, email regex, 10-char min message). **Field-level error handling (Session G — 2026-04-19):** `fieldErrors: Partial<Record<'name'\|'email'\|'message', string>>` state drives inline `<p id role="alert">` errors below each required field; each input carries `aria-invalid` + `aria-describedby` pointing to its error id; errors clear on edit; on submit with errors, focus moves to the first invalid field. Submission-level banner (API / network failures) also `role="alert"`; submit button carries `aria-busy={status === 'submitting'}`. All ids built from `useId()`. **Formspree migration (2026-07-13):** POSTs a JSON payload directly to Formspree (`NEXT_PUBLIC_VERTEX_FORMSPREE_ENDPOINT`) with `Accept: application/json` — honeypot maps to Formspree's `_gotcha`, plus `_subject` and a human-readable `Interested in` label; `email` is the reply-to. On 2xx flips to confirmation card; parses Formspree's `{errors:[{message}]}`/`{error}` into the red banner; missing-endpoint env var is guarded (console.error + generic banner). Previously POSTed to the now-removed `/api/contact` (Resend). Uses lucide `Loader2` + `Check` (both `aria-hidden="true"`). Button color adapts via `--division-accent` / `--division-bg`. Form has `noValidate` since validation is handled in JS. Session B wired the real API call — 2026-04-17. |
| `TeamGrid.tsx` | 4-member grid showing Goran (consulting, gray indicator) + Lazar/Petar/Andrej (marketing, `#B490F0` indicator). Initials avatars, per-member division color drives indicator dot, avatar ring, role label, and hover glow. **Server component (Session H Part 3 — 2026-04-19)** — async with `await getTranslations('sections.team')`; composes `<StaggerContainer>` + `<StaggerItem>` + `<BorderGlow>`. **Lazar Portfolio (2026-06-24): `TeamGridMember` gained an optional `href?` field** — when set, the card body is wrapped in a locale-aware `<Link className="group block h-full rounded-[12px] focus-ring">` (Lazar → `/lazar`); members without `href` render unchanged. **(2026-06-24) also gained an optional `image?` field** — when set, a `next/image` grayscale portrait fills the avatar circle instead of initials (Lazar → `/lazar-bw.png`). |
| `CompanyTimeline.tsx` | Vertical dotted timeline with 4 milestones (2018, 2020, 2023, 2026). Dots use `--division-accent`. **Server component (Session H Part 3 — 2026-04-19)** — async with `await getTranslations('sections.timeline.milestones')`; composes `<StaggerContainer amount={0.2}>` + `<StaggerItem>`. |
| `ValuesGrid.tsx` | 2×2 grid of 4 values with lucide `Handshake` / `Target` / `Zap` / `Shield` in accent-muted rounded-lg icon containers. **Server component (Session H Part 3 — 2026-04-19)** — async with `await getTranslations('sections.values')`; composes `<StaggerContainer amount={0.1}>` + `<StaggerItem>`. |
| `BlogCard.tsx` | Client component. Takes `post: BlogPost`. `motion.article` with `whileHover={{ y: -4 }}`. Division indicator dot (gray/purple/blue), locale-formatted date, read-time with `Clock`, title, 3-line-clamp excerpt, author name (via `post.author.name` — nested author shape as of Phase 13A), `ArrowRight` on hover. Marketing posts get purple hover glow. **L6 (2026-04-24): hover text color fix.** h3 `group-hover:text-white transition-colors` dropped — BorderGlow's pointer-follow edge glow provides hover affordance; keeping a duplicate title-color change was the source of the light-mode white-on-white bug. **Phase 13A (2026-04-24):** renders `post.featuredImage` as a 16:9 cover image above the content block when present (`next/image` with `lqip` blur placeholder + locale-aware alt via `useLocale()`); falls back to the existing division-dot-only content layout when `featuredImage` is `null` (which is the case for all 3 migrated posts until Phase 13C adds Pexels image pulls). |

## src/components/chat/ (Phase 12 — 2026-04-20)
| File | Description |
|------|-------------|
| `index.ts` | Barrel — exports `ChatWidget` |
| `BotIcon.tsx` | Inline SVG icon set — `BotIcon` / `CloseIcon` / `SendIcon`. All accept `SVGProps<SVGSVGElement>`. Inlined because `lucide-react@1.8.0` ships no useful icons (same reason as Footer's brand-icon SVGs) |
| `TypingIndicator.tsx` | 3-dot staggered pulse shown while waiting for Claude's first streamed token. `role="status"` + translated `aria-label` from `chat.status.generating`. Dots use `.typing-dot` from `globals.css`; reduced-motion-safe. **Session — Cursor & Micro-Interactions (2026-06-24):** dots now animate `@keyframes typing-dot-wave` (7px, soft left→right wave) and the inline `animationDelay` styles were removed so the CSS `:nth-child` stagger owns the ripple. |
| `ChatMessage.tsx` | Single-bubble renderer. Props: `message: ChatMessage`, `isStreaming?`. User = bright pill right-aligned with `rounded-br-md`; assistant = elevated-surface bubble left-aligned with `rounded-bl-md` and optional 2px pulsing streaming cursor at the tail. Spring fade-in entrance |
| `ChatPanel.tsx` | The open chat dialog. Props: `messages`, `onSend`, `onClose`, `isStreaming`, `error`, `userMessageCount`. Header (bot avatar + title + subtitle + close × button) → scrollable message list (`aria-live="polite"`) with typing indicator while waiting for first token → auto-resizing textarea (1–4 rows, 96px cap) + send button + footer disclaimer. Client-side 20-message cap enforced via `userMessageCount`. Positioning uses explicit per-side classes (`top-0 right-0 bottom-0 left-0` on mobile; `sm:top-auto sm:left-auto sm:bottom-24 sm:right-6` desktop) to survive `twMerge` — `sm:inset-auto` alongside `sm:bottom-24 sm:right-6` collapses as a conflict. **Session — Cursor & Micro-Interactions (2026-06-24):** the typing-indicator bubble is now wrapped in `AnimatePresence` for a 220ms fade in/out; `launchKey` state + `planeVariants` (out 380ms `cubic-bezier(0.36,0,0.66,-0.2)`, in 266ms `cubic-bezier(0.22,1,0.36,1)`) wrap the `SendIcon` in `AnimatePresence mode="wait"` for the paper-plane fly-out (`launchKey` bumps in `handleSubmit`). Send button intentionally has no `overflow:hidden` so the plane clears the circle. |
| `ChatWidget.tsx` | State-owning root. Owns `messages` / `isStreaming` / `error` / `open`. Renders the 56×56 `chat-trigger` fixed bottom-right `motion.button` (fades to opacity 0 / scale 0.8 when panel is open) + `<AnimatePresence>`-wrapped `<ChatPanel>`. Seeds conversation with the context-appropriate greeting the first time `open` flips to true (5 variants matched on pathname stripped of locale prefix). Mounts Escape-to-close and mobile body-scroll-lock effects. POSTs history to `/api/chat`, reads the response body with `getReader()` + `TextDecoder`, appends chunks to the last assistant message. Abort controller cancels in-flight stream on unmount. Kill switch via `process.env.NEXT_PUBLIC_CHAT_ENABLED === 'false'` returns null. **Session — Cursor & Micro-Interactions (2026-06-24):** the `chat-trigger` button's idle pulse is now a scaling `.chat-trigger::before` halo ring (see `globals.css`) instead of a box-shadow on the button — the `motion.button` still fades to opacity 0 on open, which carries the ring out with it (no JS change here; the alignment lives entirely in CSS). |

## src/components/ui/
| File | Description |
|------|-------------|
| `button.tsx` | shadcn Button wrapping `@base-ui/react`'s Button — `cva` variants (default/outline/secondary/ghost/destructive/link) and sizes (default/xs/sm/lg/cta/pill/icon/icon-xs/icon-sm/icon-lg). `cta` + `pill` added Session C; the underlying primitive does NOT support `asChild`, so for link-as-button use `cn(buttonVariants({ size: '...' }), '...')` on the Link directly. Adopted sitewide as of Session C — used in CTABanner, ContactForm submit, Footer (newsletter submit + back-to-top text), Navbar (hamburger + desktop language toggle + mobile-overlay language toggle), BackToTop FAB, FAQAccordion triggers, BlogListingClient filter pills. |
| `BorderGlow.tsx` / `BorderGlow.css` | Pointer-follow glow card wrapper — React component + co-located stylesheet. Tracks pointer position and sets `--edge-proximity` + `--cursor-angle` CSS vars to drive the `::before` colored-mesh border, `::after` colored-mesh fill, and `.edge-light` outer radial glow. Used by every card-grid component on the site (ServicesOverview, ConsultingServicesGrid, MarketingServicesGrid, TeamGrid, TeamShowcase, BlogCard). **Session E removed the static `border: 1px solid rgb(255 255 255 / 15%)` from `.border-glow-card`** — idle state is now borderless, pointer-follow glow owns the edge. Hover behavior and glow layers unchanged. **As of Session H (2026-04-19): `handlePointerMove` (lines 200–216) rewritten as rAF-batched.** Two refs (`pendingPointer` for the latest `{ clientX, clientY }`, `rafIdRef` for the scheduled frame id or `null`) let the event handler just write the pending coords and, if no rAF is in flight, schedule one `requestAnimationFrame(flushPointer)`. `flushPointer` clears `rafIdRef.current`, reads the pending coords + a fresh `getBoundingClientRect()`, computes edge + angle, writes the two CSS variables. A cleanup useEffect cancels any pending rAF on unmount. Result: at 1000 Hz gaming-mouse input the handler now drives ~60 CSS-var writes/second instead of ~1000 — 16× fewer layout-affecting writes. The pre-existing `animated` sweep useEffect (one-shot mount animation driven by `animateValue`) is untouched — it writes CSS vars directly, bypassing `handlePointerMove`. **As of Session J (2026-04-19): default `colors` prop flipped from the AI-palette rainbow (`['#c084fc', '#f472b6', '#38bdf8']` — purple / pink / cyan) to grayscale (`['#F5F5F5', '#C9C9C9', '#A3A3A3']`) matching the Session E unified palette.** The parallel HSLA rainbow fallbacks in `BorderGlow.css` — 7 gradient positions × 2 stack layers (`::before` border-box and `::after` padding-box), plus the `--gradient-base` linear-gradient fallback in each — were rewritten to the same grayscale values following the `COLOR_MAP = [0, 1, 2, 0, 1, 2, 1]` index mapping. Purely defensive: all 5 current callers override with the same grayscale, so runtime rendering is byte-identical; the fix prevents any future `<BorderGlow>` omitting the `colors` prop from instantiating the exact aesthetic the brand brief rejects. **As of Phase L4 (2026-04-23): component is a `useTheme()` consumer now.** `glowColor` prop is parsed in JS (`parseHSL` regex) so a CSS variable can't be its value — the component instead reads theme state and flips its HSL string default itself: `'0 0 85'` (pale grayscale) in dark, `'232 30 7'` (near-black navy) in light. `colors` mesh palette flips the same way: `['#F5F5F5','#C9C9C9','#A3A3A3']` in dark ↔ `['#0A0B12','#4B5563','#9AA0AD']` in light. `backgroundColor` default became `var(--borderglow-bg)` which chains through `var(--division-surface)` (already theme-aware via L1). All 6 call sites (ConsultingServicesGrid, MarketingServicesGrid, ServicesOverview, TeamGrid, TeamShowcase, BlogCard) had been passing 3 redundant explicit props each — 18 prop lines deleted. Callers still pass explicit overrides for custom looks. `BorderGlow.css` fallback values left unchanged (defensive-only; the component always writes the custom properties at runtime). **As of Phase L6 (2026-04-24): light-mode intensity multiplier (Remedy A for perceptual asymmetry).** Before calling `buildGlowVars`, the component computes `effectiveIntensity = glowIntensity * (theme === 'light' ? 1.4 : 1.0)` and passes the multiplied value. Rationale: a dark-ink glow on a light card reads perceptually weaker than a bright-white glow on a dark card even at the same alpha (bright halos radiate, dark halos recede). 40% intensity bump in light mode makes the hover affordance visibly balanced across themes. Dark mode byte-identical (multiplier is `1.0`). Opacity array post-multiplier in light: `[100, 60, 50, 40, 30, 20, 10] × 0.8 × 1.4 = [112 clamped to 100, 67.2, 56, 44.8, 33.6, 22.4, 11.2]%`. |
| `MagneticButton.tsx` | **Session — Cursor & Micro-Interactions (2026-06-24).** Client component. Window-pointer magnetic wrapper for prime CTAs: on `pointermove`, if the cursor is within `max(w,h)/2 + 80px` of the wrapped element it eases toward the cursor by `PULL = 0.30` × the offset, clamped to `MAX_SHIFT = 12px`, via `useSpring(x/y, { stiffness: 170, damping: 26, mass: 1 })`; springs back to 0 on leave. `whileTap={{ scale: 0.94 }}` (SQUISH) gives the press squish on a snappier 400/22 spring. The constants mirror the `--magnetic-*` tokens in `globals.css`. Wraps its child as `<motion.span className={cn('inline-flex', className)}>` so the rect hugs the button **and** a caller can pass `hidden md:inline-flex` to control wrapper visibility (the navbar CTA needs this — a forced inline-flex would leave a phantom flex gap on mobile). Under `useReducedMotion()` the listener never attaches and the squish is dropped (static element). Applied to: HeroSection's two hero buttons, Navbar's desktop "Get in Touch", Footer's Subscribe, CTABanner's CTA. NOT on nav dropdown links, footer column links, form fields, or card grids. |
| `Confetti.tsx` | **Session — Cursor & Micro-Interactions (2026-06-24).** Client component. Dependency-free, theme-aware grayscale particle burst. `count = 18` particles fired up from the centered anchor in a 74° cone with light gravity then fade; `useMemo` randomizes each particle's angle/velocity/gravity/rotation/size/round-or-square/duration. Easing is the pop-settle bezier `[0.2, 0.7, 0.3, 1]`, 900ms × 0.7–1.2 per particle, `times: [0, 0.55, 1]`. `useTheme()` selects a 3-gray palette (`['#F5F5F5','#C9C9C9','#A3A3A3']` dark ↔ `['#0A0B12','#4B5563','#9AA0AD']` light — no hue, ever). Renders a `pointer-events-none absolute inset-0` overlay; mount it inside a `position:relative` container on a success state. Returns `null` under `useReducedMotion()`. Wired into `Footer.tsx`'s newsletter `<form>` (bursts on `newsletterStatus === 'success'`). |

## src/config/
| File | Description |
|------|-------------|
| `site.ts` | `siteConfig` — name, legal name, domain, url, owner, founded, address, contact, hours, `social` (linkedin/instagram/facebook — added Session A 2026-04-17), two `divisions` objects with manager + team + description |
| `navigation.ts` | `NavItem` type, `mainNavItems` (with dropdown children for Consulting + Marketing), `footerNavItems` (grouped consulting/marketing/company) |
| `projects.ts` | **Session — 2026-06-02.** Client-projects data for the homepage "Our Work" section. Exports `Project` interface + `projects: Project[]`. Per-project `name` / `division` / `image` / `href`; `image` + `href` default to `null` (placeholder). Goran edits this one file to publish screenshots + links (drop image into `public/projects/`, set `image: '/projects/...'` + `href: 'https://...'`). Consumed by `ProjectsShowcase.tsx`. Self-documenting header comment explains the publish workflow. Local `/public` images need no `next.config.ts` change; external image hosts would (only `cdn.sanity.io` is whitelisted today). **Update (2026-06-20):** all three entries now carry real `image`/`href` (no `null` left) — Northgate Dental, Sunset Services, and "Dalibor Plečić — Author" (renamed from "Optimind"), pointing at `public/projects/{northgate,sunset,daliborac}.png` + their live URLs. |
| `lazar.ts` | **Session — Lazar Portfolio (2026-06-24).** Config for the `/lazar` page. Exports `lazarSocials` (github/linkedin/instagram — Lazar's real profile URLs as of 2026-06-24) and `lazarProjects: LazarProject[]` (a curated 3-project list reusing the same `/projects/*.png` screenshots + live URLs as `projects.ts`). Per-project label/description are translated under `lazar.work.projects.{index}`. Consumed by `LazarWork.tsx` + `LazarContact.tsx`. |

## src/lib/
| File | Description |
|------|-------------|
| `utils.ts` | `cn()` — clsx + tailwind-merge |
| `renderInlineMarkdown.tsx` | Tiny React helper — splits strings on `**…**` markers and renders `<strong>` elements. Used by `MarketingServicePage` for paragraphs that carry inline emphasis. Safe (no HTML interpretation). |
| `resend.ts` | Constructs a single `Resend` client from `RESEND_API_KEY` + exports a `resendConfig` object (`from`, `contactTo`, `audienceId`). Imported by both API routes. Session B — 2026-04-17. |
| `animations.ts` | All Motion `Variants` + `Transition` constants (springSnap, fadeInUp, staggerContainer, heroHeadline, hoverLift, …) |
| `divisions.ts` | `Division` type, `getDivisionFromPath()`, `divisionConfig` mapping |
| `metadata.ts` | `generatePageMetadata`, `consultingMetadata`, `marketingMetadata` — canonical URL, OG, Twitter, robots. **Session K (2026-04-19): added explicit `images` field to the `openGraph` and `twitter` blocks** referencing `/opengraph-image` and `/twitter-image` (relative URLs, resolved against `metadataBase`). When a page exports its own `openGraph` (as this helper does for per-page title/url/locale), Next.js treats the child block as a full replacement and drops the framework-auto-injected file-convention image — so without this explicit `images` field, every page using this helper (every consulting / marketing / about / contact / blog page) had no OG image. |
| `blog.ts` | **Sanity-backed async adapter (Phase 13A — 2026-04-24).** Same five public function names as the Phase 15F mock (`getAllSlugs`, `getAllPosts(locale)`, `getPostBySlug(slug, locale)`, `getPostsByDivision(division, locale)`, `getRelatedPosts(slug, locale, limit=2)`) but every helper is now `async` and queries Sanity via `sanityClient.fetch` with `{ next: { revalidate: 60, tags: ['blog', ...] } }` for ISR + tag-based on-demand invalidation. A `collapse(raw, locale)` helper flattens the `{ en, mk }`-shaped Sanity documents into the locale-flat `BlogPost` shape the UI expects. `BlogPost` gained `_id` + `featuredImage`; `content: string` became `body: PortableTextBlock[]`; `author: string` + `authorRole: string` flattened into `author: { name, initials, role, division, image }`. |
| `ai.ts` | **Phase 12 (2026-04-20).** Provider abstraction. Exports `AiProvider` / `ChatRole` / `ChatMessage` types + `streamAIResponse(messages, systemPrompt)` — an `AsyncGenerator<string>` that dispatches to Claude (via dynamic `@anthropic-ai/sdk` import) or Ollama (line-delimited JSON from `/api/chat`). Module-level constants: `CLAUDE_MODEL='claude-sonnet-4-6'`, `CLAUDE_MAX_TOKENS=400`, `OLLAMA_BASE_URL`, `OLLAMA_MODEL`. Provider selection via `AI_PROVIDER` env. Dynamic SDK import keeps the Anthropic bundle out of any future edge-migrated routes. |
| `chatWidget.ts` | **Phase 12 (2026-04-20).** Context-aware system-prompt builder. `buildSystemPrompt({pageUrl, locale})` composes a static base (from `siteConfig`: name, legal name, owner, contact, hours, pricing rule) + a `CURRENT PAGE CONTEXT` persona block (5 variants: contact, blog, consulting, marketing, shared) + a `LANGUAGE` block (EN or MK). Path detection reuses `getDivisionFromPath()` after stripping the `^/(en\|mk)` prefix. Exports `ChatLocale` type. |
| `sanity/client.ts` | **Phase 13A (2026-04-24).** Two clients. `sanityClient` is CDN-cached read-only (`useCdn: true`, `perspective: 'published'`) — every page uses this for published content. `sanityWriteClient` is the editor-role client (`useCdn: false`, `token: SANITY_API_WRITE_TOKEN`) reserved for server-side automation (seed script, Phase 13B generator). Exports `urlFor(source)` wrapping `createImageUrlBuilder(sanityClient)` for CDN image URLs. |
| `sanity/queries.ts` | **Phase 13A (2026-04-24).** 5 GROQ queries sharing one `blogPostFields` projection: `allPublishedPostsQuery`, `postBySlugQuery`, `postsByDivisionQuery`, `relatedPostsQuery`, `allSlugsQuery`. Projection dereferences the author ref, pulls featured-image metadata (lqip + dimensions) for `next/image` blur placeholders, and returns `{ en, mk }` shapes for every translated field so `collapse()` in `blog.ts` can flatten uniformly. |
| `pexels.ts` | **Phase 13B (2026-04-26).** Two helpers for the content generator. `searchPexelsPhoto(query)` calls the Pexels v1 `/search` endpoint (`orientation=landscape`, `size=large`, `per_page=3`) with the `VERTEX_PEXELS_API_KEY` env var, returns the top result or `null`. `downloadPexelsPhoto(photo)` fetches the `large2x` variant (~1880px wide) and returns a Node Buffer ready to pipe into `sanityWriteClient.assets.upload('image', buffer)`. |
| `meta.ts` | **Phase 13C (2026-05-01).** Meta Graph API helper for Facebook Page + Instagram Business posting. Three exports: `postToFacebook({message, link})` POSTs `URLSearchParams` to `/{page-id}/feed` with the long-lived Page Access Token and returns `{postId, postUrl}` (Meta auto-builds the link preview from the target page's OG tags). `postToInstagram({imageUrl, caption})` runs the three-step flow — `/media` create → poll `/{creation-id}?fields=status_code` until `FINISHED` (max ~20 s) → `/media_publish` — and best-effort fetches the permalink, returning `{mediaId, permalink}`. `getTokenHealth()` reads `VERTEX_META_TOKEN_ISSUED_AT` (ISO date stamp) and returns `{status: 'ok'\|'warning'\|'expired', issuedAt, daysUntilExpiry, message}` based on a 60-day calendar heuristic — Meta doesn't expose token introspection cleanly. All three load env vars lazily via a `loadMetaEnv()` helper that throws a single readable error if any of `VERTEX_META_PAGE_ID` / `VERTEX_META_IG_USER_ID` / `VERTEX_META_PAGE_ACCESS_TOKEN` are missing. |
| `telegram.ts` | **Phase 13C (2026-05-01).** Telegram Bot API helper. `sendTelegramMessage(text)` POSTs `{chat_id, text, parse_mode: 'HTML', disable_web_page_preview: false}` to `/sendMessage` and returns `{ok, error?}` — never throws (the orchestrator wraps every call in `.catch(() => {})` so a Telegram outage never breaks publishing). `escapeHtml(s)` escapes user-provided strings for safe embedding inside the HTML parse-mode messages. Reads `VERTEX_TELEGRAM_BOT_TOKEN` + `VERTEX_TELEGRAM_CHAT_ID`. |
| `contentGenerator/index.ts` | **Phase 13B (2026-04-26), extended Phase 13C (2026-05-01).** Orchestrator. `generateNextPost({ publish, postToFacebook, postToInstagram, sendTelegramNotification, log })` picks the next `pending` topic (priority asc, then `_createdAt` asc), resolves the author (assigned > division-default of `author-goran`/`author-lazar`), runs `generateDraft` → `validateDraft` → `createPost` in sequence, then POSTs to `/api/revalidate` if `publish: true`. **Phase 13C** appended the social-posting + Telegram tail: when `publish` AND `postToFacebook`, calls `metaPostToFacebook` with the Macedonian `facebookCaption` + `${siteConfig.url}/mk/blog/${slug}`; when `publish` AND `postToInstagram` AND a featured image exists, resolves the Sanity asset URL via GROQ then calls `metaPostToInstagram` with the image URL + `instagramCaption`. Both social steps catch their errors so a Meta failure never blocks the rest. Telegram (`sendTelegramMessage`) fires for every terminal state — ✅ published, 📝 draft created, ❌ Claude/validation/Sanity failure. Every step streams progress via the `LogFn` callback so the API route can SSE it to the dashboard. Every failure path calls `markTopicFailed(id, reason)` so a hypothetical future cron never retries broken topics in a loop. |
| `contentGenerator/toolSchema.ts` | **Phase 13B (2026-04-26), extended Phase 13C (2026-05-01).** JSON schema for Claude's `tool_use` structured output (`submit_blog_post`). 13B forced 8 fields; 13C added 2 more — `facebookCaption` (Macedonian, 80-300 chars target, no hashtags/emojis, 1-3 sentences) and `instagramCaption` (Macedonian, 2-4 paragraphs + literal "Повеќе на vertexconsulting.mk — линкот е во био." + 5-10 hashtags including ≥1 Macedonian one, max 1 emoji, hard 2200-char cap). Both required on every tool call. Exports the `GeneratedPost` TypeScript type for downstream consumers — 13C extended this with `facebookCaption: string` + `instagramCaption: string`. |
| `contentGenerator/buildPrompt.ts` | **Phase 13B (2026-04-26), extended Phase 13C (2026-05-01).** Brand-voice system prompt builder. Three division-specific voices (consulting / marketing / shared), banned phrases (`unleash`, `leverage`, `synergy`, `revolutionize`, `In today's fast-paced…`), 3 reference passages copied from the Phase 13A migrated posts, explicit Portable Text JSON shape spec with a worked example block (paragraph with a bold span and an internal link), Macedonian rules (Cyrillic, formal Вие register, keep Latin brand names). 500–800 words per language hard cap (tightened from 600–1100 after a `max_tokens` truncation incident). **Phase 13C** appended a "Social media captions" section with separate Facebook + Instagram rules (tone example, hashtag policy, emoji cap, the literal Cyrillic CTA line), inserted between the Bilingual rules block and the Output format block. |
| `contentGenerator/generateDraft.ts` | **Phase 13B (2026-04-26).** Claude Opus 4.7 invocation. Reads `ANTHROPIC_API_KEY` explicitly (no SDK fallback to the OAuth `ANTHROPIC_AUTH_TOKEN` — the OAuth token 401s on direct Messages API calls). Sends the long-output beta header `anthropic-beta: output-128k-2025-02-19` so `max_tokens: 16000` is accepted (Opus 4.x default cap is 8192; bilingual Cyrillic + PT JSON ~3x the raw word count + reasoning tokens push past 8k easily). 3-min timeout. Defensive shape checks emit `stop_reason`, `usage`, and top-level keys into the failure reason so truncation vs. schema drift is distinguishable from the dashboard. |
| `contentGenerator/validateDraft.ts` | **Phase 13B (2026-04-26).** Quality gates that run after Claude returns. Checks: title length, slug format + length, EN/MK excerpt length, EN/MK body word counts (500–1500), H2 count (≥2 each), tag counts (3–5 each), readTime range, pexelsQuery length, imageAlt length, banned-phrase substring scan on the EN body. Returns `{ ok: boolean, errors: string[] }`. Any failure marks the topic `failed` so it doesn't get retried. |
| `contentGenerator/createPost.ts` | **Phase 13B (2026-04-26).** Sanity write path. Uploads the Pexels image as a Sanity asset (non-blocking — proceeds without an image if Pexels fails). Slug-collision check with `YYYYMMDD` suffix fallback. Creates the `blogPost` document (status `draft` or `published` based on the `publish` flag) with the new asset attached as `featuredImage` (with bilingual `alt`). Patches the topic with `{ status: 'used', usedAt, resultingPost: <ref> }`. Also exports `markTopicFailed(id, reason)` for the orchestrator's failure paths. |

## src/hooks/ (Session D — 2026-04-17)
| File | Description |
|------|-------------|
| `README.md` | Placeholder README establishing the folder. Makes the `@/hooks` alias in `components.json` (and `tsconfig.json`'s `@/*` → `./src/*`) resolve cleanly for future hook extractions. Conventions: one hook per file, named `use<Thing>.ts`, default export the hook. Currently empty — no hooks extracted yet. |

## src/types/
| File | Description |
|------|-------------|
| `index.ts` | Stub `NavItem` type (duplicates `config/navigation.ts`'s version — the config one is canonical) |
| `ogl.d.ts` | Hand-rolled ambient module for `ogl` — declares `Renderer`, `Program`, `Mesh`, `Triangle` |

## src/_project-state/
| File | Description |
|------|-------------|
| `README.md` | Usage instructions + phase checklist |
| `00_stack-and-config.md` | Full tech stack, deps, config files, fonts, color system |
| `01_phase-01-setup.md` | Phase 1 write-up |
| `02_phase-02-design.md` | Phase 2 write-up |
| `03_phase-03-backgrounds.md` | Phase 3 write-up |
| `04_phase-04-animations.md` | Phase 4 write-up |
| `05_phase-05-layout.md` | Phase 5 write-up |
| `06_phase-06-navbar.md` | Phase 6 write-up |
| `07_phase-07-footer.md` | Phase 7 write-up |
| `08_phase-08-homepage.md` | Phase 8 write-up |
| `09_phase-09-consulting.md` | Phase 9 write-up |
| `10_phase-10-marketing.md` | Phase 10 write-up |
| `11_phase-11-shared-pages.md` | Phase 11 write-up |
| `12_phase-15a-i18n-infrastructure.md` | Phase 15A write-up — next-intl wired, locale routing live, folder move, language toggle |
| `13_phase-15b-global-ui-homepage.md` | Phase 15B write-up — message namespaces, Option A nav refactor, component migrations, MK translations |
| `14_phase-15c-consulting-translations.md` | Phase 15C write-up — consulting namespace, ConsultingServicePage structured-content refactor, per-service page.tsx async server components, ~3,500-word MK prose block |
| `15_phase-15d-marketing-translations.md` | Phase 15D write-up — marketing namespace, MarketingServicePage/Grid/TeamShowcase refactors, inline `**bold**` support via `renderInlineMarkdown`, ~3,000-word MK prose block |
| `16_phase-15e-shared-pages.md` | Phase 15E write-up — shared page namespaces, ContactForm + TeamGrid refactors, blog post inline-Markdown renderer rebuild to use locale-aware Link, privacy body stays EN with MK notice, Thank-you CTA added |
| `17_phase-15f-blog-seo-final.md` | Phase 15F write-up — locale-aware blog data layer, 3 MK post translations, inLanguage on JSON-LD, sitemap + robots + not-found, Phase 15 closure |
| `12_phase-12-chat-widget.md` | Phase 12 write-up — AI chat widget (core). `@anthropic-ai/sdk` wired behind `src/lib/ai.ts`, Node-runtime streaming route at `/api/chat`, 5-component `src/components/chat/` tree, `chat.*` translation namespace (EN + MK), grayscale bot trigger + 380×560 panel, context-aware greetings + system prompt per URL/locale. Lead capture + Telegram deferred to Phase 12B |
| `phase-l6-polish-and-fixes.md` | **Phase L6 write-up — 2026-04-24.** Three user-reported light-mode bug fixes. Silk hero damping via new `--silk-opacity` token (1 dark, 0.3 light) on the BackgroundSilk wrapper + `SILK_COLOR_LIGHT` retuned `#CDD1D7` → `#D8DCE2`. DivisionSplit hover text fix (dropped `group-hover:text-white` on h3; CTA `group-hover:text-[var(--division-text-primary)]`; tokenized 4 hardcoded rgba literals: hover border → new `--division-border-hover`, icon bg + chip bg → `var(--division-glow)`, chip color → `var(--division-text-secondary)`). BlogCard hover fix (dropped h3 `group-hover:text-white` entirely — BorderGlow provides hover affordance). Cross-section fix for MarketingServicesGrid (icon + h3) and ServicesOverview (icon + h3). BorderGlow light-mode intensity multiplier via Remedy A (`effectiveIntensity = glowIntensity * (theme === 'light' ? 1.4 : 1.0)`). Dark-mode regression byte-identical (multiplier 1.0 in dark, silk opacity 1 in dark, division-border-hover dark preserves pre-L6 `rgba(245,245,245,0.19)`). Small accepted dark-mode aesthetic drift on DivisionSplit chip text (solid `#A3A3A3` post-L6 vs perceived `~#CB` pre-L6). Plan assumption "L5 was last completed" corrected — L5 was never done; L6 handled the user-visible subset as a targeted polish phase. 7 files modified: `globals.css`, `BackgroundSilk.tsx`, `DivisionSplit.tsx`, `BlogCard.tsx`, `MarketingServicesGrid.tsx`, `ServicesOverview.tsx`, `BorderGlow.tsx`. |
| `phase-l4-global-chrome.md` | **Phase L4 write-up — 2026-04-23.** Global chrome sweep. `.glass` utility rewritten with theme-swappable CSS variables (`--glass-bg` / `--glass-border` + reserved `-strong` variants); two redundant `[data-division="consulting"] .glass` / `[data-division="marketing"] .glass` variants deleted (post-Finishing-Touches v2 they held identical values to the base). `.glass-nav` (distinct navbar backdrop) tokenized with its own `--glass-nav-bg` / `--glass-nav-border` pair — flips from `rgba(14,14,14,0.85)` / `rgba(255,255,255,0.06)` in dark to `rgba(255,255,255,0.85)` / `rgba(10,11,18,0.08)` in light. BorderGlow default prop logic rewritten to be theme-aware via `useTheme()` because `glowColor` is parsed by JS (HSL triplet, `parseHSL` regex) and cannot consume a CSS variable — the component picks `'0 0 85'` (pale grayscale) in dark or `'232 30 7'` (near-black navy) in light; `colors` mesh palette flips `['#F5F5F5','#C9C9C9','#A3A3A3']` (dark) ↔ `['#0A0B12','#4B5563','#9AA0AD']` (light); `backgroundColor` default became `var(--borderglow-bg)` which chains through `var(--division-surface)`. All 6 BorderGlow call sites (ConsultingServicesGrid, MarketingServicesGrid, ServicesOverview, TeamGrid, TeamShowcase, BlogCard) had 3 redundant explicit props each — 18 prop lines deleted. Prose classes verified already fully tokenized post-Session-E (link/strong/h2/h3 all on `--division-*` tokens — no changes). Navbar + Footer + ThemeToggle `hover:bg-white/5` patterns (6 instances total) converted to `hover:bg-[var(--nav-hover-bg)]` — a new token resolving to `rgba(255,255,255,0.05)` in dark and `rgba(10,11,18,0.05)` in light. Footer hairline separator `#262626` → `var(--division-border)`. Dead `[data-division="marketing"] .glow-hover:hover` rule deleted (grep confirmed zero callers, inline comment already said "superseded by BorderGlow"). `@keyframes chat-trigger-pulse` baked `rgba(241,243,247,α)` rewritten to `rgba(var(--borderglow-glow) / α)` so the pulse ring inverts correctly. L1 token tune: light-mode `--division-text-muted` raised from `#9AA0AD` (2.49:1 on `#F8F9FA` — failed WCAG AA) to `#5F6670` (5.50:1 ✓). WCAG AA contrast verified on 4 critical spots (body 19.64:1, footer muted 5.50:1, nav secondary 7.56:1, CTA button 19.64:1). Dark-mode regression byte-identical on every L1 token. Chat widget already fully tokenized via `var(--color-*)` — no component edits. Button primitive already fully tokenized via shadcn HSL — no primitive edits. CTABanner already on `var(--color-ink)` from Session E — in light mode the banner's boundary dissolves against the white page but dark-button + dark-heading carry visual weight. 10 files modified + 1 docs file created. |
| `phase-l2-theme-toggle.md` | **Phase L2 write-up — 2026-04-23.** Sun/moon theme toggle wired into the Navbar (desktop right cluster + new mobile-overlay top-right cluster where the language toggle was relocated). `ThemeToggle` client component at `src/components/global/ThemeToggle.tsx` consumes `useTheme()` from L1's `ThemeProvider`. Adaptations from the plan: (1) dropped the plan's proposed `buttonSize: 'icon-*'` prop because the language toggle doesn't use any CVA icon size — sibling match is className-based via a hand-tuned `BUTTON_SHAPE` constant mirroring the language toggle's 44px pill recipe; (2) rewrote the mount-gate so the outer `<Button>` className stays STABLE across mount (only inner content + a11y attrs swap) to avoid a stuck CSS opacity transition on the Button primitive's `transition-all` base class — verified via `getAnimations()` that the plan's nominal `opacity-0` toggle produced animations pinned at `currentTime: 0, playState: "running"` and left the toggle permanently invisible. SSR HTML fetch confirms the placeholder has no Sun/Moon SVG, so no wrong-icon flash. Translated aria-labels under `nav.themeToggle.{toLight, toDark}` (EN + MK). Full end-to-end verification in the browser preview: theme flip on desktop + mobile, localStorage persistence, overlay-stays-open on theme click / overlay-closes on language click, Cyrillic aria-labels on `/mk`. |
| `phase-l1-theme-foundation.md` | **Phase L1 write-up — 2026-04-23.** Light-mode plumbing. `globals.css` gained three blocks: `color-scheme` pair, `html[data-theme="light"]` grayscale core overrides, combined `html[data-theme="light"], [...consulting], [...marketing]` block overriding `--division-*` + shadcn HSL to the Crisp White palette. `ThemeProvider` client component persists to `localStorage['vertex-theme']` + subscribes to `prefers-color-scheme`. Pre-hydration inline script in `[locale]/layout.tsx` writes `data-theme` on `<html>` before React hydrates. No toggle UI yet (L2), no utility-class or component-level migrations (L3–L5). Documents adaptations from plan's stale assumptions (root doesn't own `<html>`, no `[data-division="shared"]` selector exists, dark values drifted from plan's nominal column). |
| `session-a_social-privacy.md` | Session A write-up — social URLs wired from `siteConfig.social` + full 13-section privacy policy (EN) |
| `session-b_contact-newsletter.md` | Session B write-up — contact form + newsletter backend via Resend, honeypot spam protection, `.env.local` handling |
| `session-c_accessibility.md` | Session C write-up — `.focus-ring` + `.form-input-focus` utilities, shadcn Button primitive sitewide adoption, 44×44 mobile touch targets, Footer logo aria-label unified via `common.logoAriaSuffix` |
| `session-d_performance.md` | Session D write-up — FAQAccordion rebuild from Motion `height: 'auto'` to pure-CSS `grid-template-rows: 0fr ↔ 1fr` transition (zero layout thrash), plus `src/hooks/` folder stub so the `@/hooks` import alias resolves |
| `session-e_polish.md` | Session E write-up — hardcoded hex → `--division-*` / `--color-*` tokens in 7 component files (MarketingServicePage, CTABanner, DivisionSplit, ServicesOverview, TeamGrid, BlogCard, MarketingLandingClient), legacy `.gradient-text` + `.gradient-text-brand` CSS classes deleted with usage sites migrated to HeroSection inline-style color, static 1px border dropped from BorderGlow card wrapper so the pointer-follow glow owns the edge. |
| `session-f_fonts.md` | Session F write-up — Sora → Manrope (headings) + DM Sans → Onest (body) swap with full cyrillic + cyrillic-ext subset loading, CSS variable rename (`--font-sora` → `--font-heading`, `--font-dm-sans` → `--font-body`), font tokens moved into `@theme inline` block to resolve the naming collision. Closes the Phase 15F Cyrillic blocker. |
| `session-g_a11y-hardening.md` | Session G write-up — eight audit findings closed: root layout pass-through + `<html lang={locale}>` moved to `[locale]/layout.tsx` (WCAG 3.1.1); Navbar desktop dropdown rewritten as W3C disclosure pattern (Escape closes + focus return, mouse-hover as PE); ContactForm + Footer newsletter field-level `aria-invalid` + `aria-describedby` + `role="alert"` error associations; FAQAccordion trigger/panel `aria-controls`/`aria-labelledby` pair; mobile menu `inert` on `<main>`+`<footer>` + focus return to hamburger on close; skip-to-content link; decorative-icon `aria-hidden` sweep; `aria-busy` on submit buttons. Two new translation keys — `common.skipToContent`, `nav.submenuToggleAria`. |
| `session-h_performance.md` | Session H write-up — WebGL viewport + tab-visibility gating on Silk / Plasma / GridMotion (IntersectionObserver + `document.visibilitychange`, fully halts the render loop when offscreen or tab-hidden; Plasma adds a local `simSeconds` accumulator so the shader doesn't skip-jump forward after a hidden tab resumes), BorderGlow `handlePointerMove` rewritten as rAF-batched (pending-coords ref + single `requestAnimationFrame(flushPointer)` per frame — 16× fewer CSS-var writes at 1000 Hz gaming-mouse input), `next.config.ts` gained `experimental.optimizePackageImports` for motion / lucide-react / gsap (null bundle delta +0.3 KB noise; retained as future-proofing), `.claude/launch.json` gained `vertex-prod` entry for production-build perf measurement. Measured: marketing-mobile Lighthouse TBT 168,102 ms → 530 ms (-99.7%), perf 0.38 → 0.82 on the sample where Lighthouse caught the gate engaging; other routes inside Lighthouse's ±0.3 variance. |
| `session-i_responsive-adapt.md` | Session I write-up — two breakpoint-progression bugs closed, one touch-target verification. `TeamShowcase.tsx:31` `grid-cols-1 sm:grid-cols-3` → `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (fixes iPad-mini cramp). `SocialProof.tsx:78` `grid-cols-2 lg:grid-cols-4` → `grid-cols-2 md:grid-cols-4` (fixes 768–1023px 2×2 awkwardness). Navbar hamburger measured at 375×667 — `offsetWidth × offsetHeight = 44×44`, floor met, no `min-h-[44px]` bump applied. Verified at 375/640/768/1024/1280 for TeamShowcase, 375/640/768/1024 for SocialProof, 16 `/en/*` routes at 375+1280 with zero horizontal overflow, `npm run build` passes. One-time `rm -rf .next/dev/cache` required mid-session to recover from Turbopack SST corruption. |
| `current-state.md` | **Master snapshot** — read first |
| `file-map.md` | This file |
