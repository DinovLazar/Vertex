# Current Project State

Last updated: 2026-04-24 (Phase L6)
Last completed: **Phase L6 — Light Mode Polish & Bug Fixes** (three user-reported light-mode bugs fixed in a targeted polish pass — the homepage Silk hero was too prominent in light mode, DivisionSplit cards went white-on-white on hover, BlogCard + service-card titles went white on hover. **Silk damping** — the shader math multiplies `uColor` by a `pattern` value oscillating ~0.2–1.0, so no color alone can make the light-mode silk quiet (the pattern dips always land near-dark relative to the input). Fix: wrap the BackgroundSilk canvas container in a theme-aware CSS variable `--silk-opacity` — `1` in dark (full strength, byte-identical to pre-L6), `0.3` in light (shader pattern muted to a subtle flowing texture behind the headline). `SILK_COLOR_LIGHT` retuned from `#CDD1D7` to `#D8DCE2` so the dampened output still reads as visible texture rather than near-invisible pale. The outer wrapper div in `BackgroundSilk.tsx` now carries `style={{ opacity: 'var(--silk-opacity, 1)' }}` — zero dark-mode cost. **DivisionSplit hover fix (Pattern A + C combined)** — the homepage division cards had TWO bugs. (1) Pattern A: `<h3>` title + CTA arrow row had hardcoded `group-hover:text-white transition-colors` Tailwind class — white on white in light-mode hover. (2) Pattern C (side effect of the same L5-pending cleanup): inline-styled rgba literals for hover border (`rgba(245, 245, 245, 0.19)`), icon container bg (`rgba(245, 245, 245, 0.08)`), and chip bg + chip color (`rgba(245, 245, 245, 0.06)` + `rgba(245, 245, 245, 0.8)`) were invisible on light cards regardless of hover. Fix: dropped `group-hover:text-white` on h3 (base is already `--division-text-primary`, theme-aware and readable in both hover and non-hover bg states — belt-and-suspenders removed). Changed CTA arrow row from `group-hover:text-white` to `group-hover:text-[var(--division-text-primary)]` (brightens muted → primary on hover in both themes). Added a new `--division-border-hover` token to `globals.css` (`rgba(245, 245, 245, 0.19)` dark for byte-identical regression, `rgba(10, 11, 18, 0.19)` light for inverted-direction hover affordance). Migrated the hover border, icon bg, and chip bg to `var(--division-border-hover)` / `var(--division-glow)` respectively. Migrated chip text color to `var(--division-text-secondary)` — reads as dimmed secondary text in both themes (dark: `#A3A3A3` — slightly less bright than the pre-L6 `rgba(245,245,245,0.8)` perceived-#CB; light: `#4B5563` — readable gray on the subtle chip bg). **BlogCard hover fix** — `<h3>` class was `text-[var(--division-text-primary)] group-hover:text-white transition-colors` — same Pattern A bug. Dropped the hover color change entirely since BorderGlow's pointer-follow edge glow provides the primary hover affordance; keeping a duplicate title-color change was belt-and-suspenders AND the source of the light-mode bug. Title now stays at `--division-text-primary` — dark ink in light mode (readable on #F1F3F5 card bg), bright white in dark mode (readable on #1C1C1C card bg). **BorderGlow glow-intensity boost (Remedy A)** — the perceptual asymmetry: dark glows on light cards read weaker than bright glows on dark cards, even at the same alpha. Fix: `BorderGlow.tsx` now computes `effectiveIntensity = glowIntensity * (theme === 'light' ? 1.4 : 1.0)` and passes it into `buildGlowVars()`. Dark mode: intensity stays byte-identical (`0.8 × 1.0 = 0.8`). Light mode: intensity bumps 40% (`0.8 × 1.4 = 1.12`), shifting the `buildGlowVars` opacity array from `[100, 60, 50, 40, 30, 20, 10] × 0.8 = [80, 48, 40, 32, 24, 16, 8]%` to `[100 clamped, 72, 60, 48, 36, 24, 12, 12]%` so the dark-ink glow reads more visibly on hover. Uses the already-wired `useTheme()` hook from L4. **Cross-section audit** — grepped `group-hover:text-white` + variants across `src/`. Fixed 4 additional occurrences: `MarketingServicesGrid.tsx` icon (line 48) + h3 (line 50), `ServicesOverview.tsx` icon (line 74, also had a `text-gray-400` baseline migrated to `text-[var(--division-text-secondary)]`) + h3 (line 79). All converted to `group-hover:text-[var(--division-text-primary)]` (icons) or dropped entirely (h3 titles). `ConsultingServicesGrid.tsx` already used `group-hover:text-[var(--division-text-primary)]` — zero change. `TeamGrid.tsx` + `TeamShowcase.tsx` had no hover text color changes — zero change. Post-fix grep for `group-hover:text-white|hover:text-white|group-hover:text-\[var\(--color-bright\)\]` returns zero hits. **Not done in scope (explicitly deferred to Phase L5 proper when that runs):** the `bg-white/5 group-hover:bg-white/10` icon container backgrounds in service grids + TeamGrid/TeamShowcase avatar borders (`border-white/10 bg-white/5`) — these are L5 sweep territory, out of scope for this three-bug polish. **Verified:** (a) `npm run build` clean — 48/48 static pages, zero TypeScript errors. (b) Live DOM read-back in light mode on `/en`: `--silk-opacity: .3`, `--division-border-hover: #0a0b1230` (= `rgba(10,11,18,0.188)`), `--division-glow: #0a0b120f` (= `rgba(10,11,18,0.059)`), `--division-bg: #fff`, `--division-card: #f1f3f5`, `--division-text-primary: #0a0b12`. DivisionSplit non-hover: card bg `rgb(241,243,245)`, border `rgb(229,231,235)`, h3 `rgb(10,11,18)`, chip bg `rgba(10,11,18,0.06)`, chip color `rgb(75,85,99)`. DivisionSplit hovered state (with `transition: none` forced to defeat the 500ms transition-all interfering with eval reads): card bg `rgb(255,255,255)` (= `#FFFFFF`, the target of `var(--division-bg)`), h3 stays `rgb(10,11,18)` — readable. (c) `/en/blog` light mode: 3 blog cards, h3 class `text-h3 text-[var(--division-text-primary)]` (no hover-white), h3 color `rgb(10,11,18)`. BorderGlow glow vars: `--glow-color: hsl(232deg 30% 7% / 100%)`, `--glow-color-10: 11.2%` (= 10% × 1.4 × 0.8 ✓), `--glow-color-60: 67.2%` (= 60% × 1.4 × 0.8 ✓). (d) `/en/marketing` light mode: 4 service cards, h3 class `text-h3 text-[var(--division-text-primary)]` (no hover-white), icon class uses `group-hover:text-[var(--division-text-primary)]`. (e) Dark-mode regression: `--silk-opacity: 1`, `--division-border-hover: #f5f5f530` (= `rgba(245,245,245,0.188)` ≈ pre-L6 `0.19`). DivisionSplit card bg `rgb(38,38,38)` (= `#262626`), border `rgb(64,64,64)` (= `#404040`), both pre-L6 byte-identical. BorderGlow glow vars: `--glow-color: hsl(0deg 0% 85% / 80%)`, `--glow-color-10: 8%`, `--glow-color-60: 48%` — all byte-identical to pre-L6 (intensity multiplier is `1.0` in dark). Small dark-mode aesthetic shift on DivisionSplit chips: pre-L6 chip text `rgba(245,245,245,0.8)` perceived as `~#CBCBCB` on `#262626`; post-L6 chip text `--division-text-secondary` = solid `#A3A3A3` — slightly dimmer. Chip bg also shifts from `rgba(245,245,245,0.06)` to `var(--division-glow)` = `rgba(245,245,245,0.05)` — 1% alpha difference, imperceptible. Accepted as a small aesthetic drift in exchange for theme-agnostic chips that work in light. (f) Rapid theme swap: 5 programmatic flips, all tokens flip correctly per cycle, zero console errors. Files: 6 edits across `src/app/globals.css` (added `--silk-opacity` token pair + `--division-border-hover` token pair), `src/components/backgrounds/BackgroundSilk.tsx` (outer wrapper opacity + `SILK_COLOR_LIGHT` retuned), `src/components/sections/DivisionSplit.tsx` (rgba literals + hover-white h3 + hover-white CTA all tokenized), `src/components/sections/BlogCard.tsx` (h3 hover-white dropped), `src/components/sections/MarketingServicesGrid.tsx` (icon + h3 hover-white tokenized), `src/components/sections/ServicesOverview.tsx` (icon + h3 hover-white tokenized), `src/components/ui/BorderGlow.tsx` (1.4× light-mode intensity multiplier). New docs: `src/_project-state/phase-l6-polish-and-fixes.md`. Updated docs: `current-state.md`, `file-map.md`.) Prior closed work: **Phase L3 — Hero Backgrounds (Light Mode)** (all three animated hero backgrounds now render correctly in both themes. Silk (homepage hero) gets its shader `uColor` uniform swapped at runtime via `BackgroundSilk.tsx` — the wrapper imports `useTheme` from `@/components/global` and resolves a theme-aware default `color` prop (`#2A2D33` in dark = pre-phase live value, `#CDD1D7` in light). Silk's existing `useMemo` already had `color` in its deps array, so prop changes propagate to the `uColor` uniform without a canvas remount — theme swap on `/` is seamless, `document.querySelectorAll('canvas').length` stays `1` across the toggle click. Plasma (marketing hero) got the same wrapper pattern — `BackgroundPlasma.tsx` imports `useTheme`, resolves dark `#F5F5F5` (pre-phase call-site value) / light `#2A2D33`. Unlike Silk, Plasma's single `useEffect` has `color` in its deps and tears down the whole OGL renderer when color changes, so theme flip causes a brief ~150ms canvas re-init — acceptable for the marketing hero since theme flips are rare explicit user actions. The explicit `color="#F5F5F5"` prop on `<BackgroundPlasma>` in MarketingLandingClient.tsx was removed so the wrapper's theme-aware default owns the decision. Finishing-Touches v2 had spec'd Plasma removed from marketing but it was still mounted at L3 start — per user direction, option A (make it theme-aware) rather than option B (remove it). GridMotion (consulting hero) got CSS-only theme-awareness — the `.metallic-panel` CSS class in `globals.css` was rewritten to consume seven new CSS variables (`--hero-panel-stop-1/2/3` for the radial-gradient stops, `--hero-panel-stripe-bright/shadow` for the brushed-metal striation alphas, `--hero-panel-border` for the rim, `--metallic-panel-shadow` for the inset depth shadow). Flipping `data-theme` on `<html>` swaps all seven variables → every panel paints in the new color in one compositor tick, GSAP ticker continues uninterrupted, mouse parallax still works post-flip. The outer `<section>` background changed from hardcoded `#0E0E0E` (panels variant) to `var(--hero-grid-bg)`. Dark values = live pre-L3 state exactly (preserved for regression — stops `#A8ACB4 / #4A4E56 at 50% / #14171C`, stripes `rgba(255,255,255,0.06) + rgba(0,0,0,0.14)`, shadow `inset 0 1px 0 rgba(255,255,255,0.05), inset 0 0 0 1px rgba(0,0,0,0.35)`, grid bg `#0E0E0E`, no border). Light values recalibrated — stops `#3A3D42 (dark titanium) / #8A8E94 / #D4D7DC (light silver edge that blends toward #F1F3F5 page bg)`, stripes `rgba(255,255,255,0.10) + rgba(0,0,0,0.12)`, shadow `inset 0 1px 0 rgba(255,255,255,0.50), inset 0 -1px 0 rgba(0,0,0,0.12)`, grid bg `#F1F3F5`, rim `rgba(10,11,18,0.10)`. Three pre-existing theme-unawareness bugs were also fixed in scope: (1) `BackgroundGrid.tsx` had hardcoded `#121212` on both its wrapper `<div>` and reduced-motion fallback — both changed to `var(--division-bg)` so the fallback inverts cleanly for users with `prefers-reduced-motion: reduce`. (2) `ConsultingLandingClient.tsx`'s hero scrim overlay had `'radial-gradient(ellipse 50% 40% at 50% 50%, rgba(14,14,14,0.75) 0%, rgba(14,14,14,0.4) 40%, transparent 80%)'` hardcoded — tokenized to `var(--hero-scrim-center) / var(--hero-scrim-mid)`. In dark mode the scrim darkens the center ellipse for white-text contrast over bright metallic streaks; in light mode it lightens (`rgba(241,243,245,0.40) / rgba(241,243,245,0.20)`) for black-text contrast over dark-titanium panel centers — same design intent, flipped implementation. (3) Two internal assumptions the plan held against were wrong: GridMotion's cells aren't styled inline, they render `<div className="metallic-panel" />` — so all metallic tokenization happened in the CSS class rather than in GridMotion.tsx's cell JSX (cleaner diff, CSS-only swap); and the plan's "Palette decisions" dark values (`#E8EBF0 / #7A7E86 at 55% / #1A1D22`) didn't match the live `.metallic-panel` state — actual live values used per user instruction to preserve dark regression. **Verified end-to-end:** (a) `npm run build` clean — 48/48 static pages, zero TypeScript errors. (b) Dark-mode regression — every L3 token reads its pre-phase hardcoded equivalent: `--hero-grid-bg: #0e0e0e`, `--hero-panel-stop-1: #a8acb4`, `--hero-panel-stop-2: #4a4e56`, `--hero-panel-stop-3: #14171c`, `--hero-panel-stripe-bright: rgba(255,255,255,0.06)`, `--hero-panel-stripe-shadow: rgba(0,0,0,0.14)`, `--hero-panel-border: transparent`, `--metallic-panel-shadow: inset 0 1px 0 rgba(255,255,255,0.05), inset 0 0 0 1px rgba(0,0,0,0.349)`, `--hero-scrim-center: rgba(14,14,14,0.749)`, `--hero-scrim-mid: rgba(14,14,14,0.4)`. Live panel computed-style — bg `rgb(20,23,28)` (= `#14171C`), section bg `rgb(14,14,14)`. Homepage Silk in dark = visually identical to pre-phase (dark-gray flowing silk texture on #141414 page). Consulting GridMotion in dark = visually identical (bright-silver metallic panels on #0E0E0E, tilted -15deg). Marketing Plasma in dark = visually identical (bright plasma nebula on dark page). (c) Light-mode forward — every L3 token flips correctly: `--hero-grid-bg: #f1f3f5`, `--hero-panel-stop-1: #3a3d42`, `--hero-panel-stop-3: #d4d7dc`, `--metallic-panel-shadow: inset 0 1px 0 rgba(255,255,255,0.5), inset 0 -1px 0 rgba(0,0,0,0.12)`, `--hero-scrim-center: rgba(241,243,245,0.40)`, `--hero-panel-border: rgba(10,11,18,0.1)`. Live panel bg `rgb(212,215,220)` (= `#D4D7DC`), section bg `rgb(241,243,245)` (= `#F1F3F5`). (d) Theme-swap smoothness verified via toggle click on `/en` (Silk — no canvas remount, animation continues) and `/en/consulting` (GridMotion — 28 panels flip in one paint, ticker continues). (e) Route sweep `/en`, `/en/consulting`, `/en/marketing`, `/mk/consulting`, `/mk/marketing` all render theme-correct heroes. (f) Console shows zero new errors — only pre-existing `THREE.THREE.Clock` deprecation warnings (known Phase 3 baseline, unrelated). **Aesthetic note flagged for follow-up:** Silk in light mode with `uColor = #CDD1D7` reads heavier than ideal — the shader multiplies `uColor` by a pattern ~0.2–1.0, so the `pattern=0.2` dark zones paint as ~`#2F3033` which competes with the subtitle text (`--division-text-secondary: #4B5563`). The headline (bold black) cuts through fine; body copy is compressed. Proposed tunings in `phase-l3-hero-backgrounds.md` §"Aesthetic concerns to address" — lighten `SILK_COLOR_LIGHT` to `#E8EBEE`/`#F0F2F5`, or lower `noiseIntensity` in light mode. Not applied unilaterally — awaiting user direction. Light-mode GridMotion and Plasma both read well with no tuning needed. Files: 7 edits across `src/app/globals.css` (10 new tokens under `:root` + `html[data-theme="light"]`, `.metallic-panel` class rewritten), `src/components/backgrounds/BackgroundSilk.tsx` (useTheme consumption + resolved color), `src/components/backgrounds/BackgroundPlasma.tsx` (useTheme consumption + resolved color), `src/components/backgrounds/GridMotion.tsx` (section bg tokenized), `src/components/backgrounds/BackgroundGrid.tsx` (wrapper + fallback bg tokenized), `src/app/[locale]/(site)/consulting/ConsultingLandingClient.tsx` (scrim tokenized), `src/app/[locale]/(site)/marketing/MarketingLandingClient.tsx` (explicit color prop removed). New docs: `src/_project-state/phase-l3-hero-backgrounds.md`. Updated docs: `src/_project-state/current-state.md`, `src/_project-state/file-map.md`.) Prior closed work: **Phase L4 — Global Chrome Sweep (Light Mode)** (every "appears on every page" surface is now theme-aware. `.glass` utility rewritten to consume `--glass-bg` / `--glass-border` CSS variables — flips from `rgba(255,255,255,0.03)` / `rgba(255,255,255,0.08)` in dark to `rgba(10,11,18,0.04)` / `rgba(10,11,18,0.08)` in light; the redundant `[data-division="consulting"] .glass` and `[data-division="marketing"] .glass` variants were deleted (they'd held identical values post-Finishing-Touches v2). `.glass-nav` (navbar scroll backdrop, distinct token pair) got its own `--glass-nav-bg` / `--glass-nav-border` tokens — `rgba(14,14,14,0.85)` / `rgba(255,255,255,0.06)` in dark, `rgba(255,255,255,0.85)` / `rgba(10,11,18,0.08)` in light. BorderGlow got a different treatment than the tokens suggested because its `glowColor` prop is parsed by JS (HSL triplet via `parseHSL`) and can't consume a CSS variable — the component now reads `useTheme()` from `@/components/global` and flips the HSL string itself (`'0 0 85'` in dark for the grayscale sheen, `'232 30 7'` in light for near-black navy glow), and the `colors` mesh palette flips the same way (`['#F5F5F5','#C9C9C9','#A3A3A3']` in dark → `['#0A0B12','#4B5563','#9AA0AD']` in light). `backgroundColor` can still be a CSS variable since it's passed through inline `style`, so the default became `var(--borderglow-bg)` which chains through `var(--division-surface)` (already theme-aware via L1). All 6 BorderGlow call sites — ConsultingServicesGrid, MarketingServicesGrid, ServicesOverview, TeamGrid, TeamShowcase, BlogCard — had 3 redundant explicit props each (`colors`, `glowColor`, `backgroundColor`) which now match the defaults exactly, so those 18 prop lines were deleted. Prose classes (`.prose-consulting`, `.prose-marketing`, `.prose-blog`) were verified already fully tokenized post-Session-E — link / strong / h2 / h3 colors all reference `--division-accent` / `--division-text-primary`, no changes needed. Navbar's four `hover:bg-white/5` instances (dropdown children, desktop language toggle, hamburger, mobile-overlay language toggle) and ThemeToggle's `BUTTON_SHAPE` constant (`hover:bg-white/5`) and Footer's social-icon `hover:bg-white/5` all converted to `hover:bg-[var(--nav-hover-bg)]` — a new token that resolves to `rgba(255,255,255,0.05)` in dark and `rgba(10,11,18,0.05)` in light. Footer hairline separator `style={{ backgroundColor: '#262626' }}` → `'var(--division-border)'` so it inverts cleanly. Footer brand SVGs (Linkedin/Instagram/Facebook) already used `stroke="currentColor"` — no change. Dead `.glow-hover:hover` rule in `globals.css` deleted (flagged in L1 backlog; grep confirmed zero callers — the comment inside the rule itself said "superseded by BorderGlow in Phase 4"). `@keyframes chat-trigger-pulse` baked-light `rgba(241, 243, 247, α)` rewritten to `rgba(var(--borderglow-glow) / α)` so the chat trigger's pulse ring becomes a dark ring on light mode. Chat widget components (ChatWidget, ChatPanel, ChatMessage, TypingIndicator) were audited — every color already uses `var(--color-*)` tokens (`--color-bright`, `--color-ink`, `--color-surface`, `--color-elevated`, `--color-border`, `--color-muted`) which are theme-aware via L1. No changes needed. BackToTop inherits `.glass` → automatically fixed. ScrollProgress uses `var(--division-accent)` → automatically fixed. Button primitive (`src/components/ui/button.tsx`) — all CVA variants (default/outline/secondary/ghost/destructive/link) consume shadcn HSL tokens (`bg-primary`, `text-primary-foreground`, `bg-secondary`, `bg-muted`, `border-input`, `ring-ring`, etc.) which are theme-aware via L1. No component-level changes needed — the Button primitive's sidecar file was audited but left untouched. CTABanner (`src/components/sections/CTABanner.tsx`) background was `'var(--color-ink)'` from Session E — in light mode this resolves to `#FFFFFF` (same as page `--division-bg`), so the banner becomes boundaryless in light mode. The visual weight is carried by the inverted-contrast dark button (`bg: #0A0B12`, `text: #FFFFFF`, computed contrast 19.64:1) and the dark heading. This is correct per the plan — the banner's "deepest surface" semantic remains intact (it's still `--color-ink`, which is just a different hex in light mode). Cross-codebase hex sweep confirmed post-phase: the only remaining hardcoded hex values in component files are (1) `src/components/backgrounds/*` (Silk/Plasma/GridMotion shader props — Phase L3 territory), (2) `BorderGlow.css` gradient-math fallbacks (defensive-only, per user instruction), (3) `BorderGlow.tsx` resolved colors (explicit grayscale arrays, theme-selected in JS), (4) `src/app/opengraph-image.tsx` (static OG PNG — satori runtime, no CSS vars), (5) `src/app/layout.tsx` themeColor metadata (literal hex required by browser spec — future candidate for `themeColor: [{ media: '(prefers-color-scheme: dark)', color: '#0E0E0E' }, { media: '(prefers-color-scheme: light)', color: '#FFFFFF' }]` syntax, flagged for follow-up). One token-level tuning applied: L1's light-mode `--division-text-muted: #9AA0AD` gave only 2.49:1 contrast against `--division-surface` (#F8F9FA) on footer muted links — fails WCAG AA. Bumped to `#5F6670` which gives 5.50:1 and preserves the muted-vs-secondary visual hierarchy (secondary `#4B5563` still reads heavier at 8.59:1). Dark-mode `--division-text-muted` unchanged (`#737373`). **Verified end-to-end:** (a) `npm run build` clean — 48/48 static pages, zero TypeScript errors, clean Turbopack compile; (b) dark-mode regression — every token reads its pre-phase value: `--color-ink: #0e0e0e`, `--division-bg: #141414`, `--division-accent: #f5f5f5`, `--glass-bg: #ffffff08`, `--glass-nav-bg: #0e0e0ed9`, `--nav-hover-bg: #ffffff0d`, `--borderglow-glow: 241 243 247`, `--borderglow-bg: #1c1c1c` (via `var(--division-surface)`); (c) light-mode on `/en/blog` — `--glass-bg: #0a0b120a`, `--glass-nav-bg: #ffffffd9`, `--nav-hover-bg: #0a0b120d`, `--borderglow-glow: 10 11 18`, `--borderglow-bg: #f8f9fa`, body bg `rgb(255,255,255)`, body text `rgb(10,11,18)`; (d) BorderGlow cards on `/en/consulting` (light) render with `--card-bg: #f8f9fa` (division-surface), `--glow-color: hsl(232deg 30% 7% / 80%)` (dark navy), `--gradient-one: radial-gradient(at 80% 55%, #0A0B12 0px, transparent 50%)` — all 4 cards correctly theme-flipped; (e) rapid theme-toggle test (4 consecutive clicks) — instant bg flip each click, aria-label updates, localStorage persists, no stuck states, no console errors; (f) CTABanner in light mode on `/en/blog` — banner bg `rgb(255,255,255)`, h2 `rgb(10,11,18)` on white (19.64:1 ✓), button bg `rgb(10,11,18)` + text `rgb(255,255,255)` (19.64:1 ✓); (g) WCAG AA contrast spot-checks in light mode — body text 19.64:1 ✓, footer muted link 5.50:1 ✓ (post-token-tuning, up from 2.49:1), nav-link on navbar glass ~7.56:1 ✓, CTA button 19.64:1 ✓. Only console errors remaining are the pre-existing Phase L1 "Encountered a script tag while rendering React component" React 19 dev-mode false positives — unrelated to L4. Files: 10 edits across `src/app/globals.css` (glass + borderglow + nav-hover + text-muted tokens, `.glass` rewritten, `.glass-nav` tokenized, `.glow-hover` deleted, `chat-trigger-pulse` keyframe rewritten), `src/components/ui/BorderGlow.tsx` (imported useTheme, theme-aware default glowColor + colors), `src/components/sections/{ConsultingServicesGrid,MarketingServicesGrid,ServicesOverview,TeamGrid,TeamShowcase,BlogCard}.tsx` (3 prop lines removed each = 18 total), `src/components/global/Navbar.tsx` (4 hover-bg patterns converted), `src/components/global/ThemeToggle.tsx` (1 hover-bg converted), `src/components/global/Footer.tsx` (hairline hex + social hover-bg converted). New docs: `src/_project-state/phase-l4-global-chrome.md`. Updated docs: `src/_project-state/current-state.md`, `src/_project-state/file-map.md`.) **Phase L2 — Theme Toggle Button** (sun/moon toggle wired into the Navbar in two places — the desktop right cluster immediately LEFT of the language toggle, and a new top-right cluster inside the mobile hamburger overlay where the language toggle was relocated from its previous bottom-of-overlay position so the two toggles stay visually paired on both viewports. `ThemeToggle` client component (`src/components/global/ThemeToggle.tsx`) consumes `useTheme()` from Phase L1, renders `<Button variant="ghost">` matched to the language toggle's hand-tuned pill shape (`min-h-[44px] min-w-[44px] h-auto w-auto px-3 py-1.5 rounded-full`) — none of the CVA `icon-*` sizes match the language toggle's actual shape, so the sibling match is className-based not size-variant-based. Default `lucide-react` `Sun` icon shows in dark mode (click = go light), `Moon` in light mode (click = go dark). Icon swap uses `AnimatePresence mode="wait"` + `motion.span` with 90° rotate + scale 0.7 ↔ 1 over 0.2s (ease: 'easeOut'). Mount gate: outer `<Button>` className stays STABLE across the mount boundary (only inner content swaps: empty placeholder `<span>` on SSR + first paint, real icon after `useEffect`) — this avoids the Button primitive's `transition-all` registering a CSS opacity transition that gets stuck at `currentTime: 0` when React patches the className post-mount, a bug surfaced during verification on this codebase. SSR renders `<button aria-hidden="true" tabindex="-1"><span style="width:16px;height:16px"></span></button>` — no icon, no aria-label — so there's no wrong-icon flash when the real theme is light. Translated aria-labels under `nav.themeToggle.{toLight,toDark}` — EN "Switch to light mode" / "Switch to dark mode", MK "Премини на светла тема" / "Премини на темна тема". Session C `.focus-ring` utility applied for keyboard focus visibility; 44×44 touch target met on both viewports. `hover:bg-white/5` matches the language toggle's hover state. Mobile overlay relocation: the language toggle was previously inside a `<motion.div className="mt-2">` at the bottom of the overlay's flex-col, after the "Get in Touch" CTA — per the approved plan it was moved into a new `<motion.div className="absolute top-[72px] right-4 flex items-center gap-2">` that sits just below the fixed header (h-14 = 56px on mobile) so it doesn't collide with the X close button (which lives in the header at z-50 above the overlay at z-40). Theme toggle sits immediately BEFORE the language toggle inside the cluster. Theme toggle does NOT close the overlay on click (users can see the theme flip reflected on the page behind the menu before dismissing); language toggle preserves its pre-phase `setMobileOpen(false); toggleLocale()` behavior. Desktop right cluster order is now `[ThemeToggle][language toggle][Get in Touch CTA][hamburger (md:hidden)]`. **Verified end-to-end in the browser preview**: (a) `npm run build` clean — 48/48 static pages, zero TypeScript errors, 16.1s Turbopack compile; (b) desktop at `/en` dark mode — Sun icon visible, `opacity: 1`, 44×44 bbox, tabIndex 0, `aria-label="Switch to light mode"`; (c) `btn.click()` → theme flips to light, `localStorage['vertex-theme'] === 'light'`, `<html data-theme="light">`, body bg `rgb(255,255,255)`, icon swaps to Moon, `aria-label="Switch to dark mode"`; (d) second click reverses cleanly; (e) hard-reload with `localStorage['vertex-theme']='light'` — fetched HTML source confirms the SSR output is the aria-hidden placeholder with an empty span, NOT a Sun icon that would flash to Moon; (f) mobile (375×812 preset, `window.innerWidth === 399` due to browser-internal dpr behavior) — hamburger click → overlay opens, cluster at `top:72px right-4` with theme toggle (44×44, left:259.875, right:303.875) + language toggle (44h, 71w, left:311.875, right:383) paired, both meeting the 44×44 touch target floor; (g) click theme toggle inside mobile overlay → theme flips AND overlay stays open (`overlayOpen: true`); (h) click language toggle inside mobile overlay → URL `/en → /mk`, `<html lang="mk">`, overlay closes; (i) on `/mk` the theme toggle aria-label reads "Премини на светла тема" (Cyrillic) — translation keys working end-to-end. The pre-existing Phase L1 React 19 "Encountered a script tag while rendering React component" warnings still fire twice in dev-mode only — unrelated to this phase. (full light-mode token set alongside dark, `ThemeProvider` client component persisting to `localStorage['vertex-theme']` and tracking `prefers-color-scheme`, pre-hydration inline script in the locale layout, `color-scheme` CSS property, `suppressHydrationWarning` on `<html>` — no toggle UI yet, no utility-class or component-level migrations yet). Three edits and two creates: `src/app/globals.css` gained three blocks (a `color-scheme: dark/light` pair near the top, a `html[data-theme="light"] { --color-* }` override right after the main `@theme { }` block, and a combined `html[data-theme="light"], html[data-theme="light"] [data-division="consulting"], html[data-theme="light"] [data-division="marketing"] { --division-* + shadcn HSL }` override inside `@layer base` right after the marketing block — the naked `html[data-theme="light"]` selector is in the list so `<body>` — which lives outside the `DivisionProvider` wrapper and reads `--division-*` from the `:root` cascade — also flips). Palette = Option 1 Crisp White: `#FFFFFF` / `#F8F9FA` / `#F1F3F5` / `#E5E7EB` / `#9AA0AD` / `#0A0B12`. Current dark values (`#141414` / `#F5F5F5` / `#A3A3A3` etc.) left untouched — they drifted from the nominal plan's "Dark value" column at Finishing-Touches v2 and the plan's own instruction is to keep the actual dark in place and only layer light on top. Created `src/components/global/ThemeProvider.tsx` — client component providing `{ theme, setTheme, toggleTheme }` via `ThemeContext`, initial `useState('dark')` matching SSR, mount `useEffect` syncs to whatever the inline script wrote, `setTheme` persists to `localStorage['vertex-theme']` + writes `data-theme`, `toggleTheme` flips and persists, a second `useEffect` listens to `matchMedia('(prefers-color-scheme: dark)')` and only applies OS changes when localStorage is empty (so explicit user choice persists). Exports named `useTheme()` hook that throws if called outside the provider. Both `ThemeProvider` (default) and `useTheme` (named) re-exported from `src/components/global/index.ts`. The pre-hydration `themeInitScript` constant + `<head><script dangerouslySetInnerHTML /></head>` landed in `src/app/[locale]/layout.tsx` (root `src/app/layout.tsx` is a pass-through post-Session G and does not own `<html>`, so script + `ThemeProvider` wrap both live in the locale layout — the plan explicitly allows this). `<ThemeProvider>` wraps the existing `NextIntlClientProvider → MotionWrapper → DivisionProvider → ScrollProgress → children → BackToTop → ChatWidget` hierarchy as the outermost provider. No static `data-theme` attribute on the JSX `<html>` — the inline script owns it, paired with the pre-existing `suppressHydrationWarning` so React doesn't warn on the attribute appearing on the client but not the server-rendered HTML. **Verified:** (a) `npm run build` clean, 48/48 static pages, zero TypeScript errors; (b) fresh load on OS-dark browser → `<html data-theme="dark">` with no localStorage — OS preference honored; (c) `localStorage.setItem('vertex-theme', 'light')` + hard-refresh on OS-dark → `<html data-theme="light">` painted on first frame (body bg `rgb(255,255,255)`, `--division-bg: #FFFFFF`, `--division-accent: #0A0B12`, `--division-text-primary: #0A0B12`, all shadcn HSL tokens swapped); (d) `localStorage.removeItem('vertex-theme')` + reload → reverts to OS preference (dark — body bg `rgb(20,20,20)`); (e) rendered HTML source confirms the `<script>` is inside `<head>` and no `data-theme` attribute is in the server-sent `<html>` tag, so the flash-of-wrong-theme window is zero; (f) dark-mode token read-back post-phase matches pre-phase byte-for-byte (`--color-ink: #0e0e0e`, `--color-surface: #121212`, `--color-bright: #f5f5f5`, `--division-bg: #141414`, `--division-accent: #f5f5f5`, `--background: 0 0% 8%`, etc.) — no dark-mode regression. **Expected partial breakage in light mode today**: the `.glass` utility hardcodes `rgba(255,255,255,0.03)` so the scrolled navbar looks near-invisible on white; `BorderGlow`'s default `colors={['#F5F5F5','#C9C9C9','#A3A3A3']}` produces a light-on-light glow that disappears; hero shader backgrounds (Silk / Plasma / GridMotion panels) all use hardcoded hex / metallic dark gradients; a handful of `rgba(255,255,255,α)` in `DivisionSplit` and `globals.css` `.glow-hover` stay baked-dark. These are the Phase L3 (heroes), L4 (utilities + chrome), L5 (sections + pages) backlog. Dev console emits one React 19 warning about the inline script tag on SSR — known false-positive for the pre-hydration theme pattern used by `next-themes` and every major Next.js theme project; the script runs correctly (verified by the above tests) and the warning is dev-mode only. Files: 1 created (`src/components/global/ThemeProvider.tsx`, 99 lines); 3 modified (`src/app/globals.css`, `src/app/[locale]/layout.tsx`, `src/components/global/index.ts`). Created phase writeup: `src/_project-state/phase-l1-theme-foundation.md`. Prior closed work: **Phase 12 — AI Chat Widget (Core)** (streaming chat bubble, grayscale bot-icon trigger, bilingual, context-aware persona per URL). A full streaming chat widget mounted globally in the locale layout. `@anthropic-ai/sdk` wired behind `src/lib/ai.ts` (Ollama escape hatch in the same file — switching later is one env var). New Node-runtime route at `src/app/api/chat/route.ts` streams Claude Sonnet 4.6 responses as raw UTF-8 chunks. New `src/components/chat/` tree (BotIcon / TypingIndicator / ChatMessage / ChatPanel / ChatWidget + barrel). System prompt in `src/lib/chatWidget.ts` reads `siteConfig` so company facts stay in one place; persona varies by URL (homepage/consulting/marketing/blog/contact) and language flips on locale. Greeting matches the URL context on first open, panel is 380×560 on desktop and full-screen on mobile with safe-area insets. Grayscale palette only — bright trigger on elevated-surface bg with a soft 4-second outward pulse (reduced-motion-safe). Escape closes, click-X closes, client caps at 20 user messages, server caps at 40 total + 2000 chars per message. Kill switch via `NEXT_PUBLIC_CHAT_ENABLED=false`. `chat.*` translation namespace added (~40 keys in EN + MK, MK flagged for native review in `TRANSLATION_NOTES.md` §12-A). `.env.local.example` created and exempted in `.gitignore`. Lead capture + Telegram notification deferred to Phase 12B — this phase is the core widget only. Verified in the browser preview: trigger renders 56×56 bottom-right, click opens the panel with the context-appropriate greeting, `/en/consulting` flips greeting to the consulting one, locale switch `/en → /mk` flips every UI string plus the system-prompt language instruction to Macedonian, mobile resize (375×812) takes the panel full-screen, Escape closes, and without `ANTHROPIC_API_KEY` the first send surfaces the translated `chat.errors.generic` string in a `role="alert"` element with the empty assistant placeholder properly pruned from state. One design-surface bug caught and fixed during verification: the first-draft class list used `inset-0 rounded-none sm:inset-auto` + `sm:bottom-24 sm:right-6`; `twMerge` collapsed `sm:inset-auto` with the specific bottom/right as a conflict and dropped the positions. Final shape uses explicit per-side overrides (`top-0 right-0 bottom-0 left-0` on mobile, `sm:top-auto sm:left-auto sm:bottom-24 sm:right-6` on desktop) which survive twMerge cleanly. Owner action required before the widget is usable: **add `ANTHROPIC_API_KEY=sk-ant-...` to `.env.local` locally and to the Vercel project environment variables for production.** Without the key, every message send returns a 500 that surfaces as the friendly translated error. Files: 6 created under `src/components/chat/`, 1 created at `.env.local.example`, 1 created at `src/_project-state/12_phase-12-chat-widget.md`, and 9 modified (`src/lib/ai.ts`, `src/lib/chatWidget.ts`, `src/app/api/chat/route.ts`, `src/app/[locale]/layout.tsx`, `src/app/globals.css`, `messages/en.json`, `messages/mk.json`, `TRANSLATION_NOTES.md`, `.gitignore`). `npm run build` clean — 48/48 static pages generated, TypeScript clean in 76s, Turbopack compile clean in 38.7s, `/api/chat` appears as a dynamic route. Prior closed work: **Session K — Open Graph Preview Image** (sitewide OG / Twitter card now generated and injected — links shared to WhatsApp / LinkedIn / Facebook / Telegram / X render with a branded 1200×630 PNG instead of the platform default. Three creates and two edits. **(1) `src/app/opengraph-image.tsx`** — Next.js file-convention `ImageResponse` route; `#141414` linear-gradient background with two soft white halos for depth, white-gradient "V" tile + "Vertex Consulting" wordmark top-left, big "We help businesses grow smarter." headline with `smarter.` rendered in a subtle `#FFFFFF → #A3A3A3` text gradient, "Business consulting & digital marketing — from Strumica, Macedonia." subtitle in `#A3A3A3`, division-dot row (`Consulting · Marketing` both muted `#737373`), and `vertexconsulting.mk` bottom-right; exports `alt` / `size` / `contentType` route-segment config; uses system-ui font fallback (explicit Archivo loading was tried and dropped — satori only supports ttf/otf, Google Fonts v2 serves woff2, render fails with `Unsupported OpenType signature wOF2`). Lives at `src/app/` above the `[locale]/` segment so it cascades site-wide. Static-prerendered at build time (`○ /opengraph-image` in the route table). **(2) `src/app/twitter-image.tsx`** — one-line re-export `export { default, alt, size, contentType } from './opengraph-image'`; single source of truth, both cards stay in sync via one file. **(3) `src/proxy.ts` matcher exclusion extended** from `(?!api\|_next\|_vercel\|.*\..*)` to `(?!api\|_next\|_vercel\|opengraph-image\|twitter-image\|.*\..*)` — without this, next-intl caught the locale-neutral `/opengraph-image` route (no dot in the URL) and 307-redirected it to `/en/opengraph-image`, which 404'd because the file convention lives at `src/app/`, not under `[locale]/`. Sitemap and robots already bypassed the matcher via their `.xml` / `.txt` extensions. **(4) `src/lib/metadata.ts` openGraph + twitter blocks gained an explicit `images` field** pointing at `/opengraph-image` and `/twitter-image` (relative URLs resolved against `metadataBase = https://vertexconsulting.mk`). The brief explicitly said *not* to add an `images` field anywhere — but that assumption was wrong. When a child page exports its own `openGraph` block (as `generatePageMetadata` does for every consulting / marketing / about / contact / blog page), Next.js treats it as a full replacement and drops the framework-auto-injected file-convention image. Verified empirically: pre-fix curl on `/en/about` returned `og:title` + `og:url` + `og:locale` + `og:type` but **no** `og:image` or `twitter:image`; post-fix returns the full block. **(5) Design tracks current state, not the brief's literal hex codes** — the brief was written against the pre-Session-E "Arctic & Amethyst" palette (`#0A0B12` bg, blue `#60A5FA`, purple `#B490F0`) plus Sora font, both retired since (Session E unified to grayscale, Session J flipped BorderGlow defaults too, the heading font is Archivo per the most recent commit). Caught the drift during the read-project-state phase, surfaced to the user, got approval to follow the live design system instead of reintroducing the rejected aesthetic. **Verification:** (a) `npm run build` clean, both `/opengraph-image` and `/twitter-image` listed as `○` static-prerendered routes; (b) direct fetch returns `HTTP 200 image/png 84,044 bytes`, file inspected as `PNG image data, 1200 x 630, 8-bit/color RGBA, non-interlaced`, visually matches design; (c) `/en` carries all 10 framework-auto-injected meta tags (`og:image` + `og:image:type/width/height/alt` + `twitter:image` + `twitter:image:alt/type/width/height`) with content-fingerprint hash for cache-busting; (d) `/en/about`, `/en/contact`, `/en/consulting/business-consulting`, `/mk/about` all carry `og:image` + `twitter:image` after the metadata.ts fix, with `https://vertexconsulting.mk/opengraph-image` URLs resolved through `metadataBase`. **Environmental notes:** session started with `package-lock.json` deleted in the working tree (per `git status`); had to `git restore package-lock.json` and `npm ci` before dev/build worked. A lockfile-less `npm install` had pulled an `@next/swc-win32-x64-msvc` binary that Windows reports as "not a valid Win32 application" — Turbopack requires native bindings, so Turbopack-mode build/dev failed; webpack mode (`npx next build --webpack` / `npx next dev`) worked as a fallback during verification, and the lockfile-pinned versions worked under default Turbopack post-restore. Vercel deploy needed for production to pick up the card; validate post-deploy via opengraph.xyz or LinkedIn Post Inspector to clear cached previews. Files: 3 created (`src/app/opengraph-image.tsx`, `src/app/twitter-image.tsx`, `src/_project-state/session-k_og-image.md`), 2 modified (`src/proxy.ts`, `src/lib/metadata.ts`), 2 docs updated (`current-state.md`, `file-map.md`). Full writeup: `src/_project-state/session-k_og-image.md`. Prior closed work: **Session J — Polish** (closed two specific audit items with targeted, defensive edits. **(1) BorderGlow AI-palette trap — P2.** `src/components/ui/BorderGlow.tsx` default `colors` prop flipped from `['#c084fc', '#f472b6', '#38bdf8']` (purple / pink / cyan — the textbook AI palette from the impeccable skill's explicit reject list) to `['#F5F5F5', '#C9C9C9', '#A3A3A3']` matching the Session E unified grayscale. The parallel HSLA rainbow CSS fallbacks in `src/components/ui/BorderGlow.css` — 7 gradient positions × 2 stack layers (`::before` border-box stack, `::after` padding-box stack), plus the `--gradient-base` linear-gradient fallback in each — rewritten to the same grayscale values following the `COLOR_MAP = [0, 1, 2, 0, 1, 2, 1]` index mapping. All 5 current `<BorderGlow>` callers (ConsultingServicesGrid, MarketingServicesGrid, ServicesOverview, TeamGrid, TeamShowcase, BlogCard) already pass `colors={['#F5F5F5', '#C9C9C9', '#A3A3A3']}` explicitly, so runtime rendering is byte-identical — the fix is future-proofing against any new `<BorderGlow>` omitting the prop. **(2) Dead shadcn sidebar tokens — P3.** `src/app/globals.css` had 17 unused sidebar-related lines inherited from the `base-nova` scaffold: 8 `--color-sidebar-*` aliases in `@theme inline` and 9 lines (1 comment + 8 `--sidebar*: oklch(...)` definitions) in `:root`. This codebase ships no sidebar component; confirmed via `grep --sidebar-` across `src/` that pre-removal the only hits were inside `globals.css` itself. Both blocks removed; post-removal grep returns zero. No visual or functional change; removed the temptation for a future `bg-sidebar` / `text-sidebar` utility to appear without a real component backing it. **(3) CTABanner `max-w-xl` — P3 — evaluated, not applied.** `src/components/sections/CTABanner.tsx:53` subtext `<p>` carries `max-w-xl mx-auto` with no responsive prefix. The audit suggested wrapping as `sm:max-w-xl` so the constraint only kicks in at ≥640px. Math worked through: the parent's `max-w-4xl px-6` gives inner-content width `min(848px, viewport - 48px)`; `max-w-xl` (576px) only actively constrains when that column is ≥576px, which happens at viewport ≥~624px. At all narrower viewports, parent padding already limits the paragraph below 576px so `max-w-xl` is inert. Between `max-w-xl` and `sm:max-w-xl` there's a 16-pixel band (624–639px viewport) where behavior differs sub-pixel, and byte-identical output everywhere else. Ran `grep max-w-xl` across `src/components/sections/` — CTABanner is the only match, but `max-w-2xl`, `max-w-3xl`, `max-w-4xl` appear throughout on paragraphs and headings without responsive prefixes, all following the same idiom. Flipping only this one instance to `sm:max-w-xl` would be zero-visual-benefit source churn that creates inconsistency against the rest of the component library. Decision: leave as-is, document the rationale in the session writeup. Verified CTABanner `<p>` renders at exactly 576px on `/en/consulting` at desktop viewport — `max-w-xl` is actively constraining on desktop, design is correct. **Verification done:** (a) grep `--sidebar-` across `src/` returns zero hits post-change; (b) runtime DOM inspection on `/en/consulting` confirms `.border-glow-card` count = 4 (ConsultingServicesGrid 2×2 mounted), `--gradient-one` = `radial-gradient(at 80% 55%, #F5F5F5 0px, transparent 50%)` grayscale, `--gradient-base` = `linear-gradient(#F5F5F5 0 100%)` grayscale, `--card-bg` = `#1C1C1C` (consulting surface) unchanged, `--glow-color` = `hsl(0deg 0% 85% / 80%)` unchanged; (c) Fast Refresh recompiled each edit in 150–550ms with HMR staying connected; (d) zero console errors, zero server errors; (e) `fetch('/en')` returns 200 OK. `npm run build` compiles cleanly under Turbopack (`✓ Compiled successfully in 12.4s`) but fails the TypeScript step with a pre-existing Cyrillic type error at `src/app/[locale]/layout.tsx:20` — Archivo font doesn't ship a `'cyrillic'` subset in its next/font type union. This error predates Session J (confirmed via `git diff --stat` — only `globals.css` and the two `BorderGlow.*` files in my diff) and is the Cyrillic font-subset gap flagged as the top launch blocker in the design-context memo. Not in scope for polish; needs a dedicated font session. Files modified: `src/components/ui/BorderGlow.tsx` (1 line), `src/components/ui/BorderGlow.css` (16 lines across 2 blocks), `src/app/globals.css` (17 lines across 2 blocks). Full writeup: `src/_project-state/session-j_polish.md`. Prior closed work: **Session H — Performance Hardening** (closed two P1 runtime items flagged in the Session G post-audit: shader render loops burning CPU/GPU while offscreen, and `BorderGlow`'s pointer handler firing CSS-variable writes at mouse-input rate — up to ~1000 Hz on a gaming mouse. Three fixes. **(1) WebGL viewport + tab-visibility gating** on `src/components/backgrounds/Silk.tsx`, `Plasma.tsx`, `GridMotion.tsx`. Each background gained an `IntersectionObserver({ threshold: 0 })` on its root wrapper plus a `document.visibilitychange` listener, both AND-ed through one `apply()` callback that fully halts the render loop when the wrapper scrolls offscreen OR the tab is hidden. Silk flips R3F `<Canvas frameloop>` between `'always'` and `'never'` via state; Plasma's OGL `requestAnimationFrame` loop is gated by a `start()/stop()` pair guarded with a `raf === 0` sentinel AND uses a locally-accumulated `simSeconds` instead of `performance.now() - t0` so a shader doesn't skip-jump forward by however long the tab was hidden; GridMotion uses `gsap.ticker.add`/`remove` to fully detach the ticker callback when paused. All three start with `inView = true` so the above-fold hero renders on first frame (IO callbacks fire async; starting `false` would cause a one-frame blank). **(2) BorderGlow pointer handler rAF-throttled** (`src/components/ui/BorderGlow.tsx` lines 200–216). The two CSS-variable writes now flow through a pair of refs (`pendingPointer` for the latest coords, `rafIdRef` for the scheduled frame); `handlePointerMove` only updates the coords and schedules one `requestAnimationFrame(flushPointer)` per frame. `flushPointer` reads the coords, re-reads a fresh `getBoundingClientRect()`, and writes the two vars. A cleanup useEffect cancels any pending rAF on unmount. Result: ~60 writes/second at 1000 Hz input instead of ~1000 — 16× fewer layout-affecting writes. **(3) Bundle config** `next.config.ts` gained `experimental: { optimizePackageImports: ['motion', 'lucide-react', 'gsap'] }`. Null-delta bundle reduction measured (+0.3 KB noise per route; these packages' import patterns already tree-shake effectively under Next 16's default webpack config) but retained as future-proofing for barrel-heavy imports added later. `.claude/launch.json` gained a `vertex-prod` entry (`next start -p 3001`, `autoPort: true`) for production-build perf measurement. **Measured:** marketing-mobile Lighthouse TBT dropped from **168,102 ms → 530 ms (-99.7%)**, perf score 0.38 → 0.82 (+44 pp), LCP 6.09 s → 2.85 s, on the sample where Lighthouse's trace window caught the gate engaging. Other routes' deltas sit inside Lighthouse's observed variance (±0.3 perf swing on identical URLs on this Windows runtime — observed 0.63/0.63/0.96 in three same-build same-URL runs) because Lighthouse's 45 s window keeps heroes above the fold in most sample configurations; the real long-tail wins (battery/CPU when scrolled past, tab hidden, long reading sessions) aren't captured by Lighthouse's single-page trace. Four files modified + one config + one launch entry. **Part 3 of Session H — server-component refactor.** After part 1–2 verification, audited what was actually in the 912 KB first-load JS and found that three.js / OGL / GSAP were ALREADY in async chunks — the first-load JS was mostly framework + Motion + shared app code + the 9 section components that were all being hydrated unnecessarily. Converted 9 "static" sections (ValuesGrid, CompanyTimeline, ProcessSteps, LeaderIntro, TeamShowcase, TeamGrid, ConsultingServicesGrid, MarketingServicesGrid, ServicesOverview) from client to server components, and their 4 page-level client wrappers (HomePage `page.tsx`, AboutPageClient, ConsultingLandingClient, MarketingLandingClient). Extracted a new `StaggerItem` client wrapper (`src/components/global/StaggerItem.tsx`) so sections can compose stagger animations without importing `motion` directly — pairs with the pre-existing `AnimateIn` and `StaggerContainer`. Sections that genuinely need client reactivity (HeroSection's immediate `animate="visible"` on mount, DivisionSplit's hover state, SocialProof's IntersectionObserver + counter, FAQAccordion's open state, ContactForm, BlogCard's `whileHover`, CTABanner's custom multi-delay animations) remain client. Result: **first-load JS dropped from 911–912 KB to 887–896 KB (−19 KB per route, 12 chunks → 11)** across all main routes. Modest KB-wise but architecturally correct — the sections stream as HTML instead of shipping as JS waiting to hydrate, which reduces main-thread work at TTI in a way that doesn't show up in first-load JS size measurements. Verified at runtime across `/en`, `/en/about` (4 values cards + 4 timeline items + 4 team cards), `/en/consulting` (4 services + leader), `/en/marketing` (4 services + 3 team), `/mk/consulting/business-consulting` (4 process steps rendering in MK — confirms server-side `getTranslations` picks up the locale correctly). Full writeup: `src/_project-state/session-h_performance.md`. **Also completed 2026-04-19: Session I — Responsive Adapt** (two Tailwind breakpoint-ladder fixes. `src/components/sections/TeamShowcase.tsx:31` → `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` so the 768–1023 px tablet range renders 2-col instead of three cramped 195 px cards. `src/components/sections/SocialProof.tsx:78` → `grid-cols-2 md:grid-cols-4` so the 4-col stats row starts at `md:` instead of `lg:`, giving tablets the same full row as desktop. Navbar hamburger verified at 44×44 at 375×667 — floor already met by Session C, no change. Full writeup: `src/_project-state/session-i_responsive-adapt.md`.) Prior closed work: Session G — Accessibility Hardening (closed eight findings from the follow-up a11y audit in one pass. P1: root layout restructured so `<html lang={locale}>` lives on the `[locale]/layout.tsx` HTML shell — the standard next-intl App Router pattern — resolving WCAG 3.1.1; Navbar dropdown rewritten as a W3C disclosure pattern with a dedicated `<button aria-haspopup="menu" aria-expanded aria-controls>` trigger, Escape-to-close with focus return, and mouse-hover retained as progressive enhancement; ContactForm + Footer newsletter fields gained `aria-invalid` / `aria-describedby` + `role="alert"` error associations with inline field-level errors. P2: FAQAccordion triggers got `aria-controls` + matching panel `id` with `role="region"` + `aria-labelledby`; mobile menu now applies `inert` to `<main>` and `<footer>` while open, `Escape` closes + returns focus to the hamburger; new sitewide skip-to-content link in `[locale]/(site)/layout.tsx` with translations. P3: all decorative chevrons / nav icons carry `aria-hidden="true"`; contact + newsletter submit buttons expose `aria-busy` during in-flight state. Two new translation keys — `common.skipToContent`, `nav.submenuToggleAria` — added to both `messages/en.json` and `messages/mk.json`. Prior closed work: Session F (fonts), Session E (theme/polish), Session D (performance), Session C (accessibility), Phase 15A–15F (full bilingual i18n), Phase 11, Session A, Session B. The site is translation-complete with brand fonts rendering natively on both locales, WCAG 2.4.7 / 1.4.11 / 3.1.1 / 2.4.1 / 4.1.2 compliant for keyboard focus, form contrast, document language, skip-navigation, and disclosure semantics, performance-clean on FAQ pattern + shader backgrounds + pointer handling, responsive-clean on the TeamShowcase + SocialProof grids, and fully token-driven for component-level color; remaining launch blockers are external: native-speaker MK review + lawyer-reviewed privacy MK translation.

## Project overview
Vertex Consulting website. Next.js 16 App Router, React 19, TypeScript strict, Tailwind v4 (CSS-first config), shadcn/ui (`base-nova`), Motion v12 (imported from `motion/react`). Two divisions: **Consulting** (monochrome) and **Marketing** (lavender/purple). Dark mode only. Domain: `vertexconsulting.mk`. Owner Goran Dinov. Founded 2018.

## What works right now
- **Light-mode polish bugs closed (Phase L6 — 2026-04-24).** The homepage Silk hero reads as a subtle flowing background texture in light mode (dampened via `--silk-opacity: 0.3` wrapper) rather than the heavy metallic-waves pattern that was competing with the headline. `SILK_COLOR_LIGHT` retuned `#CDD1D7` → `#D8DCE2` so the damped output still has visible texture. Every hoverable card across the site has readable text in both themes and both hover states — DivisionSplit (h3 hover-white removed, CTA + chips tokenized, hover border uses new `--division-border-hover`), BlogCard (h3 hover-white dropped — BorderGlow's edge glow carries the hover affordance), MarketingServicesGrid + ServicesOverview (icon + h3 hover-white tokenized to theme-aware `--division-text-primary`). BorderGlow's pointer-follow corner glow is now perceptually balanced between themes via a light-mode intensity multiplier in `BorderGlow.tsx` (`effectiveIntensity = glowIntensity * (theme === 'light' ? 1.4 : 1.0)`) — dark mode stays byte-identical, light mode glow ramps ~40% to compensate for the perceptual asymmetry between dark-glow-on-light-card and light-glow-on-dark-card. Cross-section grep confirmed zero remaining `group-hover:text-white` / `hover:text-white` / `group-hover:text-[var(--color-bright)]` occurrences. New tokens: `--silk-opacity` + `--division-border-hover` pairs. Dark-mode regression verified — every L6-touched token reads pre-L6-byte-identical in dark (`--silk-opacity: 1`, `--division-border-hover: rgba(245,245,245,0.188)` ≈ pre-L6 `0.19`, BorderGlow glow vars `8% / 16% / 24% / 32% / 40% / 48% / 80%` for `-10/-20/-30/-40/-50/-60/base`). Full writeup: `src/_project-state/phase-l6-polish-and-fixes.md`.
- **All three hero backgrounds render correctly in both themes (Phase L3 — 2026-04-24).** Silk (homepage), GridMotion (consulting), and Plasma (marketing) are now theme-aware. Silk and Plasma consume `useTheme()` inside their wrapper components (`BackgroundSilk.tsx`, `BackgroundPlasma.tsx`) and pass a theme-appropriate `color` prop to the shader — Silk `#2A2D33` / `#CDD1D7`, Plasma `#F5F5F5` / `#2A2D33`. Silk's internal `useMemo` already had `color` in its deps array, so prop changes propagate into the `uColor` uniform without remount — theme swap on `/` is seamless (`document.querySelectorAll('canvas').length` stays `1` across the toggle). Plasma's single effect has `color` in deps and tears down the OGL renderer on change, causing a brief ~150ms canvas re-init on marketing theme flips — acceptable since theme flips are rare. GridMotion's `.metallic-panel` CSS class in `globals.css` now consumes seven new CSS variables that flip instantaneously (one compositor tick, no React re-render, GSAP ticker continues): `--hero-panel-stop-1/2/3` (radial stops), `--hero-panel-stripe-bright/shadow` (striation alphas), `--hero-panel-border` (rim), `--metallic-panel-shadow` (inset depth). Outer section bg uses `--hero-grid-bg`. Consulting hero scrim uses `--hero-scrim-center` + `--hero-scrim-mid` — darkens the text area in dark mode, lightens it in light mode (same purpose: hero-text contrast over the animated bg, inverted direction). Dark-mode values in `:root` are the live pre-L3 state verbatim (regression-clean: stops `#A8ACB4 / #4A4E56 at 50% / #14171C`, stripes `rgba(255,255,255,0.06) + rgba(0,0,0,0.14)`, grid bg `#0E0E0E`). Light-mode values recalibrated to `#3A3D42 (dark titanium) / #8A8E94 / #D4D7DC (light silver blending toward #F1F3F5 page bg)` — preserves metallic identity while keeping panels visibly darker than the page (not inverted-to-dark-blobs). Three pre-existing theme bugs fixed in scope: `BackgroundGrid.tsx` hardcoded `#121212` on wrapper + reduced-motion fallback → `var(--division-bg)`; `ConsultingLandingClient.tsx` hardcoded `rgba(14,14,14,...)` scrim → tokenized; marketing hero's explicit `color="#F5F5F5"` on `<BackgroundPlasma>` removed so the wrapper default takes over. **New CSS variables:** `--hero-grid-bg`, `--hero-panel-stop-1`, `--hero-panel-stop-2`, `--hero-panel-stop-3`, `--hero-panel-stripe-bright`, `--hero-panel-stripe-shadow`, `--hero-panel-border`, `--metallic-panel-shadow`, `--hero-scrim-center`, `--hero-scrim-mid` — each under `:root` (live dark) + `html[data-theme="light"]`. **Verified:** build clean (48/48), dark regression passes (all tokens match pre-phase, panel computed-style `rgb(20,23,28) / rgb(14,14,14)` section), light-mode forward passes (`rgb(212,215,220) / rgb(241,243,245)`), theme-swap smooth on both `/en` and `/en/consulting`, all routes + MK locale confirmed. **Aesthetic note for follow-up:** Silk in light mode reads heavier than ideal — `uColor = #CDD1D7` paints the pattern's dark zones as ~`#2F3033` which competes with the `--division-text-secondary: #4B5563` subtitle. Proposed tuning (lighten `SILK_COLOR_LIGHT` toward `#E8EBEE`/`#F0F2F5`, or lower `noiseIntensity` in light mode) documented in `phase-l3-hero-backgrounds.md`; not applied unilaterally. GridMotion and Plasma in light mode both read well with no tuning needed. Full writeup: `src/_project-state/phase-l3-hero-backgrounds.md`.
- **Global chrome renders correctly in both themes (Phase L4 — 2026-04-23).** Every "appears on every page" surface is now theme-aware through CSS variables or `useTheme()`-driven component logic. `.glass` utility (dropdown menus, BackToTop FAB, and any generic glass card) consumes `--glass-bg` + `--glass-border` tokens that flip from light-alpha-on-dark (`rgba(255,255,255,0.03)` / `rgba(255,255,255,0.08)`) in dark mode to dark-alpha-on-light (`rgba(10,11,18,0.04)` / `rgba(10,11,18,0.08)`) in light mode — the per-division `.glass` variants (`[data-division="consulting"]`, `[data-division="marketing"]`) that had been holding identical values post-Finishing-Touches-v2 were deleted. `.glass-nav` (the scrolled-navbar backdrop) got its own distinct token pair because it needs a solid-ish higher-alpha tint, not a subtle overlay — `--glass-nav-bg: rgba(14,14,14,0.85)` + `--glass-nav-border: rgba(255,255,255,0.06)` in dark, `rgba(255,255,255,0.85)` + `rgba(10,11,18,0.08)` in light. Reserved `--glass-bg-strong` / `--glass-border-strong` tokens are defined but not yet consumed — available for future modal surfaces. BorderGlow component has theme-aware defaults picked via `useTheme()` inside the component — the `glowColor` prop is parsed by JS (`parseHSL`, HSL triplet expected), so a CSS variable can't be its value; the component reads theme state and picks `'0 0 85'` (pale grayscale sheen) in dark or `'232 30 7'` (near-black navy) in light. `colors` (the mesh-gradient palette feeding border + fill) flips the same way: `['#F5F5F5','#C9C9C9','#A3A3A3']` in dark, `['#0A0B12','#4B5563','#9AA0AD']` in light. `backgroundColor` prop default became `var(--borderglow-bg)` which chains through `var(--division-surface)` (already theme-aware via L1). All 6 BorderGlow call sites (ConsultingServicesGrid, MarketingServicesGrid, ServicesOverview, TeamGrid, TeamShowcase, BlogCard) had been passing 3 identical explicit props each (`colors`, `glowColor`, `backgroundColor`) — all 18 prop lines deleted now that the defaults are correct. Prose classes (`.prose-consulting` / `.prose-marketing` / `.prose-blog`) verified already fully tokenized post-Session-E — link color via `--division-accent`, strong + h2 + h3 via `--division-text-primary`, body via `--division-text-secondary`. Navbar + Footer + ThemeToggle hover overlays converted from dark-only `hover:bg-white/5` (Tailwind shorthand for `rgba(255,255,255,0.05)`) to `hover:bg-[var(--nav-hover-bg)]` which resolves to `rgba(255,255,255,0.05)` in dark and `rgba(10,11,18,0.05)` in light. Footer top-edge hairline moved from `style={{ backgroundColor: '#262626' }}` to `var(--division-border)` so it inverts cleanly. Footer brand SVGs already used `stroke="currentColor"` so they auto-inherit the token-driven link color. BackToTop + ScrollProgress work automatically: BackToTop inherits the rewritten `.glass` and its button bg/text both read `--division-*` tokens that L1 already flips; ScrollProgress's 2px bar reads `--division-accent` which inverts cleanly (bright bar in dark, near-black bar in light). Button primitive's CVA variants all consume shadcn HSL tokens (`bg-primary`, `bg-muted`, `border-input`, `ring-ring`, etc.) which L1 overrides in light mode — no primitive-level changes needed. CTABanner background was already on `var(--color-ink)` (Session E) which flips from `#0E0E0E` to `#FFFFFF` in light mode. In light mode the banner's boundary dissolves against the white page bg, but the visual weight is carried by the inverted-contrast dark button (19.64:1 contrast) and dark heading — semantically the banner is still "the deepest surface on the page," which is what `--color-ink` represents. Chat widget (ChatWidget, ChatPanel, ChatMessage, TypingIndicator) already consumed `var(--color-*)` tokens (`--color-bright`, `--color-ink`, `--color-surface`, `--color-elevated`, `--color-border`, `--color-muted`) exclusively — no component edits needed. The `@keyframes chat-trigger-pulse` in `globals.css` had hardcoded `rgba(241, 243, 247, α)` for the pulse ring (the only baked-light-color in chat-related styles); rewritten to `rgba(var(--borderglow-glow) / α)` so the pulse ring is bright in dark mode and near-black in light. Dead `[data-division="marketing"] .glow-hover:hover` CSS rule (flagged in the L1 backlog) deleted — grep confirmed zero callers and the inline comment said "superseded by BorderGlow in Phase 4". L1-token follow-up: `--division-text-muted` in light mode raised from `#9AA0AD` (~2.49:1 contrast on `#F8F9FA` — fails WCAG AA) to `#5F6670` (5.50:1 ✓ AA) so footer muted links and indicator dots meet the body-text floor while preserving hierarchy vs secondary (`#4B5563`, 8.59:1). **New CSS variables:** `--glass-bg`, `--glass-border`, `--glass-bg-strong`, `--glass-border-strong`, `--glass-nav-bg`, `--glass-nav-border`, `--borderglow-glow`, `--borderglow-bg`, `--nav-hover-bg` — each declared under `:root` (dark defaults) and overridden under `html[data-theme="light"]`. **Verified:** (a) `npm run build` clean — 48/48 static pages, zero TypeScript errors; (b) dark-mode token read-back matches pre-phase byte-for-byte on every L1 token (regression-clean); (c) light mode on `/en/blog` confirmed — `--glass-bg: #0a0b120a`, `--glass-nav-bg: #ffffffd9`, `--nav-hover-bg: #0a0b120d`, `--borderglow-glow: 10 11 18`, `--borderglow-bg: #f8f9fa`, body bg `rgb(255,255,255)`, body text `rgb(10,11,18)`; (d) `/en/consulting` in light — 4 BorderGlow cards render with `--card-bg: #f8f9fa`, `--glow-color: hsl(232deg 30% 7% / 80%)` (dark navy), `--gradient-one: radial-gradient(at 80% 55%, #0A0B12 0px, transparent 50%)` — theme-flipped correctly; (e) rapid-click theme-toggle test (4 consecutive clicks) — instant body-bg flip on each click, aria-label updates, localStorage persists, no stuck states, no new console errors; (f) CTABanner in light — banner `rgb(255,255,255)`, h2 `rgb(10,11,18)` at 19.64:1 ✓, button bg `rgb(10,11,18)` + text `rgb(255,255,255)` at 19.64:1 ✓; (g) WCAG AA contrast measured in light mode — body text 19.64:1, footer muted link 5.50:1, nav link (idle, secondary text) 7.56:1, CTA button 19.64:1 — all pass. Console-only remaining warnings are the pre-existing Phase L1 React 19 dev-mode "Encountered a script tag while rendering React component" false positives — unrelated. **Still visibly wrong in light mode (out-of-scope backlog):** the three hero backgrounds (Silk shader, GridMotion tiled metallic panels, Plasma shader if still mounted — Phase L3), and section-level components that still contain hardcoded dark styling — HeroSection shell, DivisionSplit, ServicesOverview card icon containers, TeamShowcase avatar borders, MarketingServicePage border-white/20 hover, ConsultingServicePage hover bgs, FAQAccordion trigger hover, BlogListingClient filter pill hover borders, ContactForm surface treatment, privacy page, thank-you page (Phase L5). Files: 10 edits in this phase — `src/app/globals.css` (glass + borderglow + nav-hover + text-muted token adjustments; `.glass` rewritten; `.glass-nav` tokenized; `.glow-hover` deleted; `chat-trigger-pulse` keyframe rewritten), `src/components/ui/BorderGlow.tsx` (useTheme import + theme-aware defaults), `src/components/sections/{ConsultingServicesGrid,MarketingServicesGrid,ServicesOverview,TeamGrid,TeamShowcase,BlogCard}.tsx` (redundant explicit props deleted), `src/components/global/Navbar.tsx` (4 hover patterns converted), `src/components/global/ThemeToggle.tsx` (1 hover pattern converted), `src/components/global/Footer.tsx` (hairline hex + social hover converted). Full writeup: `src/_project-state/phase-l4-global-chrome.md`.
- **Theme toggle button (Phase L2 — 2026-04-23).** `ThemeToggle` client component wired into the Navbar in two places. Desktop: immediately LEFT of the language toggle in the right cluster, making the order `[ThemeToggle][EN/MK toggle][Get in Touch][hamburger (md:hidden)]`. Mobile hamburger overlay: new top-right cluster at `absolute top-[72px] right-4` (just below the 56px fixed header) holds `[ThemeToggle][EN/MK toggle]` — the language toggle was relocated from its previous location at the bottom of the overlay's flex-col (inside a `<motion.div className="mt-2">` after the CTA) so the two preference controls stay visually paired across both viewports. Icon via `lucide-react`: Sun when dark (click → light), Moon when light (click → dark). Crossfade on swap: `AnimatePresence mode="wait"` + `motion.span` keyed on theme with `initial/animate/exit` of `{opacity, rotate (±90°), scale (0.7↔1)}` over 0.2s. Translated aria-label + title via `nav.themeToggle.{toLight, toDark}` — EN "Switch to light mode" / "Switch to dark mode", MK "Премини на светла тема" / "Премини на темна тема". 44×44 touch target (min-h-[44px] min-w-[44px]) meets Session C's floor on both viewports; `.focus-ring` utility for keyboard visibility; `hover:bg-white/5` matches the language toggle's hover. **Sibling-shape to the language toggle is className-based**, not CVA-size-based — the language toggle does not use any `icon-*` CVA size, it overrides `size="default"` with hand-tuned classes (`min-h-[44px] gap-1.5 h-auto px-3 py-1.5 rounded-full`), so the ThemeToggle matches that className recipe instead of picking a size variant. **No-flicker guarantee via stable-outer / swappable-inner** — the outer `<Button>` className is STABLE across the mount boundary (only `aria-label`, `aria-hidden`, `onClick`, `tabIndex`, `title` toggle), and the inner content swaps from an empty placeholder `<span style="width:16px;height:16px">` on SSR + first paint to the real `AnimatePresence`-wrapped icon on `useEffect`-driven `setMounted(true)`. This structure avoids the Button primitive's `transition-all` registering a CSS opacity transition that gets stuck at `currentTime: 0` when the className changes post-mount — a browser quirk surfaced during verification that would have left the toggle permanently invisible had the plan's nominal "toggle opacity-0 on/off" approach been kept. SSR HTML fetch confirms the server output is the aria-hidden placeholder — no Sun/Moon icon in the server-rendered tree, so no wrong-icon flash when the real theme is light. **Verified**: (a) `npm run build` clean — 48/48 static pages, zero TypeScript errors; (b) desktop dark-mode click → theme flips to light, `localStorage['vertex-theme']='light'`, `<html data-theme='light'>`, body bg `rgb(255,255,255)`, icon swaps Sun → Moon, aria-label swaps, body surfaces flip to white; (c) reverse click cleanly reverses; (d) hard-reload with `localStorage='light'` shows no icon flicker (SSR placeholder verified via curl); (e) mobile (375px preset) — hamburger click → overlay opens, top-right cluster holds both toggles at 44×44 each, theme click flips theme WITHOUT closing overlay, language click closes overlay AND changes locale; (f) on `/mk` the theme toggle's aria-label is the Cyrillic string. Files: 1 created (`src/components/global/ThemeToggle.tsx`); 3 modified (`messages/en.json`, `messages/mk.json`, `src/components/global/Navbar.tsx`, `src/components/global/index.ts`). Full writeup: `src/_project-state/phase-l2-theme-toggle.md`.
- **Light-mode theme foundation, plumbing-only (Phase L1 — 2026-04-23).** Complete light-mode token set now lives alongside the dark set in `src/app/globals.css`. `html[data-theme="light"]` overrides the six `@theme` grayscale core tokens (`--color-ink/surface/elevated/border/muted/bright` → `#FFFFFF / #F8F9FA / #F1F3F5 / #E5E7EB / #9AA0AD / #0A0B12`). A combined selector list — `html[data-theme="light"]`, `html[data-theme="light"] [data-division="consulting"]`, `html[data-theme="light"] [data-division="marketing"]` — overrides all ten `--division-*` tokens (bg/surface/card/border/accent/accent-muted/glow/text-primary/text-secondary/text-muted) and nineteen shadcn HSL tokens (background, foreground, muted, muted-foreground, card, card-foreground, popover, popover-foreground, border, input, ring, primary, primary-foreground, secondary, secondary-foreground, accent, accent-foreground, destructive, destructive-foreground) to the Crisp White palette. The naked `html[data-theme="light"]` selector is in the list because `<body>` lives outside the `DivisionProvider` wrapper and reads `--division-bg` / `--division-text-primary` from the `:root` cascade. A `color-scheme` CSS rule (dark default, flipped to light on the attribute) makes native scrollbars, form controls, and text-selection highlights adapt. `src/components/global/ThemeProvider.tsx` (client component, 99 lines) provides `ThemeContext` (`{ theme: 'light' | 'dark', setTheme, toggleTheme }`), persists to `localStorage['vertex-theme']`, and subscribes to `prefers-color-scheme` changes — OS changes only apply when localStorage is empty, so an explicit user choice wins forever after. A `themeInitScript` constant in `src/app/[locale]/layout.tsx` runs synchronously inside `<head>` BEFORE React hydrates, reads localStorage + system preference in that order, and writes `data-theme` on `<html>`. No static `data-theme` attribute sits on the JSX `<html>` element — the script owns it, and the pre-existing `suppressHydrationWarning` keeps React quiet about the server-client attribute diff. `<ThemeProvider>` wraps the existing `NextIntlClientProvider → MotionWrapper → DivisionProvider → ScrollProgress → children → BackToTop → ChatWidget` hierarchy as the outermost provider. All dark values left untouched — the current `#141414` / `#F5F5F5` / `#A3A3A3` values the site has used since Finishing-Touches v2 are byte-identical post-phase (verified via DevTools computed-style read-back). Flash-of-wrong-theme window is zero: rendered HTML source confirms the `<script>` sits inside `<head>` and no `data-theme` attribute is on the server-sent `<html>` — the script executes during browser HTML parse, before paint. DevTools tests passed: (1) fresh load → OS preference honored; (2) `localStorage.setItem('vertex-theme', 'light')` + reload → immediate light paint, body bg flips `rgb(20,20,20)` → `rgb(255,255,255)`, every `--color-*` / `--division-*` / shadcn HSL token swaps; (3) `localStorage.removeItem('vertex-theme')` + reload → reverts to OS preference; (4) dark-mode regression check confirms every token reads its pre-phase value. **No toggle UI yet** (Phase L2). **No utility-class or component-level migrations yet** (Phases L3–L5) — `.glass`, `BorderGlow`'s default colors, `[data-division="marketing"] .glow-hover`, `.metallic-panel`, Silk / Plasma / GridMotion shader backgrounds, and a handful of hardcoded `rgba(255,255,255,α)` in `DivisionSplit` / `globals.css` all still use dark-mode baselines and will look visibly wrong in light mode until swept. `npm run build` clean (48/48 static pages, zero TypeScript errors, 6.0 s Turbopack compile, 5.7 s TypeScript, 894 ms static gen). One React 19 dev-console warning ("Encountered a script tag while rendering React component") fires twice on `[locale]/layout.tsx` render — known false-positive for the pre-hydration theme pattern used by `next-themes` and every major Next.js theme project; the script demonstrably runs correctly (see test (2) above) and the warning is dev-mode only, not build-blocking. Files: 1 created (`src/components/global/ThemeProvider.tsx`); 3 modified (`src/app/globals.css`, `src/app/[locale]/layout.tsx`, `src/components/global/index.ts`). Full writeup: `src/_project-state/phase-l1-theme-foundation.md`.
- **AI Chat Widget (Phase 12 — 2026-04-20).** Global floating chat widget mounted in the locale layout. A 56×56 bot-icon trigger sits bottom-right, pulses every 4s when idle (respects `prefers-reduced-motion`), and opens a 380×560 panel (full-screen on mobile with `env(safe-area-inset-bottom)`) with a soft spring animation. The widget greets visitors with a context-appropriate message based on the current URL path — `/consulting/*`, `/marketing/*`, `/blog/*`, `/contact`, and the default (homepage/shared) each get their own greeting in both locales. User messages POST to `/api/chat`, which runs on the Node.js runtime (`export const runtime = 'nodejs'` — Anthropic SDK needs Node APIs) and streams back a `claude-sonnet-4-6` response token by token as a `ReadableStream` of raw UTF-8 text chunks (`X-Accel-Buffering: no` to keep Vercel from buffering). During streaming, a 3-dot `.typing-dot` indicator shows until the first token, then gets replaced by an incrementally-filling assistant bubble with a 2px pulsing caret at the tail. The system prompt (`src/lib/chatWidget.ts`) reads `siteConfig` so company facts stay in a single source of truth, and includes language + persona instructions so Claude answers in the current locale with the right tone for the section. The provider abstraction lives in `src/lib/ai.ts` — switching to self-hosted Ollama later requires only `AI_PROVIDER=ollama` in env; both providers expose the same streaming `AsyncGenerator<string>` interface. Escape closes the panel, click-X closes, click-trigger reopens with history preserved (state lives in the locale-layout mount so it survives client-side navigation within a locale). Client-side cap of 20 user messages per session with a translated limit message; server also enforces 40-message and 2000-char bounds. Kill switch via `NEXT_PUBLIC_CHAT_ENABLED=false`. Lead capture and Telegram notification are NOT part of this phase — Phase 12B will add them (intent detection → slide-in name+email form inside `ChatPanel` → POST to `/api/chat/lead` → Resend + Telegram ping → per-IP rate limit). All widget plumbing is in place so 12B is purely additive. Known gotcha surfaced during verification: positioning the panel with `inset-0 ... sm:inset-auto sm:bottom-24 sm:right-6` lets `twMerge` collapse the last three as a conflict and drop the specific positions — the final `ChatPanel.tsx` uses explicit per-side classes (`top-0 right-0 bottom-0 left-0` on mobile, `sm:top-auto sm:left-auto sm:bottom-24 sm:right-6` on desktop) which twMerge leaves alone. Owner action: **add `ANTHROPIC_API_KEY` to `.env.local` locally and to Vercel project env vars for production** — without it, every send surfaces the translated `chat.errors.generic` string.
- **Open Graph / Twitter preview card injected sitewide (Session K — 2026-04-19).** A 1200×630 branded PNG generated by Next.js's `ImageResponse` file convention at `src/app/opengraph-image.tsx`, served as `/opengraph-image` (build-time-prerendered). The image carries `#141414` linear-gradient background, white "V" logo tile + "Vertex Consulting" wordmark, big "We help businesses grow smarter." headline with subtle white→`#A3A3A3` gradient on `smarter.`, division-dot row with `vertexconsulting.mk` URL — visually consistent with the unified grayscale palette and Archivo-influenced spacing the rest of the site uses (font itself falls back to system-ui because satori can't parse Google Fonts' woff2 output). `src/app/twitter-image.tsx` re-exports the same generator so the Twitter/X card stays in sync via a single source of truth. `src/proxy.ts` now excludes both routes from the next-intl matcher so the locale-neutral file-convention URL serves directly without a `/en/` prefix injection. `src/lib/metadata.ts` `generatePageMetadata` sets explicit `images` fields on its `openGraph` + `twitter` blocks because Next.js shallow-replaces parent `openGraph` when a child page declares its own — without the explicit field, every page using this helper (consulting / marketing / about / contact / blog) had no `og:image` despite the file convention sitting at root. Verified `og:image` + `twitter:image` presence on `/en`, `/mk`, `/en/about`, `/en/contact`, `/en/consulting/business-consulting`, `/mk/about` — production URLs resolve to `https://vertexconsulting.mk/opengraph-image` via `metadataBase`. Vercel redeploy needed for production crawlers to pick the card up.
- **Shader backgrounds pause when offscreen or tab-hidden + BorderGlow pointer throttled (Session H — 2026-04-19).** Three runtime fixes closing two P1 items from the Session G post-audit. **(1) WebGL viewport + tab-visibility gating.** `src/components/backgrounds/Silk.tsx`, `Plasma.tsx`, `GridMotion.tsx` each install an `IntersectionObserver({ threshold: 0 })` on their root wrapper element plus a `document.visibilitychange` document-level listener; both signals AND-ed through one `apply()` callback that fully halts the render loop when either goes false. Silk flips R3F `<Canvas frameloop>` between `'always'` and `'never'` via a new state variable (wrapping the Canvas in a ref'd `<div>` so IO has something to observe); `frameloop="never"` stops R3F's `useFrame` tick entirely. Plasma's OGL `requestAnimationFrame` loop is gated by `start()`/`stop()` closures guarded with a `raf === 0` sentinel — `cancelAnimationFrame` on stop, fresh `requestAnimationFrame(loop)` on start. Plasma ALSO swapped from `const timeValue = (t - t0) * 0.001` to a locally-accumulated `let simSeconds = 0; simSeconds += dt` where `dt = lastFrame === 0 ? 0 : (t - lastFrame) * 0.001` and `lastFrame` is zeroed on resume — so the shader never skip-jumps forward by however long the tab was hidden. GridMotion attaches/detaches the `updateMotion` callback via `gsap.ticker.add` / `gsap.ticker.remove` with a `tickerAttached` boolean guard; gsap does zero per-frame work for the component when paused, and in-flight `gsap.to` tweens continue or overwrite naturally on resume since gsap manages its own tween timelines independent of the ticker callback. All three initialise with `inView = true` so the above-the-fold hero renders on first frame — `IntersectionObserver.observe()` doesn't fire its callback synchronously, so a `false` initial would cause a one-frame blank. On unmount, the effect cleanup disconnects the observer, removes the visibilitychange listener, and stops/detaches the loop. **(2) BorderGlow pointer handler rAF-throttled.** `src/components/ui/BorderGlow.tsx` `handlePointerMove` (lines 200–216) rewritten as a two-step pipeline. Two new refs: `pendingPointer` holds the latest `{ clientX, clientY }`, `rafIdRef` holds the currently-scheduled rAF id or `null`. `handlePointerMove` now only writes `pendingPointer.current` and, if `rafIdRef.current === null`, schedules `flushPointer` via `requestAnimationFrame`. `flushPointer` clears `rafIdRef.current` first, reads the pending coords + a fresh `card.getBoundingClientRect()` (captures post-scroll/resize position), computes edge + angle, writes the two CSS variables. One commit per animation frame (~60 Hz) regardless of event rate. A cleanup `useEffect` cancels any pending rAF on unmount. Net effect: a 1000 Hz gaming mouse drives ~60 CSS-variable writes per second instead of ~1000 — 16× fewer layout-affecting writes. The pre-existing `animated` sweep `useEffect` (lines 243–264, one-shot mount animation via `animateValue`) is untouched since it writes the same vars directly, not through `handlePointerMove`. **(3) Bundle config** `next.config.ts` gained `experimental: { optimizePackageImports: ['motion', 'lucide-react', 'gsap'] }`. The Turbopack build output now prints `Experiments (use with caution): · optimizePackageImports` confirming the flag is active. Measured bundle delta: **+0.3 KB JS per route** (noise — these three packages' canonical import patterns in this codebase already tree-shake effectively under Next 16's default config: `motion/react` is a subpath, lucide uses named imports, gsap uses a root import). Retained as reversible future-proofing for barrel imports added later. **Measured runtime improvement:** marketing-mobile Lighthouse TBT dropped from **168,102 ms → 530 ms (-99.7%)**, perf score 0.38 → 0.82 (+44 pp), LCP 6.09 s → 2.85 s, on the sample where Lighthouse's measurement window caught the gate engaging (scroll past the Plasma hero during the CLS-observation phase). Other routes' Lighthouse deltas sit inside the observed variance (±0.3 perf swing on this Windows runtime) — Lighthouse's 45 s trace typically keeps heroes above the fold in most sample configurations, so the pause doesn't engage during the measurement. The real long-tail wins — battery/CPU when the user scrolls past the hero, tab-hidden backgrounded sessions, and long reading sessions on low-end mobile — aren't captured by Lighthouse but are directly derived from the same mechanism the marketing-mobile sample exercised. `.claude/launch.json` gained a `vertex-prod` entry (`next start -p 3001`, `autoPort: true`) so production-build perf measurement can run alongside the `vertex-dev` dev server.
- **Static sections now stream from the server, 9 sections + 4 page wrappers converted (Session H Part 3 — 2026-04-19).** Follow-up refactor after parts 1–2 measurement showed the first-load JS was dominated not by three.js / ogl / gsap (already in async chunks) but by the framework + Motion + shared app code + 9 section components that were being hydrated unnecessarily. New client wrapper `src/components/global/StaggerItem.tsx` (`motion.<as>` with the shared `staggerItem` variant by default; `as` of `'div'|'li'|'article'|'section'|'span'`) lets server-rendered sections compose stagger animations without importing motion directly. Nine sections moved to server components: **ValuesGrid** (async, `getTranslations('sections.values')`, StaggerContainer + StaggerItem + lucide Handshake/Target/Zap/Shield icons), **CompanyTimeline** (async, `getTranslations('sections.timeline.milestones')`, 4-milestone vertical timeline with StaggerItem dots), **ProcessSteps** (`as="ol"` + `as="li"` variants on StaggerContainer/StaggerItem so the numbered-step semantics are preserved, prop-driven), **LeaderIntro** (async, `getTranslations('sections.leader')`, AnimateIn wrapper replaces the single motion.div whileInView), **TeamShowcase** (prop-driven, StaggerContainer + StaggerItem + BorderGlow, Session I's `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` ladder preserved), **TeamGrid** (async, `getTranslations('sections.team')`, BorderGlow children), **ConsultingServicesGrid** (prop-driven, `staggerContainerSlow` variant, next-intl `Link` works in server components), **MarketingServicesGrid** (prop-driven, `staggerContainerFast`), **ServicesOverview** (async, `getTranslations('home.servicesOverview')`, 8-card grid for all services). Four page-level wrappers converted from client to server: **HomePage** `src/app/[locale]/(site)/page.tsx` (was `'use client'`, now async; locale flows via the parent `[locale]/layout.tsx`'s `setRequestLocale(locale)` call so no params needed), **AboutPageClient** (async, `getTranslations('about')`, `t.raw('hero.paragraphs')` still works under server context), **ConsultingLandingClient** (async, two `getTranslations` calls on `consulting.landing` + `nav.dropdown`; BackgroundGrid — client — renders inside HeroSection — client — via props), **MarketingLandingClient** (async, same pattern with BackgroundPlasma). Filenames with the `Client` suffix kept as-is to minimise import churn across service-page templates — the suffix is now a naming carryover. Sections that genuinely need client-side reactivity (HeroSection's immediate `animate="visible"` on mount, DivisionSplit's hover state, SocialProof's IntersectionObserver + counter, FAQAccordion's open state, ContactForm, BlogCard's `whileHover`, CTABanner's three custom-delay AnimateIn-equivalent motions) remain client components. **Bundle delta: first-load JS dropped from 911–912 KB → 887–896 KB (−19 KB per main route, 12 chunks → 11) — ~2.1% reduction on all four route families**. Modest KB-wise but architecturally correct: the sections now stream as HTML instead of shipping as JS to hydrate, which reduces main-thread work at TTI in a way that the first-load-JS number alone doesn't reflect. Verified at runtime across `/en` (homepage h1 + Silk canvas + 4 h2s), `/en/about` (4 values cards + 4 timeline items + 4 team cards all rendering), `/en/consulting` (4 services + leader section), `/en/marketing` (4 services + 3 team cards), and `/mk/consulting/business-consulting` (4 process steps rendering in MK — "Првичен разговор" etc., confirming server-side `getTranslations` picks up the locale correctly via `setRequestLocale` in the locale layout). No new console errors; only the pre-existing THREE.Clock deprecation warnings from the Silk shader (present since Phase 3). `npm run build` passes cleanly (47 static pages, zero TypeScript errors, zero warnings).
- **Accessibility hardening — disclosure, forms, skip-link, document language, inert mobile menu (Session G — 2026-04-19).** Eight audit findings closed in one pass. (1) **`<html lang={locale}>` lives on the locale layout's HTML shell.** The root `src/app/layout.tsx` is now a minimal pass-through that returns `children` and keeps only `metadata` + `viewport` exports; `src/app/[locale]/layout.tsx` owns the `<html lang={locale} className={manrope.variable} ${onest.variable}}><body>` shell, font loading, `NextIntlClientProvider`, `MotionWrapper` / `DivisionProvider` / `ScrollProgress` / `BackToTop`. The old `<div lang={locale} data-locale={locale}>` wrapper div is gone — the `lang` attribute is now on `<html>` where WCAG 3.1.1 expects it. Verified at runtime: `document.documentElement.lang === 'en'` on `/en/*`, `=== 'mk'` on `/mk/*`. This is the standard next-intl App Router pattern; `/api/*` routes still bypass the layout tree entirely (they return `Response` objects, never render HTML). (2) **Navbar desktop dropdown is a W3C disclosure.** The old hover-only dropdown (`<Link>` with `onMouseEnter/Leave` on the wrapper) is now a hybrid: a dedicated `<button type="button" aria-haspopup="menu" aria-expanded={isOpen} aria-controls={menuId} aria-label={t('submenuToggleAria', { label })}>` sits next to the parent `<Link>`. Click/Enter/Space on the button toggles `activeDropdown` state; Escape closes and returns focus to the trigger via `disclosureRefs`; `onBlur` on the wrapper closes when focus leaves; mouse-hover still opens but `onMouseLeave` checks `e.currentTarget.contains(document.activeElement)` and keeps the menu open if a keyboard user is still inside it. Child links render inside the disclosed `motion.div` with the matching `id` and are plain links (no `role="menuitem"`) so Tab traverses them. The `layoutId="nav-underline"` active-state underline moved from inside the Link to the wrapper so it spans both Link and button. The old single-`ChevronDown`-inside-Link became the new button's icon with `aria-hidden="true"`. (3) **Form inputs carry error associations.** `ContactForm` refactored from a single submit-level error banner to field-level `fieldErrors: Partial<Record<'name'|'email'|'message', string>>` state; each required field gets `aria-invalid={!!fieldErrors.X}` + `aria-describedby={fieldErrors.X ? errorId : undefined}`, and the inline error `<p id={errorId} role="alert">` renders below the field. Field errors clear on change. Submit still renders the API error banner (also `role="alert"`) and the submit button references it via `aria-describedby`. On validation failure, focus moves to the first invalid field so screen readers announce the error. Footer newsletter input got the same treatment — `aria-invalid={newsletterStatus === 'error'}` + `aria-describedby` pointing to the error `<p id={newsletterErrorId} role="alert">`; error auto-clears when the user edits the field. Form inputs all now use `useId()` for unique, stable IDs so multiple forms on the same page don't collide. (4) **FAQAccordion triggers control their panels.** Each trigger gains `id={triggerId}` + `aria-controls={panelId}`; the disclosure panel `<div>` gains matching `id={panelId}` + `role="region"` + `aria-labelledby={triggerId}`. The existing `grid-template-rows: 0fr↔1fr` CSS transition from Session D is untouched — only the ARIA attributes were layered on. ChevronDown SVG (and its wrapping `motion.span`) carry `aria-hidden="true"`. (5) **Mobile menu manages inert + focus trap.** New `useEffect` watches `mobileOpen`: when true, applies `inert={true}` imperatively to `document.querySelector('main')` and `document.querySelector('footer')` (React 19 supports `inert` as a boolean attribute, but we set the HTMLElement property so we can toggle sibling elements outside the Navbar component); when false, clears `inert` on both and — if the transition was true→false — calls `focus()` on the hamburger button so the user doesn't end up focused on a now-inert background element. A second `useEffect` binds a document-level Escape listener while the menu is open. `aria-controls` on the hamburger points to the overlay `id`. (6) **Skip-to-content link.** `src/app/[locale]/(site)/layout.tsx` now rendered as `async` server component using `getTranslations('common')`; renders a `<a href="#main-content">` before the `<Navbar />` with `position: fixed`, `-translate-y-[200%]` at rest, `focus-visible:translate-y-0` to slide in on keyboard focus. Colors driven by `var(--division-accent)` / `var(--division-bg)` / `var(--division-bg)` outline — no hardcoded hex. `<main>` gained `id="main-content"` so the anchor target resolves. (7) **Decorative chevrons / icons all aria-hidden.** FAQAccordion chevron (+ motion.span wrapper), Navbar new disclosure chevron, Navbar Menu/X hamburger icons, Navbar Globe icons in both desktop + mobile language toggles, ContactForm Loader2 + Check icons all carry `aria-hidden="true"`. (8) **Submit buttons expose aria-busy.** ContactForm `<Button type="submit" aria-busy={status === 'submitting'}>` and Footer newsletter `<Button type="submit" aria-busy={newsletterStatus === 'submitting'}>` both toggle `aria-busy` alongside `disabled`. Assistive tech announces "busy" during the fetch. Two new translation keys — `common.skipToContent` ("Skip to content" / "Прескокни до содржина") + `nav.submenuToggleAria` ("Toggle {label} submenu" / "Отвори/затвори подмени за {label}") — added to both locale dictionaries.
- **Brand fonts load with full Cyrillic coverage on both locales (Session F — 2026-04-17).** `src/app/layout.tsx` now loads **Manrope** (headings, weights 400/500/600/700/800) and **Onest** (body, weights 400/500/600/700) via `next/font/google`, both with `subsets: ['latin', 'latin-ext', 'cyrillic', 'cyrillic-ext']`. Before Session F, Sora + DM Sans were loading `subsets: ['latin']` only — Macedonian content on `/mk` fell back to the OS default sans (Segoe UI on Windows, SF Pro on macOS). Post-swap: `getComputedStyle(h1).fontFamily` = `Manrope, "Manrope Fallback", system-ui, sans-serif`; `getComputedStyle(p).fontFamily` = `Onest, "Onest Fallback", system-ui, sans-serif` on both `/en/...` and `/mk/...` routes. The `U+400-45F` Cyrillic range + `U+460-52F` Cyrillic-ext range are explicitly loaded for both faces (verified via `document.fonts`). Also migrated from DM Sans, which is on the impeccable skill's reflex-default typography reject list.
- **Font CSS variables renamed to semantic names (Session F — 2026-04-17).** The next/font-injected variables were renamed `--font-sora` → `--font-heading` and `--font-dm-sans` → `--font-body`. Because those names now match the Tailwind-v4 token names declared in `@theme`, the font tokens were relocated from the standard `@theme { --font-heading: var(--font-sora), ...; }` pattern to a dedicated `@theme inline { --font-heading: var(--font-heading), system-ui, sans-serif; ... }` block at the top of `globals.css` — the `inline` keyword tells Tailwind v4 to generate `.font-heading { font-family: var(--font-heading), system-ui, sans-serif; }` directly without an intermediate `:root` emission, so next/font's class-based CSS variable injection on `<html>` remains the sole source of truth and the `system-ui, sans-serif` tail is preserved in the utility itself. The 6 hand-written `.prose-consulting` / `.prose-marketing` / `.prose-blog` h2+h3 rules that previously referenced `var(--font-sora)` directly were rewritten to reference `var(--font-heading)`; same for the `fontFamily` inline style in `GridMotion.tsx`. All `font-heading` / `font-body` Tailwind utility class usages across 30+ component files keep working unchanged — the Tailwind utility names were preserved.
- **All component-level color bypasses the token system has been eliminated (Session E — 2026-04-17).** Every hardcoded hex in `src/components/sections/*` + `src/app/[locale]/(site)/marketing/MarketingLandingClient.tsx` now resolves through `var(--division-*)` or `var(--color-*)`. Five direct swaps: `MarketingServicePage.tsx` overlines (`#A3A3A3` × 3 → `var(--division-text-muted)`), `CTABanner.tsx` background (`#0E0E0E` → `var(--color-ink)` — exact token match), `ServicesOverview.tsx` + `TeamGrid.tsx` indicator dots (`#A3A3A3` → `var(--division-text-muted)`), plus a sweep that caught two more matching sites: `BlogCard.tsx` `divisionColor` constant and `MarketingLandingClient.tsx` two service/team overlines. `DivisionSplit.tsx` was refactored via **Option A** — `accentColor` / `bgColor` per-division fields deleted entirely from the data array; all solid-color usages now reference `var(--division-accent)` / `var(--division-bg)` / `var(--division-card)` directly, and the `${X}NN` alpha-suffix pattern (which CSS vars can't satisfy syntactically) was converted to explicit `rgba(245,245,245,α)` values. Only exceptions remaining: `BorderGlow colors={[...]}` props (the pointer-follow gradient math needs concrete hex), WebGL shader props on `BackgroundSilk`/`Plasma`/`Grid`, `themeColor` in layout metadata, inline HTML emails in `/api/*` routes, and the canonical division-to-Tailwind mapping in `src/lib/divisions.ts` (that file IS part of the token-bridge layer). Token definitions themselves in `globals.css` are unchanged.
- **Legacy `.gradient-text` and `.gradient-text-brand` CSS classes removed (Session E — 2026-04-17).** Both classes had been flattened to solid `#F5F5F5` during the Finishing-Touches-v2 grayscale refactor but their names still implied a gradient, making them a future-maintenance trap. The 4 CSS rules (`.gradient-text`, the two `[data-division="..."] .gradient-text` blocks, and `.gradient-text-brand`) were deleted from `globals.css`. Three live usage sites migrated: `MarketingLandingClient.tsx` (`headlineClassName="gradient-text"` prop removed entirely — HeroSection's default color is correct now), `SocialProof.tsx` (`gradient-text-brand` class replaced with inline `style={{ color: 'var(--division-text-primary)' }}`), `HeroSection.tsx` default `headlineClassName='gradient-text-brand'` dropped and color routed through inline `style`. The stale tailwind-merge warning comment in `ConsultingLandingClient.tsx` (7 lines explaining why passing `text-[var(--division-text-primary)]` as `headlineClassName` broke `text-display`) was deleted — the inline-style approach makes it moot.
- **HeroSection hero-headline color via inline style (Session E — 2026-04-17).** `<motion.h1>` now carries `style={{ color: 'var(--division-text-primary)' }}` directly on the element. The className stays `cn('font-heading text-display', headlineClassName)` (with `headlineClassName` optional, no default) — but because color is set via `style` not via a utility class, it cannot be stripped by tailwind-merge even if a future caller passes an arbitrary-value color class. Verified at runtime: all 3 landing hero h1s render at the correct fluid `text-display` size (40px at the test viewport) with `rgb(245, 245, 245)` computed color on the shared, consulting, and marketing pages.
- **BorderGlow card wrapper has no static 1px border (Session E — 2026-04-17).** `.border-glow-card` no longer declares `border: 1px solid rgb(255 255 255 / 15%)`. Every BorderGlow card on the site (homepage ServicesOverview 8-card grid, ConsultingServicesGrid 2×2, MarketingServicesGrid 2×2, TeamGrid 4-card, TeamShowcase 3-card, BlogCard grid) now shows a borderless idle state — just the card background fill. On hover, the existing `::before` + `::after` pseudo-elements plus the `.edge-light` child element still render the pointer-follow colored-mesh glow and radial outer glow exactly as before; hover behavior is unchanged. Confirmed at runtime: `getComputedStyle(cardEl).borderWidth === '0px'` across all tested cards.
- **FAQ expand/collapse without layout thrash (Session D — 2026-04-17).** `FAQAccordion.tsx` switched from Motion `height: 0 ↔ 'auto'` (which forces a browser layout recalc every frame of the animation) to the modern pure-CSS `grid-template-rows: 0fr ↔ 1fr` technique. Outer wrapper: `grid transition-[grid-template-rows,opacity] duration-300 ease-out` swapping `grid-rows-[1fr] opacity-100` vs `grid-rows-[0fr] opacity-0`, plus `aria-hidden={!isOpen}` for screen readers. Inner wrapper: `overflow-hidden` to clip content during the transition. Content stays mounted at all times — no `AnimatePresence`, no unmount cycle. The animation is compositor-driven; animation budget stays under 2ms per frame even under 4× CPU throttle. Browser support (Chrome 117+ / Firefox 124+ / Safari 17.4+) is universal across the target audience. `prefers-reduced-motion` is respected automatically via the existing global `transition-duration: 0.01ms !important` rule in `globals.css`. The Button primitive usage from Session C is preserved — only the panel-below-the-button animation changed.
- **Accessibility — keyboard, forms, touch targets (Session C — 2026-04-17).** Every interactive element across the site now shows a visible focus ring on keyboard navigation: bright-white (`var(--division-accent)` → `#F5F5F5` post-grayscale refactor), 2px width, 2px offset. Two new utility classes drive this — `.focus-ring` (for `<Link>` and non-button interactive elements; uses `:focus-visible` so mouse users see no ring) and `.form-input-focus` (replaces the 7× `focus:outline-none focus:border-[var(--division-accent)]` on form fields with a strong outline-plus-border treatment that meets WCAG 1.4.11 3:1 non-text contrast). The shadcn `<Button>` primitive (`src/components/ui/button.tsx`) is now adopted sitewide — adopted in CTABanner, ContactForm submit, Footer (newsletter submit + back-to-top text button), Navbar (hamburger + desktop language toggle + mobile-overlay language toggle), BackToTop FAB, FAQAccordion triggers, and BlogListingClient filter pills. Two new CVA size variants added to `button.tsx`: `cta` (h-12 px-6 font-heading rounded-button — landing-page CTAs) and `pill` (h-11 px-4 rounded-full — filter pills with built-in 44px touch floor). Mobile touch targets meet the 44×44px Apple HIG floor: navbar nav-links use `min-h-[44px] md:min-h-0 md:py-2` (mobile-only floor, desktop stays compact at 36px); navbar hamburger is `h-11 w-11`; footer social icons are `min-h-[44px] min-w-[44px] p-3`; back-to-top FAB is `h-11 w-11`; blog filter pills inherit 44px from the `pill` size; footer column nav-links use the same mobile-only floor as navbar nav-links. Footer logo `aria-label` now matches Navbar pattern (`{siteConfig.name} {tCommon('logoAriaSuffix')}`); the `logoAriaSuffix` key was moved from `nav.*` to `common.*` so both components reference one source of truth ("home" / "почетна").
- **Bilingual routing (Phase 15A — 2026-04-17).** Every site route is served under `/en/...` and `/mk/...`; `/` 307-redirects to the preferred locale based on `Accept-Language` (default `en`). `next-intl@4.9.1` is fully configured — `src/i18n/routing.ts` declares the locales (`en`, `mk`), `src/i18n/request.ts` loads `messages/${locale}.json`, `src/i18n/navigation.ts` exports locale-aware `Link` / `usePathname` / `useRouter`. The edge handler lives at `src/proxy.ts` (Next 16 renamed `middleware` → `proxy`). The site layout tree was rehomed from `src/app/(site)/` to `src/app/[locale]/(site)/`. The navbar language toggle is real — clicking it flips between `/en/<path>` and `/mk/<path>` preserving the current path; the mobile overlay toggle does the same. `generatePageMetadata` emits `<link rel="alternate" hreflang>` for `en`, `mk`, and `x-default`.
- **Global chrome + homepage translations (Phase 15B — 2026-04-17).** Navbar, Footer, BackToTop, BlogCard, ValuesGrid, CompanyTimeline, TeamGrid division chips, LeaderIntro overline, DivisionSplit, ServicesOverview, SocialProof, CTABanner defaults, and the homepage `page.tsx` all read strings via `useTranslations`. `messages/en.json` + `messages/mk.json` carry `common.*`, full `nav.*` (including `nav.dropdown.*`), full `footer.*`, full `home.*`, and `sections.*` namespaces. `src/config/navigation.ts` refactored to Option A: `NavItem.labelKey` is a fully-qualified message key (e.g. `nav.consulting`, `nav.dropdown.webDesign`, `footer.company.privacy`); the consulting/marketing footer columns reuse the navbar dropdown keys so the same service names show everywhere. `BlogCard` dates use next-intl's `useFormatter` to avoid hydration mismatches (dates currently render in English format on `/mk` due to Node ICU limits — flagged in `TRANSLATION_NOTES.md`). Marketing division pages, About story + team bios, Contact body, and blog post content still render English on `/mk` — those are 15D / 15E / 15F.
- **Consulting division translations (Phase 15C — 2026-04-17).** Consulting landing + all 4 service pages fully Macedonian on `/mk/consulting/*`. `ConsultingServicePage.tsx` rewritten as a server component that takes `content: ContentSection[]` (structured — no more JSX-as-prop), mapping over each section's `heading` / `paragraphs` / optional `bullets` / optional post-bullet paragraphs. `Bullet.term?` supports the AI Consulting `<strong>Document processing</strong> — automating…` pattern. `ConsultingServicesGrid.tsx` refactored to prop-driven. Each consulting service page is a server component: `generateMetadata` awaits `params`, calls `getTranslations`, passes `locale` to `consultingMetadata`; default export calls `setRequestLocale(locale)` then feeds the template. FAQPage JSON-LD now emits Macedonian on `/mk/consulting/*`. `messages/*.json` expanded with the `consulting.*` namespace (~3,500 words MK prose). `src/types/index.ts` now exports `ContentSection`, `Bullet`, `ProcessStep`, `FAQItem`, `RelatedServiceLink` — reused by 15D's marketing pages.
- **Marketing division translations (Phase 15D — 2026-04-17).** Marketing landing + all 4 service pages fully Macedonian on `/mk/marketing/*`. `MarketingServicePage.tsx`, `MarketingServicesGrid.tsx`, and `TeamShowcase.tsx` all refactored to mirror 15C's consulting architecture — server-component template for the service pages, prop-driven grid + team showcase. Each service page.tsx is async server-component with `getTranslations`. `messages/*.json` expanded with the `marketing.*` namespace (~3,000 words MK prose). **New helper `src/lib/renderInlineMarkdown.tsx`** — parses `**text**` markers in paragraph strings into `<strong>` elements, used inside the marketing service-page template so translation paragraphs can carry emphasis like `"We use **Next.js**"` or `"what the industry calls **vibe coding**"`. Team showcase (Lazar / Petar / Andrej) now reads member name / role / bio / initials from translations — names stay Latin on both locales, roles + bios translate. Cross-division related-services links verified in both directions on both locales (`/mk/marketing/ai-development` ↔ `/mk/consulting/ai-consulting`, `/mk/marketing/it-infrastructure` ↔ `/mk/consulting/it-systems`).
- **Blog post content + final SEO polish (Phase 15F — 2026-04-17).** All 3 blog posts fully translated to Macedonian — title, excerpt, body (~1000 words each), authorRole, tags. `src/lib/blog.ts` refactored from a single `mockPosts: BlogPost[]` to `Record<string, Record<Locale, BlogPost>>` keyed by slug; every helper (`getAllPosts`, `getPostBySlug`, `getPostsByDivision`, `getRelatedPosts`) now takes a `locale` parameter. New `getAllSlugs()` helper. Blog `generateStaticParams` now emits `{ locale, slug }` pairs → 6 prerendered post pages. `BlogPostClient` receives `post: BlogPost` as a prop (was slug). BlogPosting JSON-LD now includes `inLanguage: "mk-MK" | "en-US"`, `dateModified`, `author.jobTitle` (translated role), and `keywords` (translated tag list). FAQPage JSON-LD (service pages) also emits `inLanguage`. `src/app/sitemap.ts` + `src/app/robots.ts` created — sitemap emits both locales per static + blog path with `xhtml:link` hreflang alternates (en / mk / x-default); robots disallows `/en/privacy`, `/mk/privacy`, `/en/thank-you`, `/mk/thank-you`, `/api/`. `src/app/[locale]/not-found.tsx` created with new `notFound.*` namespace. Root `/` redirect verified to honor `Accept-Language` (curl confirmed en / mk / no-header fallbacks). **13 translation namespaces total in `messages/*.json` — ~10,000 words of MK prose across the site.** Phase 15 overall complete.
- **Shared pages translations (Phase 15E — 2026-04-17).** About, Contact, Thank-you, Blog listing, and Blog post template chrome fully Macedonian on `/mk/*`. `messages/*.json` carries `about.*` (meta, hero, values, team × 4 members, timeline, ctaBanner), `contact.*` (meta, hero, form with all labels + placeholders + 4 validation errors + 4 select options + submit states + success card + privacy notice, info panel with all labels + hoursValue, map.iframeTitle), `thankYou.*` (new CTA added), `blog.*` (listing hero + 4 filter pills + empty state, post chrome: backLink + readTimeLong + relatedHeading). `TeamGrid.tsx` refactored to prop-driven (matching 15D's TeamShowcase). `ContactForm.tsx` every string translated; MK validation errors triggered and verified. The **blog post inline-Markdown renderer** was rebuilt to emit React elements using locale-aware `Link` for internal `/...` hrefs instead of raw `<a>` via `dangerouslySetInnerHTML` — inline body links on `/mk/blog/[slug]` now navigate to `/mk/consulting/...` client-side, preserving locale. **Privacy policy body stays English on both locales** (Session A produced a 5,000-word GDPR-grade policy — translating without counsel review would be irresponsible; `/mk/privacy` shows a conditional notice banner explaining). `locale` is now threaded through `/blog/[slug]` metadata so canonical URLs and hreflangs match the visited locale. BlogCard + BlogPostClient dates use next-intl's `useFormatter` for hydration-safe locale formatting (falls back to EN format on MK due to Node ICU limits — flagged). **Total MK site content so far: ~7,300 words of prose.** Translation judgment calls flagged in `TRANSLATION_NOTES.md` (items #24–28).
- **Root layout + theming pipeline.** As of Session G (2026-04-19), the root `src/app/layout.tsx` is a minimal pass-through that returns `children` and only exports `metadata` + `viewport`. The `<html lang={locale}>` + `<body>` shell, Manrope/Onest font loading (with `cyrillic` + `cyrillic-ext` subsets — Session F 2026-04-17), `NextIntlClientProvider`, `MotionWrapper` with `reducedMotion="user"`, `DivisionProvider` setting `data-division` based on path, `ScrollProgress` bar, and `BackToTop` all live inside `src/app/[locale]/layout.tsx`. `/api/*` routes bypass the layout tree entirely — they return `Response` objects and never render HTML, so they don't pay for any of the above.
- **Division theming.** Navigating to `/consulting*` swaps to the monochrome palette; `/marketing*` swaps to lavender; everything else is the shared brand-blue palette. Both shadcn HSL vars and custom `--division-*` vars flip together.
- **Homepage (`/`).** Full production homepage with 5 sections: (1) **Hero** — Silk WebGL background, gradient headline, dual CTAs ("Explore Consulting" / "Explore Marketing"). (2) **Division Split** — two interactive cards previewing Consulting and Marketing themes on hover. (3) **Services Overview** — 8-card grid covering all services from both divisions, with division indicator dots and per-division hover glows. (4) **Social Proof** — 4 animated stat counters (IntersectionObserver-based, count up when in view) + founder quote. (5) **CTA Banner** — full-width gradient section with "Get in Touch" button linking to `/contact`.
- **Consulting landing (`/consulting`).** Full production page: `HeroSection` with `BackgroundGrid` child (GSAP parallax of 28 consulting words), plain-white headline (no gradient), dual CTAs (`#services` / `#leader`); `ConsultingServicesGrid` 2×2; `LeaderIntro` for Goran Dinov on a surface-tinted section; `CTABanner` at the bottom. Page is split into `page.tsx` (server component holding metadata) and `ConsultingLandingClient.tsx` (client component holding the UI). Full metadata via `generatePageMetadata`.
- **Consulting service pages (all 4).** `/consulting/business-consulting`, `/consulting/workflow-restructuring`, `/consulting/it-systems`, `/consulting/ai-consulting` are each a server component exporting metadata (via `consultingMetadata`) that renders the shared `<ConsultingServicePage>` template with page-specific `title`, `subtitle`, long-form `content` JSX (800+ words, h2/h3/p/ul/strong), `processSteps`, `faqItems`, and `relatedServices`. Each page emits a `FAQPage` JSON-LD `<script>` via `FAQAccordion`. Content is draft quality and should be reviewed with the client.
- **Marketing landing (`/marketing`).** Full production page: server component exporting metadata → `MarketingLandingClient` renders `HeroSection` with `BackgroundPlasma` child and `gradient-text` headline (purple `#E0BBFF → #9474D4`), dual CTAs (`#services` / `#team`); `MarketingServicesGrid` 2×2 with purple glow hover; `TeamShowcase` with Lazar/Petar/Andrej initials cards on a surface-tinted section; `CTABanner` ("Let's build something great.") at the bottom.
- **Marketing service pages (all 4).** `/marketing/web-design`, `/marketing/social-media`, `/marketing/it-infrastructure`, `/marketing/ai-development` are each a server component exporting metadata (via `marketingMetadata`) that renders the shared `<MarketingServicePage>` template with page-specific `title`, `subtitle`, long-form `content` JSX (web-design 820 words / ai-development 814 words), `processSteps`, `faqItems`, and `relatedServices`. Each emits a `FAQPage` JSON-LD `<script>` via `FAQAccordion`. IT Infrastructure cross-links to `/consulting/it-systems`; AI Development cross-links to `/consulting/ai-consulting`. Content is draft quality and should be reviewed with the client.
- **About page (`/about`).** Full production page on the shared (brand-blue) theme. Server component exports metadata → `AboutPageClient` renders four `<Section>`s: (1) hero/story with overline, h1, and 3-paragraph company narrative; (2) `ValuesGrid` (4 values with Handshake/Target/Zap/Shield lucide icons) on a surface-tinted section; (3) `TeamGrid` showing all 4 team members (Goran with gray consulting indicator, Lazar/Petar/Andrej with `#B490F0` marketing indicators); (4) `CompanyTimeline` with 4 milestones (2018, 2020, 2023, 2026) on surface-tinted section. Closes with a custom `CTABanner` ("Let's build something together." → `/contact`).
- **Contact page (`/contact`).** Server component → `ContactPageClient` renders three sections: (1) header with h1 "Let's talk about your business." + lede paragraph; (2) 5-column grid — 3-col `<ContactForm>` and 2-col info panel (MapPin/Phone/Mail/Clock rows reading from `siteConfig`, plus a division-emails block with both `info@` and `marketing@` addresses); (3) embedded Google Maps iframe with CSS filter to match the dark theme. No CTA banner (the form *is* the CTA).
- **Contact form (`<ContactForm>`).** Client component with full client-side validation (name, email regex, 10-char message minimum). 5 visible inputs: name, email, phone (optional), division select (`consulting` / `marketing` / `both` / ""), message textarea, plus a hidden off-screen honeypot `website` input for bot filtering. Submit POSTs to `/api/contact` → Resend sends an HTML+text email to `vertexcons1@gmail.com` with `replyTo` set to the user's address, then the form flips `status` to `success` and is replaced with a confirmation card. Error state renders via red `text-red-400 bg-red-500/10` banner. Submit button pulls `var(--division-accent)` + `var(--division-bg)` so it adapts per-division — on shared pages it's brand-blue on dark-blue-black. (Session B — 2026-04-17.)
- **Blog listing page (`/blog`).** Server component → `BlogListingClient`. Header with h1 "Insights from the field." + lede, 4 filter pills (`All posts` / `Consulting` / `Marketing` / `General`), then a responsive 3-col/2-col/1-col `BlogCard` grid. Active filter pill shows as division-accent bg; inactive pills are bordered. Empty-state card rendered if no posts match. Keyed `<motion.div>` on filter re-triggers stagger when filter changes. Closes with default `CTABanner`.
- **Blog post pages (`/blog/[slug]`).** Dynamic route with `generateStaticParams` producing 3 prerendered SSG pages (one per mock post). `generateMetadata` awaits `params` (Next 16 async) and builds per-post metadata; 404 fallback exported with `noIndex`. Client component renders: Back link, header (division label + date + read-time, h1, excerpt, author-initials avatar + role), content pane using `.prose-blog` with simple Markdown-style renderer (supports `## h2`, `**bold**`, `[link](href)` via `dangerouslySetInnerHTML`), optional related-posts grid (up to 2 same-division posts), and default `CTABanner`. Emits `BlogPosting` JSON-LD via inline `<script type="application/ld+json">`.
- **Mock blog data (`src/lib/blog.ts`).** 3 fully-written posts (consulting / marketing / shared). Public API: `BlogPost` interface, `mockPosts` array, `getAllPosts()` (date-descending), `getPostBySlug(slug)`, `getPostsByDivision(division)`, `getRelatedPosts(slug, limit=2)`. Shapes are designed to survive the Phase-13 Sanity migration — only the data source changes, not the signatures.
- **Privacy page (`/privacy`).** Full 13-section privacy policy (EN) approved by Goran. Renders on the shared theme with `prose-blog` typography, narrower `max-w-3xl` reading width, third-party services rendered as a bordered `<table>` (Vercel/Cloudflare/Resend/Google Maps), `<hr>` separators between sections, italicized disclaimer at the bottom. Metadata description updated; `noIndex: true` retained.
- **Thank-you page (`/thank-you`)** — minimal but real, `noIndex`.
- **Footer social icons** in the bottom bar link to real Vertex profiles for Instagram (`instagram.com/vertxsystems.mk`) and Facebook (`facebook.com/share/1CEaD21Asq/`). LinkedIn still points to `linkedin.com` because the account does not exist yet. URLs now read from `siteConfig.social` instead of being hardcoded in the component.
- **Navbar.** Fully implemented: scroll-to-hide, `glass` background after scroll, desktop dropdowns for Consulting/Marketing with motion fade + chevron rotate, animated `layoutId` underline for active route, language toggle (UI only, no logic), primary CTA, hamburger → full-screen mobile overlay with staggered links, body-scroll lock. **Mobile overlay is now scrollable with top-aligned content (fix 2026-04-19)** — inner wrapper switched from `flex flex-col items-center justify-center h-full gap-6 px-6` to `flex flex-col items-center gap-5 h-full overflow-y-auto pt-24 pb-10 px-6`. Prevents clipping when the combined nav list (Consulting + 4 sub-links + Marketing + 4 sub-links + About + Blog + Contact + CTA + language toggle) exceeds viewport height on iPhone-sized screens; `pt-24` clears the 56px fixed header on mobile (`h-14`), `pb-10` keeps the language toggle off the screen edge.
- **Footer.** Fully implemented: gradient accent line at top, newsletter CTA strip (now functional — stateful form that POSTs to `/api/newsletter`, adds the address to the Resend `Vertex Newsletter` audience, sends a welcome email, and replaces the form with a "You're in" success message), 4-column grid (logo + tagline + address/phone/email, consulting services, marketing services, company links), bottom bar with social icons, dynamic-year copyright, and smooth-scroll back-to-top button. Includes a hidden off-screen honeypot `newsletter-website` input for bot filtering. Division-aware via CSS vars; columns stagger-animate on scroll into view (`whileInView` with `once: true`). Responsive: 4 cols desktop → 2 cols tablet → 1 col mobile. Three brand icons (LinkedIn/Instagram/Facebook) are inline SVGs because `lucide-react@1.8.0` ships no brand marks. **Mobile-specific polish applied 2026-04-19:** logo `<span>` bumped to `text-2xl` on mobile (preserved as `text-h3` on `sm:+` to honor the desktop-untouched rule) and gained `tracking-tight`; tagline `<p>` `mt-3 → mt-2` and `max-w-xs` so it doesn't stretch full-width when the column collapses; contact-info wrapper added `mb-4 sm:mb-0` so the logo/contact block reads as a discrete unit before the service columns; grid gap tightened from a flat `gap-10 lg:gap-8` to `gap-8 sm:gap-10 lg:gap-8` (40px → 32px between stacked columns on mobile only; tablet/desktop unchanged).
- **Section components (Phase 8 + Phase 9 + Phase 10 + Phase 11).** Eighteen reusable components under `src/components/sections/`: Phase 8 set — `HeroSection`, `DivisionSplit` (homepage-only), `ServicesOverview` (all 8 services), `SocialProof`, `CTABanner`. Phase 9 additions — `ProcessSteps` (numbered timeline, any division), `FAQAccordion` (expandable items + inline FAQPage JSON-LD), `ConsultingServicesGrid` (consulting-only 2×2 monochrome grid), `LeaderIntro` (founder bio with initials avatar), and `ConsultingServicePage` (template that composes hero, prose, process, FAQ, related services, and CTA for service subpages). Phase 10 additions — `MarketingServicesGrid` (marketing-only 2×2 purple grid with lavender glow hover), `TeamShowcase` (Lazar/Petar/Andrej cards with initials avatars), and `MarketingServicePage` (marketing variant of the service-page template with `#B490F0` overlines). Phase 11 additions — `ContactForm` (validated 5-field form, placeholder submit), `TeamGrid` (About-page 4-card grid with per-member division indicators), `CompanyTimeline` (vertical dotted timeline of company history), `ValuesGrid` (4-card grid with lucide icons), `BlogCard` (listing tile with division dot, date, read-time, author, read-arrow). All exported from `src/components/sections/index.ts`.
- **Animation system.** Reusable variants + three wrapper components (`AnimateIn`, `StaggerContainer`, plus hero/hover variants used directly). Respects `prefers-reduced-motion` in both CSS and JS.
- **Reduced-motion fallback** on all three backgrounds renders a flat `--division-bg` div.

## What is placeholder / incomplete
- **Light mode is correct at the frame + chrome + hero level (L1–L4 done).** Remaining light-mode wrongness is concentrated inside specific section components that still hold hardcoded dark styling — HeroSection shell (`hover:bg-white/5`), DivisionSplit (`rgba(245,245,245,α)` literals for hover bg / card bg / border), ServicesOverview card icon containers (`bg-white/5 group-hover:bg-white/10`), ConsultingServicesGrid + MarketingServicesGrid card icon containers (same), TeamGrid avatar borders (`border-white/10 bg-white/5`), TeamShowcase avatar borders (`border-white/20 bg-[rgba(255,255,255,0.08)]`), MarketingServicePage related-services pills (`hover:border-white/20 hover:shadow-[0_0_20px_rgba(255,255,255,0.05)]`), ConsultingServicePage related-services pills (`hover:bg-white/5`), FAQAccordion trigger hover (`hover:bg-white/5`), BlogListingClient filter pill borders (`hover:border-white/20`), ContactForm surface treatment, privacy page, thank-you page. Phase L5 sweeps all of these. The pattern is the same as L4's Navbar/Footer work: `hover:bg-white/N` → `hover:bg-[var(--nav-hover-bg)]`, `border-white/N` → a more specific token if semantics diverge.
- **Silk shader (homepage hero) reads heavier than ideal in light mode.** `uColor = #CDD1D7` multiplied by the pattern (0.2–1.0) produces dark silk zones at `pattern=0.2` that paint as ~`#2F3033`, which competes with the subtitle text (`--division-text-secondary: #4B5563`). Headline (bold black) cuts through fine; body copy is compressed. Not regression-blocking — dark mode is byte-identical to pre-phase and light mode is functional. Proposed tunings (`SILK_COLOR_LIGHT` → `#E8EBEE`/`#F0F2F5`, or `noiseIntensity` lowered for light theme) documented in `phase-l3-hero-backgrounds.md` §"Aesthetic concerns to address"; awaiting user direction before applying.
- **Homepage stat counters are placeholder values** — `8+` years, `50+` projects, `2` divisions, `100%` client-first. Confirm real numbers with client.
- **Homepage founder quote is draft copy** ("We don't just advise — we roll up our sleeves..."). Confirm verbatim with Goran.
- **Homepage service card descriptions are draft copy** — will be refined with client input.
- **No rate limiting on either API endpoint.** `/api/contact` and `/api/newsletter` rely on the honeypot alone. If spam becomes a problem, add Cloudflare Turnstile on the forms or edge rate limiting on the routes.
- **Newsletter campaigns are not yet sent.** Subscribers land in the Resend `Vertex Newsletter` audience, but Goran has not built the monthly-campaign automation — custom broadcast logic using the audience as source is a follow-up.
- **LinkedIn social link in Footer** still points to `https://linkedin.com` because the Vertex LinkedIn account has not been created yet. Update `siteConfig.social.linkedin` once the account exists. Instagram and Facebook now point to real profiles.
- **Privacy policy is EN only.** Macedonian translation pending. The route renders under both locales after Phase 15A's folder move, but both show the same English body.
- **All 4 consulting service pages** are complete (Phase 9) — see "What works right now". Content is draft quality and awaits client review.
- **All 4 marketing service pages** are complete (Phase 10) — see "What works right now". Content is draft quality and awaits client review.
- **Marketing team bios and avatars are placeholders.** `TeamShowcase` (on `/marketing`) and `TeamGrid` (on `/about`) both use initials (`L`, `P`, `A`, `GD`) and draft copy for Goran/Lazar/Petar/Andrej. Real photos and copy needed before launch.
- **Blog content is mock data.** `src/lib/blog.ts` ships 3 full-length draft posts. These will be replaced by Sanity CMS queries in Phase 13 using the same function signatures (`getAllPosts`, `getPostBySlug`, etc.).
- **Blog post content renderer is intentionally minimal.** Supports only `## h2`, `**bold**`, and `[text](href)` via regex + `dangerouslySetInnerHTML`. Safe for the mock data we control, but Phase 13 should swap to `@portabletext/react` when Sanity is added.
- **About timeline year "2026"** for the Vertex Marketing launch is aspirational — verify with Goran before launch.
- **AI chat API routes** (`/api/chat`, `/api/chat/lead`) still return `{ ok: true }` stubs with no Claude calls — reserved for Phase 12. `/api/contact` and `/api/newsletter` are now fully implemented with Resend (see above).
- **AI chat widget.** Not mounted. Library stubs (`src/lib/ai.ts`, `src/lib/chatWidget.ts`) exist but are essentially empty.
- **Sanity.** `src/lib/sanity/client.ts` and `queries.ts` are empty `export {}` stubs. `next-sanity` + `@sanity/image-url` are installed.
- **Privacy policy legal body remains English on both locales.** 15E added a MK-only notice banner explaining the translated policy is pending counsel review. Full MK translation will be a separate focused task with a lawyer.
- **Native-speaker MK review pending.** The MK translations were LLM-drafted (proficient but not native). Goran should commission a ~30-min native-speaker review pass before launch — it catches the small strangenesses that make a site feel amateurish in a local market. Cheapest, highest-ROI remaining task.
- **Typography primitives / card / input components.** Only `button.tsx` and `BorderGlow.tsx` exist under `src/components/ui/`.

## Global components available
From `@/components/global`:
- `AnimateIn` — `whileInView` single-element fade-in-up (configurable variants/delay/once/amount/as).
- `StaggerContainer` — `whileInView` parent; children use `staggerItem` variant.
- `MotionWrapper` — `MotionConfig reducedMotion="user"`. Mounted once in root layout.
- `DivisionProvider` — reads `usePathname()` and renders a wrapper `<div data-division={division}>`.
- `ScrollProgress` — top-of-page 2px progress bar, color tracks `--division-accent`.
- `BackToTop` — circular glass button, appears after 500px scroll, respects reduced motion via parent config.
- `Section` — server component, `py-20 md:py-28`, inner `max-w-7xl` with responsive padding, optional `fullWidth` + `id`.
- `Navbar` — see above.
- `Footer` — see above. No props; reads `footerNavItems` + `siteConfig`.

## Background components available
From `@/components/backgrounds`:
- `BackgroundSilk` — R3F shader, used on `/`. Props: `color` (`#7B7481`), `speed`, `scale`, `noiseIntensity`, `rotation`.
- `BackgroundPlasma` — OGL shader, used on `/marketing`. Props: `color` (`#9474D4`), `speed`, `direction`, `scale`, `opacity`, `mouseInteractive`.
- `BackgroundGrid` — GSAP DOM grid, used on `/consulting`. Props: `items` (default consulting vocabulary), `gradientColor`.

All three ship with a `prefers-reduced-motion` fallback (flat `--division-bg`) and are lazy-loaded via `next/dynamic` with `ssr: false`.

## Animation system
All variants in `src/lib/animations.ts`:
- Transitions: `springSnap` (default), `springPop`, `springGentle`, `easeOut`, `easeOutSlow`.
- Entrance: `fadeInUp`, `fadeInUpSlow`, `fadeInLeft`, `fadeInRight`, `fadeIn`, `scaleIn`.
- Stagger: `staggerContainer` (default), `staggerContainerSlow`, `staggerContainerFast`, `staggerItem`.
- Hover/tap (objects, not variants): `hoverLift`, `hoverScale`, `hoverGlow`.
- Hero: `heroHeadline`, `heroSubtitle`, `heroCTA`.

## Division theming
- **Detection:** path-based via `getDivisionFromPath(pathname)` in `src/lib/divisions.ts` — `/consulting*`, `/marketing*`, else `shared`.
- **Provider:** `DivisionProvider` reads the pathname, emits `<div data-division={division}>`. No React context is used.
- **CSS:** `[data-division="consulting"]` and `[data-division="marketing"]` blocks in `globals.css` override both the shadcn HSL tokens (`--background`, `--foreground`, `--primary`, …) and the bespoke `--division-*` tokens. The default (shared) values sit on `:root`.
- **Components read `var(--division-accent)`, `var(--division-text-primary)`, etc.** via inline `style={{...}}` or bracket classes `text-[var(--division-text-secondary)]`. Tailwind utilities (`text-consulting-400`, `bg-marketing-bg`) still work and are independent of the data-division swap.

## Navigation structure
From `src/config/navigation.ts`:

`mainNavItems`:
- Consulting → /consulting
  - Business Consulting → /consulting/business-consulting
  - Workflow Restructuring → /consulting/workflow-restructuring
  - IT & Systems → /consulting/it-systems
  - AI Consulting → /consulting/ai-consulting
- Marketing → /marketing
  - Web Design → /marketing/web-design
  - Social Media → /marketing/social-media
  - IT Infrastructure → /marketing/it-infrastructure
  - AI Development → /marketing/ai-development
- About → /about
- Blog → /blog
- Contact → /contact

`footerNavItems` is the same set regrouped as `consulting` / `marketing` / `company` (adds `Privacy Policy` → /privacy).

## Site config (`src/config/site.ts`)
- `name`: Vertex Consulting
- `legalName`: ВЕРТЕКС КОНСАЛТИНГ ДООЕЛ
- `url`: https://vertexconsulting.mk
- `owner`: Goran Dinov, founded 2018
- `address`: Str. Mladinska 43, Strumica, Macedonia
- `contact.phone`: +389 70 214 033
- `contact.emailInfo` / `contact.emailMarketing`
- `hours`: Monday to Friday, 09:00 to 17:00
- `social.linkedin` / `social.instagram` / `social.facebook` — Instagram and Facebook are real; LinkedIn is a `linkedin.com` placeholder until the account exists.
- `divisions.consulting.team`: ["Goran Dinov"]
- `divisions.marketing.team`: ["Lazar", "Petar", "Andrej"]

## Known issues / technical debt
- **BlogCard dates on `/mk` render in English format.** `BlogCard.tsx` uses next-intl's `useFormatter().dateTime()` — server and client agree (no hydration mismatch), but Node's Intl data for `mk-MK` on this Windows runtime falls back to English. Phase 15F addresses; working as-is for now.
- **`lucide-react@1.8.0` ships no brand icons.** This is a misleading version number — it's not the usual 0.x Lucide library. Footer needs inline SVGs for LinkedIn/Instagram/Facebook as a result. If a future phase upgrades the package, those inline SVGs can be swapped back to lucide imports.
- **`blog/[slug]/page.tsx` has no data loader or `generateStaticParams`.** Build of SSG output may behave unexpectedly; no reproduction steps yet.
- **`src/types/index.ts`** duplicates `NavItem` from `src/config/navigation.ts` with a weaker shape. Treat the config one as canonical.
- **Navbar mobile overlay covers `ScrollProgress`** — the bar is `z-60` but the overlay is `z-40`, so this is fine; noted here only so future refactors don't flip the order.
- **Dev server must be restarted after edits to `src/proxy.ts`.** Turbopack caches the compiled edge handler under `.next/dev/server/edge/` and can fail to pick up changes. `rm -rf .next` + preview restart is the fix.

## What comes next
- **Phase L5 — Sections + pages sweep (still pending — L6 handled the user-visible subset).** L6 closed the user-reported hover-text-white bugs in DivisionSplit / BlogCard / MarketingServicesGrid / ServicesOverview, plus tokenized DivisionSplit's `rgba(245,245,245,α)` hover border + icon bg + chip bg/color. Remaining L5 backlog — now smaller: HeroSection shell (`hover:bg-white/5`), ServicesOverview + ConsultingServicesGrid + MarketingServicesGrid card icon container *backgrounds* (`bg-white/5 group-hover:bg-white/10` — L6 only fixed the icon SVG colors, not the wrapper bg), TeamGrid + TeamShowcase avatar borders (`border-white/10 bg-white/5` / `border-white/20 bg-[rgba(255,255,255,0.08)]`), MarketingServicePage + ConsultingServicePage related-services pills, FAQAccordion trigger hover, BlogListingClient filter pill borders, ContactForm, privacy page, thank-you page. Pattern: where a `hover:bg-white/N` / `border-white/N` exists, either convert to `hover:bg-[var(--nav-hover-bg)]` (for the hover overlay pattern already established in L4) or invent a more specific token if the semantics diverge. Also re-audit landing clients, CTA banners, blog cards for any hardcoded hex Session E missed; confirm all `prose-*` + body-level section bg/text flow cleanly (most should, given L1 flipped `--division-*` + shadcn HSL cleanly).
- **Phase 16 — Performance Audit & Launch.** Post-L5 the light-mode surface is complete. Next launch-track work: a final Lighthouse/Core-Web-Vitals pass, native MK review pass (flagged in `TRANSLATION_NOTES.md`), lawyer-reviewed MK privacy text, and production deploy.
- **Silk light-mode color tuning (optional micro-phase).** `SILK_COLOR_LIGHT` is currently `#CDD1D7` in `BackgroundSilk.tsx` — reads heavier than ideal (subtitle text contrast compressed). Proposed: lighten to `#E8EBEE` or `#F0F2F5`, or lower `noiseIntensity` to `0.8`/`1.0` in light theme via a threaded prop. Single-file change, ~10 lines. Awaiting user direction on chosen value before applying.
- **Native-speaker MK review.** 30-minute pass by a native Macedonian speaker over `messages/mk.json`. Now includes the Phase 12 `chat.*` namespace (5–10 extra min — see `TRANSLATION_NOTES.md` §12-A). Highest-ROI remaining task.
- **Privacy policy MK translation** — separate task, needs lawyer review. Schedule after legal consultation. Non-blocking for soft launch (notice banner is in place).
- **Phase 12B — Lead capture + Telegram.** The core widget is live; still to add: intent detection (lightweight second LLM call, or a keyword heuristic), name+email capture form slide-in at the bottom of `ChatPanel`, `/api/chat/lead` route wiring to Resend + Telegram, conversation-summary generation via a second Claude call, per-IP rate limiting on the lead route. See `D-15_Website_Design_Document.md` §5 for the full spec and `src/_project-state/12_phase-12-chat-widget.md` → "What the next phase should know" for the integration points.
- **Phase 13 — Sanity CMS.** Replace mock blog data in `src/lib/blog.ts` with Sanity queries matching the existing signatures (`getAllPosts`, `getPostBySlug`, `getPostsByDivision`, `getRelatedPosts`). 15F's locale-keyed shape (`Record<string, Record<Locale, BlogPost>>`) maps cleanly onto Sanity's per-language document pattern. Swap the simple Markdown renderer in `BlogPostClient` for `@portabletext/react`. Once landed, extend the chat widget's system prompt to include service descriptions from Sanity so Claude can answer specifics the current hard-coded `siteConfig` prompt doesn't cover.
- **Phase 14 — SEO & Structured Data (extended).** 15F shipped bilingual hreflang + sitemap + robots + BlogPosting + FAQPage `inLanguage`. Phase 14's still-pending items: `Organization`, `LocalBusiness`, `WebSite`, `BreadcrumbList` JSON-LD. Optional: `/llms.txt`.
- **Phase 16 — Performance Audit & Launch.** Lighthouse / CWV pass, Resend delivery test under load, final polish.
- **Telegram notification on contact form** could be a future addition — the Resend send in `/api/contact` is already landing the email in Goran's Gmail, but a Telegram ping would add real-time awareness. Easy to add: one `fetch` call after the successful Resend send in the route handler.
- **Footer LinkedIn URL** needs a real account link once Vertex creates the LinkedIn page (Instagram and Facebook are now real).
- **Privacy policy MK translation** needed once the bilingual setup (Phase 15) lands.
- **Cookie banner** is not implemented. Section 9 of the privacy policy mentions cookies, but the site does not yet ask for consent. If analytics that require consent are added before Phase 15, a cookie banner becomes legally required.
